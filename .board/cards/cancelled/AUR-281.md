---
id: AUR-281
version: 1
title: Documentar contratos de providers
status: cancelled
validation: tested
profile_owner: AUR-408
office: O12-release
depends_on: [AUR-067, AUR-196, AUR-408]
requirements: [PR-LLM-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [docs/contracts/providers, examples/contracts/providers, tests/acceptance/AUR-281.sh, docs/specs/AUR-281.md, tests/unit/AUR-281.go, tests/contracts/AUR-281.go, tests/integration/AUR-281.go, tests/e2e/AUR-281.sh]
read_paths: [go.mod, go.sum, internal, docs, .board/oci/profiles/registry.v1.json, .board/oci/profiles/docs-tool-offline-v1.json, .board/locks/oci/docs-tool-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

cada provider habilitado possui configuração sem segredo e um exemplo que passa a mesma contract suite.

## Non-goals

- Habilitar provider novo ou alterar o adapter de provider em `internal/`; o card documenta e exemplifica apenas os já habilitados.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-067, AUR-196 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- cada provider habilitado possui configuração sem segredo e um exemplo que passa a mesma contract suite.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Documentar contratos de providers

- Given: `docs/contracts/providers` enumera os providers habilitados e `examples/contracts/providers` traz um exemplo por provider, com o profile `docs-tool-offline-v1` sem rede.
- When: a contract suite de providers roda uma vez sobre cada exemplo de `examples/contracts/providers` dentro do profile `docs-tool-offline-v1`.
- Then: todos os exemplos de provider passam a mesma contract suite e `.board/evidence/AUR-281/acceptance/AC-001.json` registra provider, arquivo de exemplo e verdict, com o scan de segredo vazio.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-LLM-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Documentar contratos de providers” em `docs/contracts/providers`, `examples/contracts/providers`, `tests/acceptance/AUR-281.sh`, `docs/specs/AUR-281.md`.

## TDD proof

- Test: `tests/acceptance/AUR-281.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-281/AC-001: Documentar contratos de providers ausente`; build/loader/infra não valem.
- Green: escrever a página de contrato por provider em `docs/contracts/providers` e os exemplos correspondentes em `examples/contracts/providers`, usando só campos portáveis e referência de segredo por variável de ambiente.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-281.go::TestAUR281`.
- Contract: `tests/contracts/AUR-281.go::ContractAUR281`.
- Integration: `tests/integration/AUR-281.go::IntegrationAUR281`.
- E2E: `tests/e2e/AUR-281.sh::E2EAUR281`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-281`

Expected artifact: `.board/evidence/AUR-281/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: trocar o exemplo LiteLLM por campo vendor-only.
- Expected: o acceptance falha com `AUR-281/AC-001/MUT-001`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository` -> “Documentar contratos de providers” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-281.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
