---
id: AUR-298
version: 1
title: Allowlistar evento de webhook
status: cancelled
validation: tested
profile_owner: AUR-409
office: O06-scm
depends_on: [AUR-098, AUR-409]
requirements: [PR-SCM-002, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-003, CR-GH-005]
paths: [internal/webhook/event.go, internal/webhook/event_test.go, tests/acceptance/AUR-298.sh, docs/specs/AUR-298.md, tests/unit/AUR-298.go, tests/contracts/AUR-298.go, tests/integration/AUR-298.go, tests/e2e/AUR-298.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/fake-scm-offline-v1.json, .board/locks/oci/fake-scm-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, scm]
---

## Outcome

somente eventos e actions versionados são normalizados; desconhecido retorna ignored-with-reason sem enqueue.

## Non-goals

- Acrescentar tipos de evento ao domínio e rotear eventos permitidos para handlers específicos; entrega normalização e filtro.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-098 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- somente eventos e actions versionados são normalizados; desconhecido retorna ignored-with-reason sem enqueue.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Allowlistar evento de webhook

- Given: `internal/webhook/event.go` conhece uma allowlist versionada de eventos e actions, e o fixture envia um evento permitido e um evento desconhecido.
- When: os dois deliveries passam pela normalização de evento executada no profile `fake-scm-offline-v1`.
- Then: o evento permitido é normalizado e enfileirado, o desconhecido devolve ignored-with-reason sem enqueue, e a fila termina com exatamente um item.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-SCM-002, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-003, CR-GH-005]

## Public contract

- Somente “Allowlistar evento de webhook” em `internal/webhook/event.go`, `internal/webhook/event_test.go`, `tests/acceptance/AUR-298.sh`, `docs/specs/AUR-298.md`.

## TDD proof

- Test: `tests/acceptance/AUR-298.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-298/AC-001: Allowlistar evento de webhook ausente`; build/loader/infra não valem.
- Green: implementar em `internal/webhook/event.go` a allowlist versionada de eventos e actions e o retorno ignored-with-reason para o que estiver fora dela, testado em `internal/webhook/event_test.go`.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-298.go::TestAUR298`.
- Contract: `tests/contracts/AUR-298.go::ContractAUR298`.
- Integration: `tests/integration/AUR-298.go::IntegrationAUR298`.
- E2E: `tests/e2e/AUR-298.sh::E2EAUR298`.

## Acceptance

container_profile: `fake-scm-offline-v1`

accept: `./.board/bin/oci-run --profile fake-scm-offline-v1 --card AUR-298`

Expected artifact: `.board/evidence/AUR-298/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: enfileirar evento desconhecido.
- Expected: o acceptance falha com `AUR-298/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / scm

- Change: mudar o head após leitura ou duplicar efeito.
- Expected: o acceptance falha com `AUR-298/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `scm` -> “Allowlistar evento de webhook” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-298.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
