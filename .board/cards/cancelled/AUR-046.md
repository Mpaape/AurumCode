---
id: AUR-046
version: 1
title: Indexar entidade de arquivo como fallback
status: cancelled
validation: tested
profile_owner: AUR-407
office: O02-index
depends_on: [AUR-045, AUR-407]
requirements: [PR-ARCH-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-006]
paths: [internal/index/entity, tests/acceptance/AUR-046.sh, docs/specs/AUR-046.md, tests/specs/AUR-046, tests/contracts/AUR-046.go, tests/unit/AUR-046.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository, database, git-metadata]
---

## Outcome

linguagem desconhecida ou parser falho produz entidade de arquivo e estado Partial explícito.

## Non-goals

- Não tenta aproveitar entidades de um parse interrompido no meio: o fallback substitui o resultado inteiro do arquivo por uma única entidade de arquivo.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-045 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- linguagem desconhecida ou parser falho produz entidade de arquivo e estado Partial explícito.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Indexar entidade de arquivo como fallback

- Given: `tests/specs/AUR-046/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Indexar entidade de arquivo como fallback”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Indexar entidade de arquivo como fallback” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que linguagem desconhecida ou parser falho produz entidade de arquivo e estado Partial explícito.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-006]

## Public contract

- Somente o contrato “Indexar entidade de arquivo como fallback” e os artefatos em `internal/index/entity`, `tests/acceptance/AUR-046.sh`, `docs/specs/AUR-046.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-046.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-046/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/index/entity` para emitir uma entidade de granularidade arquivo quando a linguagem é desconhecida ou o parser falha, marcando o resultado como Partial com o motivo registrado.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“descartar silenciosamente arquivo não parseável.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-046.go::TestAUR046`.
- Contract: `tests/contracts/AUR-046.go::ContractAUR046`.
- Integration: not-applicable: o comportamento é local e coberto pelo acceptance.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `sqlite-offline-v1`

accept: `./.board/bin/oci-run --profile sqlite-offline-v1 --card AUR-046`

Expected artifact: `.board/evidence/AUR-046/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Indexar entidade de arquivo como fallback” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: descartar silenciosamente arquivo não parseável.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-046/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / database

- Change: corromper o cabeçalho ou exceder o budget do SQLite durante a operação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-046/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-003: AC-001 / git-metadata

- Change: resolver o alvo para a raiz do worktree em vez do Git dir autorizado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-046/AC-001/MUT-003`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `database`, `git-metadata` -> contrato “Indexar entidade de arquivo como fallback” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-046.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
