---
id: AUR-267
version: 1
title: Expor skill list por MCP
status: cancelled
validation: tested
profile_owner: AUR-403
office: O11-mcp
depends_on: [AUR-072, AUR-169, AUR-403]
requirements: [PR-MCP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]
paths: [internal/mcp/tools/read/skill_list.go, tests/acceptance/AUR-267.sh, docs/specs/AUR-267.md, tests/unit/AUR-267.go, tests/contracts/AUR-267.go, tests/integration/AUR-267.go, tests/e2e/AUR-267.sh]
read_paths: [go.mod, go.sum, internal, .board/oci/profiles/registry.v1.json, .board/oci/profiles/go-unit-offline-v1.json, .board/locks/oci/go-unit-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, mcp-client]
---

## Outcome

a tool skill_list retorna nomes, versões, digests e capabilities declaradas sem conteúdo sensível.

## Non-goals

- Devolver o prompt body ou os arquivos de suporte das skills instaladas.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-072, AUR-169 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a tool skill_list retorna nomes, versões, digests e capabilities declaradas sem conteúdo sensível.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Expor skill list por MCP

- Given: os manifests aprovados de AUR-072, AUR-169 estão disponíveis na mesma base, fixture bounded e perfil OCI ativo.
- When: a tool MCP `skill_list` é chamada uma vez sobre um registro com duas skills instaladas, uma delas com prompt body extenso.
- Then: o resultado lista nome, versão, digest e capabilities declaradas de cada skill, sem nenhum trecho do prompt body, e a evidência AC-001 registra o digest de cada entrada.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-MCP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]

## Public contract

- Introduz somente “Expor skill list por MCP” nos artefatos `internal/mcp/tools/read/skill_list.go`, `tests/acceptance/AUR-267.sh`, `docs/specs/AUR-267.md`.

## TDD proof

- Test: `tests/acceptance/AUR-267.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-267/AC-001: Expor skill list por MCP ausente`; build/loader/infra não valem.
- Green: implementar `internal/mcp/tools/read/skill_list.go` projetando apenas nome, versão, digest e capabilities do manifest de cada skill, sem carregar o corpo do prompt na resposta.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-267.go::TestAUR267`.
- Contract: `tests/contracts/AUR-267.go::ContractAUR267`.
- Integration: `tests/integration/AUR-267.go::IntegrationAUR267`.
- E2E: `tests/e2e/AUR-267.sh::E2EAUR267`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-267`

Expected artifact: `.board/evidence/AUR-267/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: retornar prompt body completo da skill.
- Expected: o acceptance inalterado falha com `AUR-267/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / mcp-client

- Change: injetar instrução de elevação num tool result.
- Expected: o acceptance inalterado falha com `AUR-267/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `mcp-client` -> contrato “Expor skill list por MCP” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-267.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
