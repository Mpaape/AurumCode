---
id: AUR-039
version: 1
title: Ler árvore Git sem executar checkout
status: cancelled
validation: tested
profile_owner: AUR-404
office: O02-index
depends_on: [AUR-024, AUR-029, AUR-233, AUR-404]
requirements: [PR-ARCH-001, PR-REV-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-COV-001, CR-COV-002, CR-COV-005]
paths: [internal/gitread, internal/ports/gittree.go, internal/ports/gittree_test.go, tests/acceptance/AUR-039.sh, docs/specs/AUR-039.md, tests/specs/AUR-039, tests/contracts/AUR-039.go, tests/integration/AUR-039.go, tests/e2e/AUR-039.sh, tests/unit/AUR-039.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, git-object-store, filesystem]
---

## Outcome

commits, trees e blobs são enumerados sem hooks, scripts ou submodule execution.

## Non-goals

- Não faz checkout, fetch ou qualquer escrita no repositório observado: `internal/gitread` abre a árvore em modo somente leitura sobre o SHA já fixado.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-024, AUR-029, AUR-233 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- commits, trees e blobs são enumerados sem hooks, scripts ou submodule execution.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Ler árvore Git sem executar checkout

- Given: `tests/specs/AUR-039/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Ler árvore Git sem executar checkout”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Ler árvore Git sem executar checkout” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que commits, trees e blobs são enumerados sem hooks, scripts ou submodule execution.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-COV-001, CR-COV-002, CR-COV-005]

## Public contract

- Somente o contrato “Ler árvore Git sem executar checkout” e os artefatos em `internal/gitread`, `internal/ports/gittree.go`, `internal/ports/gittree_test.go`, `tests/acceptance/AUR-039.sh`, `docs/specs/AUR-039.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-039.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-039/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/gitread` atrás da porta `internal/ports/gittree.go` enumerando commits, trees e blobs por leitura de objetos, com hooks e filtros desligados e recursão em submodule recusada por erro tipado.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“seguir automaticamente um submodule hostil.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-039.go::TestAUR039`.
- Contract: `tests/contracts/AUR-039.go::ContractAUR039`.
- Integration: `tests/integration/AUR-039.go::IntegrationAUR039`.
- E2E: `tests/e2e/AUR-039.sh::E2EAUR039`.

## Acceptance

container_profile: `go-git-offline-v1`

accept: `./.board/bin/oci-run --profile go-git-offline-v1 --card AUR-039`

Expected artifact: `.board/evidence/AUR-039/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Ler árvore Git sem executar checkout” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: seguir automaticamente um submodule hostil.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-039/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / git-object-store

- Change: trocar o blob validado por outro SHA após a seleção.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-039/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-003: AC-001 / filesystem

- Change: substituir um caminho permitido por symlink que escape do repositório.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-039/AC-001/MUT-003`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `git-object-store`, `filesystem` -> contrato “Ler árvore Git sem executar checkout” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-039.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
