---
id: AUR-311
version: 1
title: Retirar pkg types por compatibilidade
status: cancelled
validation: tested
profile_owner: AUR-403
office: O14-legacy
depends_on: [AUR-001, AUR-023, AUR-403]
requirements: [PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [pkg/types, tests/migration/legacy/pkg-types, tests/acceptance/AUR-311.sh, docs/specs/AUR-311.md, tests/unit/AUR-311.go, tests/contracts/AUR-311.go, tests/integration/AUR-311.go, tests/e2e/AUR-311.sh]
read_paths: [go.mod, go.sum, internal, .board/oci/profiles/registry.v1.json, .board/oci/profiles/go-unit-offline-v1.json, .board/locks/oci/go-unit-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

consumidores de pkg/types são mapeados e cada tipo migra ou ganha alias deprecated antes de remoção.

## Non-goals

- Não apagar `pkg/types` nem encerrar o período de deprecation; a remoção física dos aliases depende de card posterior com os consumidores já migrados.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-001, AUR-023 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- consumidores de pkg/types são mapeados e cada tipo migra ou ganha alias deprecated antes de remoção.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Retirar pkg types por compatibilidade

- Given: os manifests de AUR-001 e AUR-023 estão selados e o grafo de importadores de `pkg/types` está congelado como fixture bounded em `tests/migration/legacy/pkg-types`.
- When: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-311` compila os consumidores offline e confronta cada tipo exportado de `pkg/types` com o mapa de importadores.
- Then: `.board/evidence/AUR-311/acceptance/AC-001.json` lista cada tipo de `pkg/types` como migrado ou com alias deprecated e seus importadores; tipo removido enquanto ainda importado sai 1 e reprova o manifesto.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Retirar pkg types por compatibilidade” em `pkg/types`, `tests/migration/legacy/pkg-types`, `tests/acceptance/AUR-311.sh`, `docs/specs/AUR-311.md`.

## TDD proof

- Test: `tests/acceptance/AUR-311.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-311/AC-001: Retirar pkg types por compatibilidade ausente`; build/loader/infra não valem.
- Green: gerar o mapa de importadores de `pkg/types` e emitir, por tipo, ou a referência de destino migrado ou o alias deprecated correspondente antes de permitir qualquer remoção.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-311.go::TestAUR311`.
- Contract: `tests/contracts/AUR-311.go::ContractAUR311`.
- Integration: `tests/integration/AUR-311.go::IntegrationAUR311`.
- E2E: `tests/e2e/AUR-311.sh::E2EAUR311`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-311`

Expected artifact: `.board/evidence/AUR-311/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: deletar tipo ainda importado por consumidor.
- Expected: o acceptance falha com `AUR-311/AC-001/MUT-001`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository` -> “Retirar pkg types por compatibilidade” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-311.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
