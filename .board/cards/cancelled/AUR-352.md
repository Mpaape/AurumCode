---
id: AUR-352
version: 1
title: Inventariar documentação gerada de scripts sem execução
status: cancelled
validation: tested
profile_owner: AUR-408
office: O14-legacy
depends_on: [AUR-001, AUR-009, AUR-312, AUR-408]
requirements: [PR-ARCH-001, PR-DOC-003, PR-SEC-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-004, CR-TRUST-005]
paths: [.aurumcode/bash, tests/characterization/legacy/aurumcode-bash-docs, tests/acceptance/AUR-352.sh, docs/specs/AUR-352.md]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, process-runtime]
---

## Outcome

Um manifesto liga cada `.aurumcode/bash/*.md` ao script-fonte alegado, hash,
ownership e disposição, com `executed=false` e sem avaliar blocos shell.

## Non-goals

- Executar, regenerar ou corrigir scripts/docs; AUR-312 decide os scripts raiz.
- Declarar que texto gerado é atual sem prova de source digest.

## Preconditions

- AUR-001 sela a árvore, AUR-009 define canários e AUR-312 caracteriza scripts-fonte.
- Parser Markdown lexical nega subprocess, link fetch e shell evaluation.

## Postconditions

- Cada doc possui source relation ou `orphan`, disposição e `executed=false`.
- Docs e scripts permanecem intactos, com zero processo/rede.

## Acceptance scenarios

### AC-001: Mapear bash docs sem rodar snippets

- Given: árvore hash-lockada e fixtures com orphan, stale digest e fenced command hostil.
- When: parser extrai apenas metadata/source references.
- Then: todos os docs aparecem com relation/disposition e execution flag false.
- And: replay/source hashes coincidem e process audit é vazio.

- Covers requirements: [PR-ARCH-001, PR-DOC-003, PR-SEC-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-004, CR-TRUST-005]

## Public contract

- `manifest.tsv` usa doc path/hash/source path/source digest/status/disposition/executed; não contém fenced body.

## TDD proof

- Test: `tests/acceptance/AUR-352.sh::AC-001`.
- Red: doc omitido ou execution flag ausente falha sem executar snippet.
- Green: parser local produz manifest e hashes estáveis.
- Refactor: relation matching mantém statuses, order e digest.
- Unit: `tests/characterization/legacy/aurumcode-bash-docs::unit`.
- Contract: `tests/acceptance/AUR-352.sh::contract`.
- Integration: `tests/characterization/legacy/aurumcode-bash-docs::integration`.
- E2E: not-applicable: regeneração é card posterior.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-352`

Expected artifact: `.board/evidence/AUR-352/acceptance/AC-001.json` vincula relations, hashes, no-process audit e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: inserir doc orphan/stale depois da enumeração.
- Expected: tree/manifest coverage ou source digest falha AC-001.

### MUT-002: AC-001 / process-runtime

- Change: executar fenced command sintético durante parse.
- Expected: process/canary probe invalida `executed=false` e veta.

## Security and privacy

- Data flow: headings/source refs/hashes -> TSV; fenced bodies/output não são sinks permitidos.
- Secrets: canário sintético em code block não aparece no manifesto.
- Least privilege: read-only, zero shell/process/network.

## Documentation

- `docs/specs/AUR-352.md` define relation/status/disposition e non-execution.

## Compatibility, migration, rollback

- Compatibility: docs permanecem byte-idênticos.
- Migration: orphan/stale recebe card de regen/remove individual.
- Rollback: remover somente derivados.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
