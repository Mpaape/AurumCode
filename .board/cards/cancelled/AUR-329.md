---
id: AUR-329
version: 1
title: Parsear blob Rust sem estado
status: cancelled
validation: tested
profile_owner: AUR-406
office: O02-index
depends_on: [AUR-224, AUR-406]
requirements: [PR-REV-001, PR-MEM-002, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-COV-002, CR-COV-005]
paths: [internal/parsers/grammars/rust, tests/fixtures/polyglot/rust/entities, tests/acceptance/AUR-329.sh, docs/specs/AUR-329.md, tests/unit/AUR-329.go, tests/contracts/AUR-329.go, tests/integration/AUR-329.go, tests/e2e/AUR-329.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/parser-worker-v1.json, .board/locks/oci/parser-worker-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, parser-worker, filesystem]
---

## Outcome

uma entrada Rust válida produz module, trait, impl, struct e function sem executar macro.

## Non-goals

- Não expandir macros, rodar cargo ou rustc nem resolver `mod` para outros arquivos; o adapter descreve somente o que está no blob.

## Preconditions

- O contrato AUR-224 e o worker pinado estão aprovados na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- uma entrada Rust válida produz module, trait, impl, struct e function sem executar macro.
- Nenhum processo do repo é iniciado e nenhum path fora de `paths` muda.

## Acceptance scenarios

### AC-001: Parsear blob Rust sem estado

- Given: um blob rust golden e um hostil, ambos bounded e ligados a blob SHA.
- When: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-329` entrega os bytes do blob Rust, o language tag `rust` e o grammar digest ao worker, sem cargo ou rustc disponíveis no profile.
- Then: `.board/evidence/AUR-329/acceptance/AC-001.json` traz module, trait, impl, struct e function conforme a golden de `tests/fixtures/polyglot/rust/entities`, e a macro não expandida aparece marcada, sem range inventado.
- And: replay devolve entidades byte-idênticas em ordem canônica.

- Covers requirements: [PR-REV-001, PR-MEM-002, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-COV-002, CR-COV-005]

## Public contract

- Adiciona somente o adapter rust ao ParserPort e os artifacts `internal/parsers/grammars/rust`, `tests/fixtures/polyglot/rust/entities`, `tests/acceptance/AUR-329.sh`, `docs/specs/AUR-329.md`.

## TDD proof

- Test: `tests/acceptance/AUR-329.sh::AC-001`.
- Red: o contract suite falha com `AUR-329/AC-001: Parsear blob Rust sem estado ausente`, não por grammar/image indisponível.
- Green: registrar o adapter `rust` em `internal/parsers/grammars/rust` emitindo module, trait, impl, struct e function do texto e marcando invocação de macro como não expandida, sem range sintético.
- Refactor: preservar entity IDs, ranges, ordenação e typed errors.
- Unit: `tests/unit/AUR-329.go::TestAUR329`.
- Contract: `tests/contracts/AUR-329.go::ContractAUR329`.
- Integration: `tests/integration/AUR-329.go::IntegrationAUR329`.
- E2E: `tests/e2e/AUR-329.sh::E2EAUR329`.

## Acceptance

container_profile: `parser-worker-v1`

accept: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-329`

Expected artifact: `.board/evidence/AUR-329/acceptance/AC-001.json` contém golden digest, parser digest e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: inventar range para macro não expandida.
- Expected: o acceptance falha com `AUR-329/AC-001/MUT-001`; restore passa e compile/infra não conta.

### MUT-002: AC-001 / parser-worker

- Change: trocar grammar digest ou responder depois do timeout.
- Expected: o acceptance falha com `AUR-329/AC-001/MUT-002`; não aceita entidades parciais como sucesso.

### MUT-003: AC-001 / filesystem

- Change: fornecer symlink/source relation que escape do snapshot.
- Expected: o acceptance falha com `AUR-329/AC-001/MUT-003`, sem leitura externa; restore passa.

## Security and privacy

- Data flow: bytes do Git blob -> worker pinado -> Entity records bounded.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: worker não-root, read-only, network none, sem socket/mount/device e sem execução do blob.

## Documentation

- `docs/specs/AUR-329.md` fixa grammar/version, entidades, unsupported syntax, limites e erro.

## Compatibility, migration, rollback

- Compatibility: ParserPort e outras linguagens permanecem inalterados.
- Migration: not-applicable; parser version nova invalida derived entities por provenance.
- Rollback: remover registro rust e derived entities daquela parser version; fonte não muda.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
