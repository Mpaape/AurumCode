---
id: AUR-228
version: 1
title: Executar worker não-root e read-only
status: cancelled
validation: tested
profile_owner: AUR-410
office: O08-testqa
depends_on: [AUR-227, AUR-410]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/sandbox/profile/nonroot.go, tests/containers/sandbox/nonroot, tests/acceptance/AUR-228.sh, docs/specs/AUR-228.md, tests/unit/AUR-228.go, tests/contracts/AUR-228.go, tests/integration/AUR-228.go, tests/e2e/AUR-228.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/oci-conformance-v1.json, .board/locks/oci/oci-conformance-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

o worker inicia com UID/GID não-zero, rootfs read-only, checkout read-only e apenas tmpfs bounded gravável.

## Non-goals

- Negar rede, socket do engine ou devices, e aplicar cap-drop e limites de CPU e memória: esses controles pertencem aos cards de isolamento e de recursos.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-227 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- o worker inicia com UID/GID não-zero, rootfs read-only, checkout read-only e apenas tmpfs bounded gravável.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Executar worker não-root e read-only

- Given: o manifest aprovado de AUR-227 fixa o selector de engine na mesma base, e `tests/containers/sandbox/nonroot` traz a fixture que tenta escrever no rootfs e no checkout montado.
- When: o worker é iniciado pelo perfil de `internal/sandbox/profile/nonroot.go` e a fixture consulta o UID efetivo e tenta gravar em rootfs, checkout e tmpfs.
- Then: o UID e o GID reportados são diferentes de zero, as escritas em rootfs e checkout falham como sistema de arquivos somente leitura, e apenas o tmpfs bounded aceita bytes até o tamanho declarado.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Executar worker não-root e read-only” nos artefatos `internal/sandbox/profile/nonroot.go`, `tests/containers/sandbox/nonroot`, `tests/acceptance/AUR-228.sh`, `docs/specs/AUR-228.md`.

## TDD proof

- Test: `tests/acceptance/AUR-228.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-228/AC-001: Executar worker não-root e read-only ausente`; build/loader/infra não valem.
- Green: preencher `internal/sandbox/profile/nonroot.go` com a flag de usuário não-zero, rootfs read-only, montagem read-only do checkout e um único tmpfs de tamanho fixo.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-228.go::TestAUR228`.
- Contract: `tests/contracts/AUR-228.go::ContractAUR228`.
- Integration: `tests/integration/AUR-228.go::IntegrationAUR228`.
- E2E: `tests/e2e/AUR-228.sh::E2EAUR228`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-228`

Expected artifact: `.board/evidence/AUR-228/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover --user ou --read-only do argv esperado.
- Expected: o acceptance inalterado falha com `AUR-228/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-228/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Executar worker não-root e read-only” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-228.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
