---
id: AUR-202
version: 1
title: Monitorar piloto sintético por evidência
status: cancelled
validation: tested
profile_owner: AUR-412
office: O12-release
depends_on: [AUR-195, AUR-196, AUR-197, AUR-198, AUR-199, AUR-200, AUR-201, AUR-190, AUR-399, AUR-412]
requirements: [PR-ARCH-001, PR-REL-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-005]
paths: [internal/monitors/pilot, tests/e2e/pilot, tests/acceptance/AUR-202.sh, docs/specs/AUR-202.md, tests/fixtures/AUR-202, tests/unit/AUR-202.go, tests/contracts/AUR-202.go, tests/integration/AUR-202.go, tests/e2e/AUR-202.sh]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, supply-chain, container-engine]
---

## Outcome

`AUR202ResultV1` é o único resultado reversível de “Monitorar piloto sintético por evidência” e limita a implementação a `internal/monitors/pilot`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “fazer monitor fechar o próprio incidente”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-195, AUR-196, AUR-197, AUR-198, AUR-199, AUR-200, AUR-201, AUR-190, AUR-399]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-202` contém exatamente um caso nominal e um contracaso mutado, sob 64 artefatos, 8 MiB total, 120 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR202ResultV1` canônico e o contracaso produz `AUR202_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Monitorar piloto sintético por evidência

- Given: CandidateIdentityV1, digests de [AUR-195, AUR-196, AUR-197, AUR-198, AUR-199, AUR-200, AUR-201, AUR-190, AUR-399] e a fixture bounded `tests/fixtures/AUR-202` com caso nominal e “fazer monitor fechar o próprio incidente”.
- When: o consumidor invoca “Monitorar piloto sintético por evidência” uma vez no port público card-scoped, com limite de 64 artefatos, 8 MiB total, 120 s e 2 CPUs.
- Then: monitor repete consumer demo e abre card em desvio sem autoaprovar; o consumidor recebe `AUR202ResultV1` com status e digest canônicos. O contracaso “fazer monitor fechar o próprio incidente” retorna `AUR202_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-REL-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-005]

## Public contract

- `AUR202ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Monitorar piloto sintético por evidência”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-202.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-202/AC-001: AUR202_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR202ResultV1` dentro de 64 artefatos, 8 MiB total, 120 s e 2 CPUs, enquanto “fazer monitor fechar o próprio incidente” retorna `AUR202_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-202.go::TestAUR202NominalAndMutated`.
- Contract: `tests/contracts/AUR-202.go::TestAUR202ResultV1`.
- Integration: `tests/integration/AUR-202.go::TestAUR202Boundary`.
- E2E: `tests/e2e/AUR-202.sh::AC-001`.

## Acceptance

container_profile: `release-build-v1`

accept: `./.board/bin/oci-run --profile release-build-v1 --card AUR-202`

Expected artifact: o coordenador valida `.board/evidence/AUR-202/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: fazer monitor fechar o próprio incidente.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-202/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / supply-chain

- Change: alterar digest pinado ou dependency lock.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-202/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / container-engine

- Change: solicitar root, privileged, host mount, device ou engine socket.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-202/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR202ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-202.md` fixa input, output, `AUR202_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR202ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
