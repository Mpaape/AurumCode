---
id: AUR-181
version: 1
title: Executar corpus de ataques de input
status: cancelled
validation: tested
profile_owner: AUR-406
office: O00-security
depends_on: [AUR-173, AUR-406]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [tests/adversarial/inputs, tests/acceptance/AUR-181.sh, docs/specs/AUR-181.md, tests/fixtures/AUR-181, tests/unit/AUR-181.go, tests/contracts/AUR-181.go, tests/integration/AUR-181.go, tests/e2e/AUR-181.sh]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, container-engine, secret-channel]
---

## Outcome

`AUR181ResultV1` é um manifesto determinístico que decide “Executar corpus de ataques de input” somente a partir de digests e verdicts dos produtores dependentes; este card não reexecuta nem reúne comportamentos implementáveis de forma independente.

## Non-goals

- Não relaxar o contrato para aceitar “remover limite de bytes do diff”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-173]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-181` contém exatamente um caso nominal e um contracaso mutado, sob 64 casos, 4 MiB total, 60 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR181ResultV1` canônico e o contracaso produz `AUR181_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Executar corpus de ataques de input

- Given: CandidateIdentityV1, digests de [AUR-173] e a fixture bounded `tests/fixtures/AUR-181` com caso nominal e “remover limite de bytes do diff”.
- When: o harness de `tests/adversarial/inputs` percorre o corpus selado de ataques de input e confere path e digest de cada verdict sob `parser-worker-v1`, sem reenviar payload hostil ao parser, dentro de 64 casos, 4 MiB, 60 s e 2 CPUs.
- Then: o coordenador recebe `AUR181ResultV1` com uma linha por input, aggregate digest e verdict tipado; o contracaso “remover limite de bytes do diff” retorna `AUR181_INVALID_INPUT` sem manifesto aprovado.
- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- `AUR181ResultV1` contém apenas input IDs, digests, verdicts, aggregate digest e erro tipado; não contém payload, comportamento reexecutado, segredo ou autoridade humana.

## TDD proof

- Test: `tests/acceptance/AUR-181.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-181/AC-001: AUR181_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR181ResultV1` dentro de 64 casos, 4 MiB total, 60 s e 2 CPUs, enquanto “remover limite de bytes do diff” retorna `AUR181_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-181.go::TestAUR181NominalAndMutated`.
- Contract: `tests/contracts/AUR-181.go::TestAUR181ResultV1`.
- Integration: `tests/integration/AUR-181.go::TestAUR181Boundary`.
- E2E: `tests/e2e/AUR-181.sh::AC-001`.

## Acceptance

container_profile: `parser-worker-v1`

accept: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-181`

Expected artifact: o coordenador valida `.board/evidence/AUR-181/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover limite de bytes do diff.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-181/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / container-engine

- Change: solicitar root, privileged, host mount, device ou engine socket.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-181/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: injetar AURUM_SECRET_CANARY na falha.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-181/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR181ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-181.md` fixa input, output, `AUR181_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR181ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
