---
id: AUR-280
version: 1
title: Executar cenário demo PowerShell
status: cancelled
validation: tested
profile_owner: AUR-411
office: O09-demo
depends_on: [AUR-187, AUR-249, AUR-258, AUR-411]
requirements: [PR-DEMO-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [demo-repo/scenarios/powershell, tests/acceptance/AUR-280.sh, docs/specs/AUR-280.md, tests/unit/AUR-280.go, tests/contracts/AUR-280.go, tests/integration/AUR-280.go, tests/e2e/AUR-280.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

o consumer demo PowerShell prova review, docs e testgen em worker isolado.

## Non-goals

- Cenários demo em outras linguagens (Go, Python, TypeScript) e publicação do resultado do demo em qualquer forja.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-187, AUR-249, AUR-258 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- o consumer demo PowerShell prova review, docs e testgen em worker isolado.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Executar cenário demo PowerShell

- Given: `demo-repo/scenarios/powershell` contém um script .ps1 que passa entrada externa a Invoke-Expression e o profile `polyglot-toolchain-v1` está ativo com worker sem rede.
- When: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-280` executa o cenário demo PowerShell de ponta a ponta dentro do worker isolado.
- Then: `.board/evidence/AUR-280/acceptance/AC-001.json` lista o finding do .ps1, o doc gerado e o teste gerado com path e digest de cada um, e o worker encerra com exit code 0 sem egress.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-DEMO-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Somente “Executar cenário demo PowerShell” em `demo-repo/scenarios/powershell`, `tests/acceptance/AUR-280.sh`, `docs/specs/AUR-280.md`.

## TDD proof

- Test: `tests/acceptance/AUR-280.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-280/AC-001: Executar cenário demo PowerShell ausente`; build/loader/infra não valem.
- Green: adicionar o cenário .ps1 em `demo-repo/scenarios/powershell` e o driver de `tests/acceptance/AUR-280.sh` que dispara review, docs e testgen no worker `polyglot-toolchain-v1` e grava os três artefatos.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-280.go::TestAUR280`.
- Contract: `tests/contracts/AUR-280.go::ContractAUR280`.
- Integration: `tests/integration/AUR-280.go::IntegrationAUR280`.
- E2E: `tests/e2e/AUR-280.sh::E2EAUR280`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-280`

Expected artifact: `.board/evidence/AUR-280/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: habilitar Invoke-Expression e omitir finding.
- Expected: o acceptance falha com `AUR-280/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance falha com `AUR-280/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `container-engine` -> “Executar cenário demo PowerShell” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-280.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
