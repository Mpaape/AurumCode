---
id: AUR-387
version: 1
title: Caracterizar settings MCP do Gemini
status: cancelled
validation: tested
profile_owner: AUR-408
office: O14-legacy
depends_on: [AUR-001, AUR-009, AUR-021, AUR-068, AUR-408]
requirements: [PR-ARCH-001, PR-CFG-002, PR-MCP-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [.gemini/settings.json, tests/characterization/legacy/agent-settings/AUR-387, tests/acceptance/AUR-387.sh, docs/specs/AUR-387.md]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: restricted
trust_boundaries: [repository, mcp, secret-channel]
---

## Outcome

o manifesto contém uma linha para `.gemini/settings.json` com dialect `gemini-settings-json`, key-path digest, identifiers, `secret_values=0` e `commands_executed=0`.

## Non-goals

- Interpretar a semântica de configuração do Gemini CLI ou reconciliar `.gemini/settings.json` com `.cursor/mcp.json`; o card caracteriza o dialeto `gemini-settings-json` isolado.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-001, AUR-009, AUR-021, AUR-068]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/characterization/legacy/agent-settings/AUR-387/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- o manifesto contém uma linha para `.gemini/settings.json` com dialect `gemini-settings-json`, key-path digest, identifiers, `secret_values=0` e `commands_executed=0`.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Caracterizar settings MCP do Gemini

- Given: `tests/characterization/legacy/agent-settings/AUR-387/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 256 key paths, 1 MiB e 10 s.
- When: o parser non-executing abre literalmente `.gemini/settings.json`, rejeita symlink/duplicate key/campo desconhecido e emite somente paths/classes/digests.
- Then: o nominal sai 0 com `pass` e a linha de `.gemini/settings.json` traz `dialect=gemini-settings-json`, digest dos key paths, `secret_values=0` e `commands_executed=0`, sem herdar entrada de `.cursor/mcp.json`; duplicate key, symlink/tamper, chave desconhecida e over-limit saem 1 com `settings_parse_inconclusive`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded`.
- Covers requirements: [PR-ARCH-001, PR-CFG-002, PR-MCP-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `settings_parse_inconclusive|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-387.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-387/AC-001/settings_parse_inconclusive`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir uma linha sanitizada para `.gemini/settings.json` com dialect/status/digests/counts e nenhum dispatch MCP.
- Refactor: após GREEN, normalizar key paths preservando dialect, identifiers, counts zero e manifest digest; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-387.sh::ContractAUR387`.
- Integration: `tests/acceptance/AUR-387.sh::IntegrationAUR387`.
- E2E: `tests/acceptance/AUR-387.sh::E2EAUR387`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-387`

Expected artifact: o coordinator deriva `.board/evidence/AUR-387/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: substituir `.gemini/settings.json` por symlink que escape do checkout.
- Expected: o resolver sai 1 com `AUR-387/AC-001/source-escape` antes da leitura externa; restore recupera o source hash.

### MUT-002: AC-001 / mcp

- Change: executar o command sintético da fixture ou iniciar um MCP server durante parse.
- Expected: o process spy sai 1 com `AUR-387/AC-001/mcp-executed`; restore mantém `commands_executed=0`.

### MUT-003: AC-001 / secret-channel

- Change: persistir o valor do canário inline em vez de sua classe.
- Expected: o sink scan sai 1 com `AUR-387/AC-001/secret-value` sem imprimir o valor; restore mantém `secret_values=0`.

## Security and privacy

- Data flow: dialect `gemini-settings-json`, key paths, server/tool identifiers e classes de referência -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-387.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: nenhum setting local vira default; uma config de produto nova exige schema e autorização próprios.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
