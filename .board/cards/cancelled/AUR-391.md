---
id: AUR-391
version: 1
title: Caracterizar dockerignore da raiz
status: cancelled
validation: tested
profile_owner: AUR-404
office: O14-legacy
depends_on: [AUR-001, AUR-233, AUR-404]
requirements: [PR-ARCH-001, PR-SUP-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [.dockerignore, tests/characterization/legacy/root-ignores/AUR-391, tests/acceptance/AUR-391.sh, docs/specs/AUR-391.md]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, filesystem, supply-chain]
---

## Outcome

o manifesto registra `.dockerignore`, source/pattern/result digests e `semantic_probe=pass` para 32 paths sintéticos com include, exclude e negation.

## Non-goals

- Executar `docker build`, medir o tamanho real do build context ou alinhar `.dockerignore` com `.gitignore`; o probe compara somente os 32 vereditos do matcher offline.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-001, AUR-233]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/characterization/legacy/root-ignores/AUR-391/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- o manifesto registra `.dockerignore`, source/pattern/result digests e `semantic_probe=pass` para 32 paths sintéticos com include, exclude e negation.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Caracterizar dockerignore da raiz

- Given: a fixture `tests/characterization/legacy/root-ignores/AUR-391/cases.tsv` sela o digest de `.dockerignore`, os 32 paths sintéticos e o veredito de cada um sob matcher Docker-compatible, com teto de 4.096 patterns e 10 s.
- When: o probe executa somente pattern set e semântica observada por matcher Docker-compatible offline sem executar build, em fixture descartável, e compara os 32 resultados.
- Then: o nominal retorna `pass` e o manifesto registra `.dockerignore`, source/pattern/result digests e `semantic_probe=pass` para 32 paths sintéticos com include, exclude e negation; ausência, tamper/symlink, campo inválido e over-limit retornam respectivamente `ignore_semantics_incomplete`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded` antes de efeito externo.
- Covers requirements: [PR-ARCH-001, PR-SUP-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `ignore_semantics_incomplete|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-391.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-391/AC-001/ignore_semantics_incomplete`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir o manifesto semântico de `.dockerignore` com source/pattern/result digests e todos os probes aprovados.
- Refactor: após GREEN, reordenar a matriz preservando pattern/result digests, diferenças e clean-tree proof; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-391.sh::ContractAUR391`.
- Integration: `tests/acceptance/AUR-391.sh::IntegrationAUR391`.
- E2E: `tests/acceptance/AUR-391.sh::E2EAUR391`.

## Acceptance

container_profile: `go-git-offline-v1`

accept: `./.board/bin/oci-run --profile go-git-offline-v1 --card AUR-391`

Expected artifact: o coordinator deriva `.board/evidence/AUR-391/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: alterar `.dockerignore` depois do source lock.
- Expected: o aceite sai 1 com `AUR-391/AC-001/source-digest-mismatch`; restore recupera o hash.

### MUT-002: AC-001 / filesystem

- Change: inverter a expectativa de uma negation ou escrever fora do clone descartável.
- Expected: o containment/result gate sai 1 com `AUR-391/AC-001/semantic-mismatch`; restore passa os 32 casos.

### MUT-003: AC-001 / supply-chain

- Change: declarar um arquivo credential-shaped excluído quando o matcher o inclui.
- Expected: o exposure case sai 1 com `AUR-391/AC-001/sensitive-path-included`; restore marca a expectativa correta.

## Security and privacy

- Data flow: pattern set e semântica observada por matcher Docker-compatible offline sem executar build -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-391.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: mudança de pattern exige card posterior com os mesmos 32 probes e análise de build context.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
