---
id: AUR-302
version: 1
title: Caracterizar locks Go legados
status: cancelled
validation: tested
profile_owner: AUR-404
office: O14-legacy
depends_on: [AUR-001, AUR-233, AUR-404]
requirements: [PR-SUP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [go.mod, go.sum, tests/characterization/legacy/go-locks, tests/acceptance/AUR-302.sh, docs/specs/AUR-302.md, tests/unit/AUR-302.go, tests/contracts/AUR-302.go, tests/integration/AUR-302.go, tests/e2e/AUR-302.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/go-git-offline-v1.json, .board/locks/oci/go-git-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, supply-chain]
---

## Outcome

toolchain, módulos, checksums, licenças e imports atuais de go.mod/go.sum são registrados antes de qualquer update.

## Non-goals

- Atualizar dependências, rodar `go mod tidy` ou remover módulos não usados; o card congela o retrato do estado atual.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-001, AUR-233 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- toolchain, módulos, checksums, licenças e imports atuais de go.mod/go.sum são registrados antes de qualquer update.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Caracterizar locks Go legados

- Given: `go.mod` e `go.sum` estão na versão legada corrente e o profile `go-git-offline-v1` roda sem rede e sem proxy de módulos.
- When: a caracterização de `tests/characterization/legacy/go-locks` inventaria toolchain, módulos, checksums, licenças e imports a partir dos arquivos atuais.
- Then: o baseline gravado em `tests/characterization/legacy/go-locks` bate byte a byte com o inventário recomputado e qualquer divergência de checksum reprova AC-001.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-SUP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Caracterizar locks Go legados” em `go.mod`, `go.sum`, `tests/characterization/legacy/go-locks`, `tests/acceptance/AUR-302.sh`, `docs/specs/AUR-302.md`.

## TDD proof

- Test: `tests/acceptance/AUR-302.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-302/AC-001: Caracterizar locks Go legados ausente`; build/loader/infra não valem.
- Green: escrever em `tests/characterization/legacy/go-locks` o extrator que lê `go.mod` e `go.sum`, emite o inventário determinístico dos cinco eixos e o compara com o baseline versionado.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-302.go::TestAUR302`.
- Contract: `tests/contracts/AUR-302.go::ContractAUR302`.
- Integration: `tests/integration/AUR-302.go::IntegrationAUR302`.
- E2E: `tests/e2e/AUR-302.sh::E2EAUR302`.

## Acceptance

container_profile: `go-git-offline-v1`

accept: `./.board/bin/oci-run --profile go-git-offline-v1 --card AUR-302`

Expected artifact: `.board/evidence/AUR-302/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: executar go mod tidy e aceitar mudança não explicada de lock.
- Expected: o acceptance falha com `AUR-302/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / supply-chain

- Change: trocar digest pinado por tag mutável.
- Expected: o acceptance falha com `AUR-302/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `supply-chain` -> “Caracterizar locks Go legados” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-302.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
