---
id: AUR-240
version: 1
title: Entregar skill Go
status: cancelled
validation: tested
profile_owner: AUR-411
office: O04-agents
depends_on: [AUR-074, AUR-222, AUR-411]
requirements: [PR-SKL-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]
paths: [skills/builtin/languages/go, tests/acceptance/AUR-240.sh, docs/specs/AUR-240.md, tests/unit/AUR-240.go, tests/contracts/AUR-240.go, tests/integration/AUR-240.go, tests/e2e/AUR-240.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository, skill-runtime]
---

## Outcome

a skill Go mapeia entidades e regras Go para o Finding canônico sem capabilities mutáveis.

## Non-goals

- Executar `go build`, `go vet` ou baixar módulos do repositório analisado: a skill Go opera apenas sobre as entidades já extraídas pelo parser.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-074, AUR-222 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a skill Go mapeia entidades e regras Go para o Finding canônico sem capabilities mutáveis.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Entregar skill Go

- Given: os manifests aprovados de AUR-074, AUR-222 estão disponíveis na mesma base, fixture bounded e perfil OCI ativo.
- When: `skills/builtin/languages/go` analisa a fixture Go com erro retornado e ignorado, e o resultado é comparado ao golden Go do card.
- Then: o Finding canônico emitido nomeia a chamada Go cujo erro foi descartado, com rule ID, arquivo e span, e o manifest da skill não expõe nenhuma ferramenta capaz de escrever no checkout.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-SKL-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]

## Public contract

- Introduz somente “Entregar skill Go” nos artefatos `skills/builtin/languages/go`, `tests/acceptance/AUR-240.sh`, `docs/specs/AUR-240.md`.

## TDD proof

- Test: `tests/acceptance/AUR-240.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-240/AC-001: Entregar skill Go ausente`; build/loader/infra não valem.
- Green: escrever em `skills/builtin/languages/go` o mapeamento das entidades Go e da regra de erro ignorado para o Finding canônico, declarando somente ferramentas de leitura.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-240.go::TestAUR240`.
- Contract: `tests/contracts/AUR-240.go::ContractAUR240`.
- Integration: `tests/integration/AUR-240.go::IntegrationAUR240`.
- E2E: `tests/e2e/AUR-240.sh::E2EAUR240`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-240`

Expected artifact: `.board/evidence/AUR-240/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover a regra de erro ignorado do golden Go.
- Expected: o acceptance inalterado falha com `AUR-240/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / skill-runtime

- Change: incluir capability ou path não declarado.
- Expected: o acceptance inalterado falha com `AUR-240/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `skill-runtime` -> contrato “Entregar skill Go” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-240.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Cancellation

- 2026-08-12: cancelado na reorientação do board para entregas executáveis. O
  dono determinou que o AurumCode precisa disparar agentes e fazer code review
  automático, e que cada card deve terminar num comando executável. Este card
  não compõe esse caminho, ou especifica em greenfield um comportamento que o
  código existente já cumpre, ou depende de um profile OCI que o runner recusa
  carregar. O histórico permanece rastreável em Git; nada foi apagado.
- Substituído pela série AUR-500..AUR-515, em que cada card entrega um comando
  verificável rodando sob `bootstrap-readonly-v1`, o único profile carregável.
