---
id: AUR-015
version: 2
title: Versionar standard de secure code review
status: doing
office: O00-research
depends_on: []
requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [.board/research/secure-code-review.md, standards/security-review, tests/acceptance/AUR-015.sh, docs/specs/AUR-015.md, tests/specs/AUR-015, tests/integration/AUR-015_test.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: 5729803ebcb2c3ba89da461bed43647d2c26de62
spec_digest: sha256:40928326163df674de707190c2bc8196d6976a56de946db6c6d50db8f7394f07
risk: high
data_class: internal
trust_boundaries: [repository, network-source]
---

## Outcome

OWASP, CWE e NIST SSDF são mapeados a controles testáveis.

## Non-goals

- Este card não varre o código do AurumCode em busca das vulnerabilidades catalogadas; o mapeamento é normativo e rodar scanners pertence aos cards do escritório de segurança.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- o snapshot base e a policy bootstrap estão disponíveis.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.
- Honestidade (Lei 11) sobre a camada Integration: esta camada hoje NÃO é exercida pelo `accept:` selado deste card — a imagem pinada `bootstrap-readonly-v1` é Alpine 3.22 + BusyBox sem toolchain Go, e `.board/validate.sh` não chama `go build`/`go vet`/`go test` em nenhum ponto (`grep -n "go test\|go build\|go vet" .board/validate.sh` não retorna ocorrência). `git mv tests/integration/AUR-015_test.go` para fora da árvore e reexecutar o `accept:` selado sai `0` idêntico ao nominal: o portão selado não detecta a ausência deste arquivo. A cobertura real desta camada depende do card de governança em voo GOV-second-reader, que fará um segundo leitor do board executar `go test` fora do sandbox, contra o repositório read-only, em imagem digest-pinada. Até esse gate existir e rodar, este card não deve ser tratado como pronto para `done`: a prova de Integration é real (o teste passa e falha corretamente sob mutação), mas não está hoje amarrada a nenhum portão automático deste board.

## Postconditions

- OWASP, CWE e NIST SSDF são mapeados a controles testáveis.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Versionar standard de secure code review

- Given: `.board/research/secure-code-review.md` existe e não está vazio, e contém, case-insensitive, os termos `OWASP`, `threat`, `trust boundary` e `fail closed`; a ausência de qualquer um desses quatro termos é o caso `invalid` deste card.
- When: AC-001 executa `tests/acceptance/AUR-015.sh` contra o arquivo `.board/research/secure-code-review.md` produzido em `paths` no perfil OCI pinado `bootstrap-readonly-v1` e captura `exit_code` e a linha de diagnóstico em stderr.
- Then: o vetor `nominal` sai `0` e o artifact prova que OWASP, CWE e NIST SSDF são mapeados a controles testáveis.
- And: a ausência de `OWASP`, `threat`, `trust boundary` ou `fail closed` no arquivo faz `tests/acceptance/AUR-015.sh` sair diferente de zero antes de qualquer efeito fora de `paths`, imprimindo `AUR-015/AC-001: missing <termo>`; replay do mesmo `base_sha` reproduz exit code e stdout idênticos.

- Covers requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Versionar standard de secure code review” e os artefatos em `.board/research/secure-code-review.md`, `standards/security-review`, `tests/acceptance/AUR-015.sh`, `docs/specs/AUR-015.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-015.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-015/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Preencher `standards/security-review` ligando cada regra a OWASP, CWE e prática NIST SSDF com versão e data da fonte, e a um controle testável com um fixture vulnerável e um fixture corrigido.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“deixar uma regra crítica sem CWE ou controle equivalente.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: `tests/integration/AUR-015_test.go::TestAUR015Boundary`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-015`

Expected artifact: `.board/evidence/AUR-015/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Versionar standard de secure code review” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: deixar uma regra crítica sem CWE ou controle equivalente.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-015/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / network-source

- Change: substituir a referência pesquisada por uma URL não allowlisted ou conteúdo sem versão.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-015/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `network-source` -> contrato “Versionar standard de secure code review” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-015.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o mesmo `accept:` extrai o bloco `` ```console `` da seção "## Example" e falha com `AUR-015/AC-001/doc-drift` se a linha de saída documentada não bater byte a byte com a saída real desta execução (`record_count` incluso), e falha com `AUR-015/AC-001/behavior-missing` se o arquivo estiver ausente ou vazio — removê-lo ou deixá-lo divergir derruba o `accept:` com código tipado.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-015/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige o bundle de evidência recomputável ligado à identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

