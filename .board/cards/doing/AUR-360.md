---
id: AUR-360
version: 1
title: Fixar Go toolchain e módulos
status: doing
office: O00-security
depends_on: [AUR-359]
requirements: [PR-SUP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [.board/bootstrap/locks/go.yml, tests/bootstrap/locks/AUR-360, tests/acceptance/AUR-360.sh, docs/specs/AUR-360.md]
read_paths: [go.mod, go.sum, Makefile, Dockerfile]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: 7a1b93024842aa889e3e9820c1b4bcb5c55a9dc3
spec_digest: sha256:d722d85eeb43fe6ce6127d05f0e2b6127b5e80693f172ecaeb35f2552c80bcfa
risk: critical
data_class: internal
trust_boundaries: [repository, supply-chain]
---

## Outcome

o lock contém `go_version=1.21`, module `github.com/Mpaape/AurumCode`, hashes de `go.mod`/`go.sum` e cada módulo como versão imutável mais checksum.

## Non-goals

- Atualizar, resolver ou baixar dependências Go: o card sela `go.mod`/`go.sum` como estão e nunca roda `go mod tidy` nem promove versão de módulo.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-359]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/bootstrap/locks/AUR-360/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- o lock contém `go_version=1.21`, module `github.com/Mpaape/AurumCode`, hashes de `go.mod`/`go.sum` e cada módulo como versão imutável mais checksum.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Fixar Go toolchain e módulos

- Given: `tests/bootstrap/locks/AUR-360/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 256 entradas, 4 MiB e 15 s.
- When: `VerifyBootstrapLockV1` lê `.board/bootstrap/locks/go.yml` e confronta `go_version`, o module path e cada par versão+checksum com os hashes observados de `go.mod`/`go.sum`, sem baixar módulo algum.
- Then: o nominal sai 0 com `pass` e cada módulo de `go.sum` casa com exatamente um par versão+checksum do lock; `go_version` fora de 1.21, module path alterado, symlink/tamper no source, campo inválido e over-limit saem 1 com `go_lock_mismatch`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded`, antes de efeito externo.
- Covers requirements: [PR-SUP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `go_lock_mismatch|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-360.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-360/AC-001/go_lock_mismatch`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir o lock .board/bootstrap/locks/go.yml e validar o lock contém `go_version=1.21`, module `github.com/Mpaape/AurumCode`, hashes de `go.mod`/`go.sum` e cada módulo como versão imutável mais checksum antes de qualquer consumo do domínio.
- Refactor: após GREEN, reordenar entradas canonicamente e preservar identity set, error codes e manifest digest no replay; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-360.sh::ContractAUR360`.
- Integration: `tests/acceptance/AUR-360.sh::IntegrationAUR360`.
- E2E: `tests/acceptance/AUR-360.sh::E2EAUR360`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-360`

Expected artifact: o coordinator deriva `.board/evidence/AUR-360/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: alterar somente o module path no snapshot depois do source hash lock.
- Expected: o aceite sai 1 com `AUR-360/AC-001/module-path-mismatch`; restore volta ao manifesto original.

### MUT-002: AC-001 / supply-chain

- Change: remover o checksum de `github.com/gorilla/mux` mantendo sua versão.
- Expected: o aceite sai 1 com `AUR-360/AC-001/module-checksum-missing`; restore valida todos os módulos.

## Security and privacy

- Data flow: Go 1.21, module path e checksums de todas as dependências -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-360.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: consumidores migram somente depois que o agregador AUR-233 aceita todos os lock manifests.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer A: cada hunk e as dez dimensões do protocolo, priorizando correção, SOLID, compatibilidade, determinismo e sensibilidade dos testes.
- Reviewer B: cada hunk e as dez dimensões do protocolo, priorizando entradas hostis, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 selada, mas usam processos, sessões, conversas, caches, memória e relatórios separados; registrar backend real e nível I0/I1/I2/I3.
- Skeptical approver: pré-sela hipóteses, executa todas as mutações em OCI limpo, restaura e repete o aceite; falha, divergência ou inconclusivo veta.

## Evidence

Somente o coordinator grava artefatos sanitizados em `.board/evidence/AUR-360/`: spec/identity seladas, red-green-refactor, clean tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay restaurado, hashes e verdicts. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

