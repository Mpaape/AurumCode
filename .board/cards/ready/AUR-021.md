---
id: AUR-021
version: 2
title: Fixar MCP conformance e segurança
status: ready
office: O00-research
depends_on: []
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [.board/research/mcp.md, standards/mcp, tests/acceptance/AUR-021.sh, docs/specs/AUR-021.md, tests/specs/AUR-021, tests/integration/AUR-021_test.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, network-source]
---

## Outcome

versão MCP, SDK Go e separação read-only versus mutável ficam testáveis.

## Non-goals

- Este card não expõe servidor MCP nem registra tool executável; versiona somente a classificação e os limites de conformidade que o servidor futuro terá de satisfazer.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- o snapshot base e a policy bootstrap estão disponíveis.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- versão MCP, SDK Go e separação read-only versus mutável ficam testáveis.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Fixar MCP conformance e segurança

- Given: `.board/research/mcp.md` existe e não está vazio e contém, case-insensitive, os cinco termos `stdio`, `read-only`, `OAuth`, `injection` e `capability`; a ausência de qualquer um deles é o caso `invalid`.
- When: AC-001 executa `tests/acceptance/AUR-021.sh` contra `.board/research/mcp.md` no perfil OCI pinado `bootstrap-readonly-v1` e captura `exit_code`.
- Then: o vetor `nominal` sai `0` e o artifact prova que versão MCP, SDK Go e separação read-only versus mutável ficam testáveis.
- And: a ausência de qualquer um dos cinco termos faz `tests/acceptance/AUR-021.sh` sair diferente de zero antes de qualquer efeito fora de `paths`; replay do mesmo `base_sha` reproduz exit code idêntico.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Fixar MCP conformance e segurança” e os artefatos em `.board/research/mcp.md`, `standards/mcp`, `tests/acceptance/AUR-021.sh`, `docs/specs/AUR-021.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-021.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-021/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Fixar em `standards/mcp` a versão do protocolo e do SDK Go adotados e classificar cada tool como somente leitura ou mutável, de modo que `review.publish` só seja aceita na classe mutável com consentimento explícito.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“expor review.publish como read-only.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: `tests/integration/AUR-021_test.go::TestAUR021Boundary`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-021`

Expected artifact: `.board/evidence/AUR-021/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Fixar MCP conformance e segurança” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: expor review.publish como read-only.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-021/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / network-source

- Change: substituir a referência pesquisada por uma URL não allowlisted ou conteúdo sem versão.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-021/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `network-source` -> contrato “Fixar MCP conformance e segurança” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-021.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-021/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige o bundle de evidência recomputável ligado à identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

