---
id: AUR-018
version: 1
title: Fixar contratos SCM e CI seguros
status: ready
office: O00-research
depends_on: []
requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [.board/research/scm-ci.md, standards/scm, tests/acceptance/AUR-018.sh, docs/specs/AUR-018.md, tests/specs/AUR-018, tests/integration/AUR-018.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, network-source]
---

## Outcome

GitHub, Gitea e GitLab possuem capability e trust-boundary matrix.

## Non-goals

- Este card não implementa cliente de GitHub, Gitea ou GitLab nem configura pipeline de CI; fixa apenas a matriz de capabilities e fronteiras que os adapters deverão respeitar.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- o snapshot base e a policy bootstrap estão disponíveis.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- GitHub, Gitea e GitLab possuem capability e trust-boundary matrix.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Fixar contratos SCM e CI seguros

- Given: `tests/specs/AUR-018/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Fixar contratos SCM e CI seguros”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Fixar contratos SCM e CI seguros” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que GitHub, Gitea e GitLab possuem capability e trust-boundary matrix.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Fixar contratos SCM e CI seguros” e os artefatos em `.board/research/scm-ci.md`, `standards/scm`, `tests/acceptance/AUR-018.sh`, `docs/specs/AUR-018.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-018.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-018/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Montar em `standards/scm` a matriz por forge — GitHub, Gitea e GitLab — com escopo de token, evento de CI, fronteira de confiança e a regra de que head hostil só é analisado com credencial somente leitura.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“permitir análise de head hostil com credencial de escrita.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: `tests/integration/AUR-018.go::IntegrationAUR018`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-018`

Expected artifact: `.board/evidence/AUR-018/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Fixar contratos SCM e CI seguros” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: permitir análise de head hostil com credencial de escrita.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-018/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / network-source

- Change: substituir a referência pesquisada por uma URL não allowlisted ou conteúdo sem versão.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-018/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `network-source` -> contrato “Fixar contratos SCM e CI seguros” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-018.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-018/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige ainda evento humano autenticado para a identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

