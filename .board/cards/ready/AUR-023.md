---
id: AUR-023
version: 1
title: Bloquear imports que violam arquitetura
status: ready
validation: tested
profile_owner: AUR-403
office: O01-core
depends_on: [AUR-003, AUR-005, AUR-014, AUR-015, AUR-016, AUR-017, AUR-233, AUR-403]
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [internal/architecture, tests/architecture, tests/acceptance/AUR-023.sh, docs/specs/AUR-023.md, tests/specs/AUR-023, tests/contracts/AUR-023.go, tests/integration/AUR-023.go, tests/unit/AUR-023.go]
read_paths: [go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

domain e application não importam SDK, I/O ou adapters.

## Non-goals

- Este card não move nem refatora código que hoje viole a regra de camadas; o linter apenas reporta a violação, e corrigir cada pacote infrator é trabalho de cards próprios.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-003, AUR-005, AUR-014, AUR-015, AUR-016, AUR-017, AUR-233 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- domain e application não importam SDK, I/O ou adapters.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Bloquear imports que violam arquitetura

- Given: `tests/specs/AUR-023/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Bloquear imports que violam arquitetura”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Bloquear imports que violam arquitetura” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que domain e application não importam SDK, I/O ou adapters.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Bloquear imports que violam arquitetura” e os artefatos em `internal/architecture`, `tests/architecture`, `tests/acceptance/AUR-023.sh`, `docs/specs/AUR-023.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-023.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-023/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar em `internal/architecture` o linter que percorre o import graph dos pacotes Go e falha quando domain ou application referenciam SDK, pacote de I/O ou adapter, apontando o par pacote/import infrator.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“adicionar import de net/http no domain fixture.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-023.go::TestAUR023`.
- Contract: `tests/contracts/AUR-023.go::ContractAUR023`.
- Integration: `tests/integration/AUR-023.go::IntegrationAUR023`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-023`

Expected artifact: `.board/evidence/AUR-023/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Bloquear imports que violam arquitetura” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: adicionar import de net/http no domain fixture.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-023/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Bloquear imports que violam arquitetura” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-023.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
