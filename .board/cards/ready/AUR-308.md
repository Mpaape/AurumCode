---
id: AUR-308
version: 1
title: Caracterizar internal documentation legado
status: ready
validation: tested
office: O14-legacy
depends_on: [AUR-001, AUR-006]
requirements: [PR-DOC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [tests/characterization/legacy/documentation, tests/acceptance/AUR-308.sh, docs/specs/AUR-308.md, tests/unit/AUR-308.go, tests/contracts/AUR-308.go, tests/integration/AUR-308.go, tests/e2e/AUR-308.sh]
read_paths: [internal/documentation, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, filesystem]
---

## Outcome

cada extractor, normalizer, incremental e site package existente é marcado keep, migrate, replace ou delete por teste real.

## Non-goals

- Não remover, reescrever nem migrar qualquer pacote de `internal/documentation`; este card apenas classifica e a execução de cada migração fica nos cards de destino.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-001 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- cada extractor, normalizer, incremental e site package existente é marcado keep, migrate, replace ou delete por teste real.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Caracterizar internal documentation legado

- Given: o inventário selado por AUR-001 lista cada extractor, normalizer, incremental e site package de `internal/documentation`, com fixtures bounded em `tests/characterization/legacy/documentation`.
- When: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-308` executa a suíte de caracterização sobre os pacotes de `internal/documentation` sem iniciar o pipeline de docs.
- Then: `.board/evidence/AUR-308/acceptance/AC-001.json` lista cada pacote de `internal/documentation` com veredito keep, migrate, replace ou delete e o teste que o sustenta; pacote sem teste vinculado sai 1 e bloqueia o manifesto.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-DOC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Caracterizar internal documentation legado” em `internal/documentation`, `tests/characterization/legacy/documentation`, `tests/acceptance/AUR-308.sh`, `docs/specs/AUR-308.md`.

## TDD proof

- Test: `tests/acceptance/AUR-308.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-308/AC-001: Caracterizar internal documentation legado ausente`; build/loader/infra não valem.
- Green: escrever os testes de caracterização por pacote em `tests/characterization/legacy/documentation` e o emissor que grava o veredito keep/migrate/replace/delete apenas quando há teste executado.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-308.go::TestAUR308`.
- Contract: `tests/contracts/AUR-308.go::ContractAUR308`.
- Integration: `tests/integration/AUR-308.go::IntegrationAUR308`.
- E2E: `tests/e2e/AUR-308.sh::E2EAUR308`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-308`

Expected artifact: `.board/evidence/AUR-308/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: classificar pacote sem teste como reutilizável.
- Expected: o acceptance falha com `AUR-308/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / filesystem

- Change: apontar output para symlink fora do staging.
- Expected: o acceptance falha com `AUR-308/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `filesystem` -> “Caracterizar internal documentation legado” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-308.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer A: every-hunk review nas dez dimensões, priorizando correção, SOLID, compatibilidade, documentação e sensibilidade dos testes.
- Reviewer B: every-hunk review nas dez dimensões, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 idêntica e selada; processos, sessões, conversations, caches e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas as MUT no profile `bootstrap-readonly-v1`, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-308/` sanitizado: spec/identity, red-green-refactor, gates, clean tree, coverage maps A/B, challenge, mutations, replay, hashes e verdicts. `done` exige o bundle de evidência recomputável; nunca segredo, env, prompt/resposta brutos ou chain-of-thought.
