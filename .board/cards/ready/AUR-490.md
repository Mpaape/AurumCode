---
id: AUR-490
version: 2
title: review --base roda os mesmos passes que --pr
status: ready
validation: tested
profile_owner: AUR-006
office: O03-review
depends_on: [AUR-006, AUR-489]
requirements: [PR-ARCH-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [cmd/aurumcode, docs/getting-started.md, tests/acceptance/AUR-490.sh]
read_paths: [internal/analyzer, internal/config, internal/review, internal/review/cache, internal/review/rules, internal/prompt, internal/render, internal/context, internal/analysis, internal/memory, internal/apply, internal/testgen, internal/git/githubclient, internal/llm, internal/llm/cost, internal/llm/httpbase, internal/llm/provider/litellm, internal/security/redaction, pkg/types, tests/fixtures/review, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
container_profile: `bootstrap-readonly-v1`
risk: medium
data_class: internal
trust_boundaries: [repository, llm-provider]
---

## Outcome

O unico caminho documentado para uso local -- `aurumcode review --base` -- entrega
o que a doc promete "sem configuracao": analise estatica deterministica, contexto
de codebase, resumo e diagrama, memoria por repositorio. Hoje tudo isso so existe
em `runPRReview` (`cmd/aurumcode/pr.go:219,229,387,399,489,492`); `main.go:156-165`
nao importa nenhum dos pacotes, e `review.memory: local` fora de `--pr` e no-op
silencioso.

## Acceptance

- AC-001 Analise deterministica local. Em fixture com `dbPassword := "hunter2-super-secret"`
  adicionado, `review --base HEAD~1 --fail-on error` SEM provedor configurado
  reporta o achado `analysis/hardcoded-secret` com arquivo e linha e sai 3.
  Selector `TestAUR490LocalAnalysis`.
- AC-002 Resumo e diagrama locais. A saida do review (stdout, formato padrao)
  contem a secao de resumo e um bloco ```mermaid, gerados pelos mesmos
  `render.Summary`/`render.Mermaid` do caminho do PR. Selector `TestAUR490LocalRender`.
- AC-003 Memoria local honrada. Com `review.memory: local` no `.aurumcode/config.yml`,
  o review local grava as notas no diretorio por repositorio definido no AUR-489,
  e um segundo review as le. Selector `TestAUR490LocalMemory`.
- AC-004 Um so lugar. As chamadas aos passes vivem numa funcao compartilhada
  pelos dois caminhos (`--pr` e `--base`); o teste `TestAUR490SharedPasses`
  prova por reflexao ou por fixture que a lista de passes dos dois caminhos e a
  mesma. `docs/getting-started.md` "Uso local" mostra a saida com o resumo.

## Non-goals

Publicar no PR a partir de `--base`. Novos passes.

## Validation

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-490`

`./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-490` roda
`tests/acceptance/AUR-490.sh AC-001..AC-004`.

## Skeptical mutations

- MUT-001: remover a chamada de `analysis.Analyze` do caminho `--base` -> AC-001 cai.
- MUT-002: remover o render do caminho `--base` -> AC-002 cai.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
