---
id: AUR-499
version: 1
title: O review publica a versao sugerida e a entrada de changelog no PR e no CI
status: ready
validation: tested
profile_owner: AUR-006
office: O03-review
depends_on: [AUR-006, AUR-452, AUR-489, AUR-490, AUR-498]
requirements: [PR-REL-001, PR-REL-002]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [cmd/aurumcode, internal/config, internal/git/githubclient, internal/render, internal/analyzer, action.yml, scripts/action-entrypoint.sh, .github/workflows/review.yml, tests/acceptance/AUR-499.sh, docs/specs/AUR-499.md]
read_paths: [internal/changelog, internal/review, internal/prompt, internal/llm, internal/security/redaction, pkg/types, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
container_profile: `bootstrap-readonly-v1`
risk: medium
data_class: internal
trust_boundaries: [repository, llm-provider, github-actions]
---

## Outcome

O review, ao revisar um PR, usa o motor do AUR-498 sobre as mensagens de commit
do proprio PR e mostra a proxima versao sugerida e a entrada de changelog no
relatorio do review e, quando publicado, no corpo do review do PR; a Action
tambem expoe a versao sugerida como output do CI para o workflow do consumidor.
Nada e tagueado, escrito em `CHANGELOG.md` ou publicado sem uma acao explicita
do consumidor.

## Acceptance

- AC-001 Fonte de commits. O caminho `--pr` le titulo/corpo/resumo do PR e a
  lista de mensagens de commit do PR; o caminho `--base` le os commits do
  intervalo local. Um caminho ausente de metadados nao derruba o review: a
  secao e omitida com um limite declarado.
- AC-002 Superficie. O relatorio do review inclui a versao sugerida e a entrada
  de changelog gerados pelo motor do AUR-498; o corpo publicado no PR inclui a
  mesma secao.
- AC-003 Output de CI. A Action declara um output com a versao sugerida e o
  caminho/entrada do changelog, preenchido pelo entrypoint sem `::set-output`
  deprecado, disponivel ao workflow do consumidor.
- AC-004 Fronteira. Texto de commit/PR e tratado como dado nao confiavel:
  redigido no sink como o historico do PR, nunca executado, e nunca autoriza
  tag/release/publicacao por si so. Publicacao continua atras do gate de
  autorizacao existente.

## Non-goals

Criar tag ou release, escrever `CHANGELOG.md` no repositorio do consumidor,
decidir bump a partir de tag publicada (isso e AUR-472/AUR-478), e qualquer novo
provedor.

## Validation

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-499`

## Skeptical mutations

- MUT-001: omitir a secao no corpo publicado -> AC-002 cai.
- MUT-002: nao declarar o output da Action -> AC-003 cai.
- MUT-003: ecoar o texto do commit sem redacao no sink -> AC-004 cai.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
