---
id: AUR-432
version: 1
title: Revisar codigo privado sem vazar segredo
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-430, AUR-431, AUR-434]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [internal/review, cmd/aurumcode, tests/fixtures/review/secret, tests/acceptance/AUR-432.sh, tests/unit/AUR-432.go, tests/integration/AUR-432.go, tests/e2e/AUR-432.sh, docs/specs/AUR-432.md]
read_paths: [internal/config, internal/git/githubclient, tests/fixtures/scm/github, cmd/regenerate-docs, internal/documentation/extractors, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/site, internal/documentation/welcome, internal/pipeline, tests/fixtures/docs/goproject, action.yml, internal/security/redaction, internal/analyzer, internal/prompt, internal/llm, pkg/types, tests/fixtures/repos/git-demo, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Um segredo presente no codigo revisado nao chega ao modelo nem aparece na saida, no cache ou no relatorio.

## Gap medido (2026-08-12)

A fiacao de redacao NAO existe no caminho de review; o builder parou e
reportou em vez de contornar. Evidencia: zero importadores nao-teste de
internal/security/redaction; internal/review/reviewer.go:89-92 envia
System+User crus ao orchestrator; cmd/aurumcode/main.go:93-94 escreve
achados direto em os.Stdout sem redaction.NewWriter; o RedactSecret de
internal/llm/httpbase so trunca corpos de resposta HTTP em erro e nao cobre
o prompt. Este card agora e dono da fiacao (internal/review e cmd/aurumcode
entraram em paths) e depende de AUR-431 e AUR-434 para nao disputar posse:
redigir o diff antes de montar o prompt e envolver a saida com o writer de
redacao sao o nucleo do Outcome, nao detalhe. Achado herdado do revisor do
AUR-436 (medio, mapeado ao Outcome deste card): cmd/aurumcode/main.go:353
ecoa LLM_BASE_URL com userinfo verbatim no stderr do caminho de sucesso —
http://user:SENHA@host sai inteiro; conserto na origem com url.Redacted().

## Reparo de contrato (candidato 0d91078)

O aceite compila cmd/aurumcode contra o motor; o accept selado saiu 79
(missing_input) e o builder provou por simulacao que exatamente estes cinco
acrescimos de read-set levam o aceite a exit 0. Registrado tambem o efeito
colateral da fiacao: os accepts congelados de AUR-430/431/434/436 nao copiam
internal/security/redaction no staging fechado e passam a build_failed; a
manutencao de uma linha por script sera aplicada pelo coordenador junto da
integracao, com os quatro accepts reexecutados como prova.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Um segredo presente no codigo revisado nao chega ao modelo nem aparece na saida, no cache ou no relatorio.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Revisar codigo privado sem vazar segredo

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --base HEAD~1` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --base HEAD~1` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-432.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-432/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-432.go::TestAUR432`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-432.go::IntegrationAUR432`.
- E2E: `tests/e2e/AUR-432.sh::E2EAUR432`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-432`

Expected artifact: `.board/evidence/AUR-432/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: desligar a redacao no caminho do envio faz o segredo aparecer e o aceite falhar.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-432/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-432.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-432/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.

## Delivery record

- commit: 6de8e0dcd76995b284a9b4c13c84a2eba9167a1e
- review: approved
- validation: passed
