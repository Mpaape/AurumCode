---
id: AUR-435
version: 1
title: Achar falha de seguranca, nao so estilo
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-434, AUR-015, AUR-432]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [internal/review, cmd/aurumcode, tests/fixtures/review/vuln, tests/acceptance/AUR-435.sh, tests/unit/AUR-435.go, tests/integration/AUR-435.go, tests/e2e/AUR-435.sh, docs/specs/AUR-435.md]
read_paths: [standards/security-review, internal/security/redaction, internal/analyzer, internal/prompt, internal/llm, pkg/types, tests/fixtures/repos/git-demo, tests/fixtures/review/known-problem-response.json, tests/fixtures/review/degraded-freeform-response.txt, tests/fixtures/review/secret, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Uma passagem de seguranca aponta vulnerabilidade no codigo revisado usando o padrao de seguranca do projeto, separada dos achados de qualidade.

## Restauracao

Este comportamento ja existiu no repositorio e foi removido em 2025-11-03 pelos
commits `24dae01`, `09d6033` e `698a4ae`. Recuperar em vez de reescrever.

- Origem: commit `c12d7ab` (anterior a exclusao).
- Arquivos: internal/review/rules.go, .aurumcode/rules/security.yml
- Estado medido: A penalidade de seguranca ja existe no scorer (-10 por achado). Falta o matcher: as regras sao metadados sem padrao.
- Regra: restaurar o que passa nos testes, corrigir o defeito nomeado acima, e
  nao reescrever do zero o que ja existe. O que nao compilar e nao servir ao
  Outcome deste card fica de fora.

## Sequenciamento de posse (2026-08-13)

O flag --seguranca vive em cmd/aurumcode, que entrou em paths. AUR-432 (fiacao
de redacao) tambem e dono de internal/review e cmd/aurumcode e fecha um buraco
de seguranca vivo; ele vem primeiro, este card depois, AUR-441 (cache) por
ultimo na mesma cadeia de posse. Read-set espelha o fecho provado pelos cards
irmãos do motor.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Uma passagem de seguranca aponta vulnerabilidade no codigo revisado usando o padrao de seguranca do projeto, separada dos achados de qualidade.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Achar falha de seguranca, nao so estilo

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --base HEAD~1 --seguranca` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --base HEAD~1 --seguranca` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-435.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-435/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-435.go::TestAUR435`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-435.go::IntegrationAUR435`.
- E2E: `tests/e2e/AUR-435.sh::E2EAUR435`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-435`

Expected artifact: `.board/evidence/AUR-435/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: reusar a rubrica de qualidade na passagem de seguranca faz o aceite falhar por ausencia do achado esperado.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-435/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-435.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-435/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.

## Delivery record

- commit: fd99d3a44d47598fe22705e95c2343bbcbfb4e2d
- review: approved
- validation: passed
