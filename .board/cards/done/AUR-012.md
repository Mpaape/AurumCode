---
id: AUR-012
version: 1
title: Bootstrapar repositório demo consumidor
status: done
validation: tested
profile_owner: AUR-006
office: O09-demo
depends_on: [AUR-006, AUR-011]
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [demo-repo/scaffold, tests/e2e/consumer/bootstrap, tests/acceptance/AUR-012.sh, docs/specs/AUR-012.md, tests/specs/AUR-012]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

um repo vazio recebe todos os cenários sem importar dados privados do AurumCode.

## Non-goals

- Este card não executa os cenários de review dentro do repo demo consumidor: prova só o bootstrap do scaffold, e rodar os casos ponta a ponta pertence a cards de E2E.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-011 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- um repo vazio recebe todos os cenários sem importar dados privados do AurumCode.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Bootstrapar repositório demo consumidor

- Given: `tests/specs/AUR-012/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Bootstrapar repositório demo consumidor”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Bootstrapar repositório demo consumidor” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que um repo vazio recebe todos os cenários sem importar dados privados do AurumCode.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Bootstrapar repositório demo consumidor” e os artefatos em `demo-repo/scaffold`, `tests/e2e/consumer/bootstrap`, `tests/acceptance/AUR-012.sh`, `docs/specs/AUR-012.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-012.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-012/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Escrever o scaffold em `demo-repo/scaffold` que copia para um checkout vazio somente os arquivos da allowlist, e o bootstrap em `tests/e2e/consumer/bootstrap` que aborta ao encontrar qualquer path fora dela.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“copiar um arquivo fora da allowlist para o consumer.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: not-applicable: o comportamento é local e coberto pelo acceptance.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-012`

Expected artifact: `.board/evidence/AUR-012/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Bootstrapar repositório demo consumidor” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: copiar um arquivo fora da allowlist para o consumer.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-012/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Bootstrapar repositório demo consumidor” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-012.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Delivery record

- commit: f5a03efaf8151d5efef8288391567682a4b80e5f
- review: approved
- validation: passed
