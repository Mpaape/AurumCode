---
id: AUR-014
version: 1
title: Versionar standard de code review
status: done
validation: tested
office: O00-research
depends_on: [AUR-233]
requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [.board/research/code-review-standards.md, standards/code-review, tests/acceptance/AUR-014.sh, docs/specs/AUR-014.md, tests/specs/AUR-014]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository, network-source]
---

## Outcome

cada regra de review tem fonte oficial, versão e exemplos positivo e negativo.

## Non-goals

- Este card não aplica as regras `CR-*` a diff algum nem constrói o analisador que as executa; entrega apenas o catálogo versionado com exemplos positivo e negativo por regra.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- o snapshot base e a policy bootstrap estão disponíveis.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- cada regra de review tem fonte oficial, versão e exemplos positivo e negativo.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Versionar standard de code review

- Given: o standard contém o conjunto canônico de `CR-*`; `standards/code-review/cases.yaml` fornece para cada ID uma fonte primária/version/date, um fixture positivo e um negativo com verdict esperado.
- When: o linter resolve cada CR do texto e do frontmatter dos cards para exatamente uma linha do catálogo e executa somente os fixtures locais bounded, sem buscar rede.
- Then: cada CR possui fonte/version válidas e ambos os exemplos distinguem pass/fail pelo rule ID correto; CR ausente/duplicado, URL fora da allowlist, standard retirado ou fixture sem assert falha por código próprio.
- And: nenhum card pode citar CR inexistente e nenhum CR pode ficar sem card/AC/mutação; replay ordena por CR ID e preserva digest.
- Covers requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Versionar standard de code review” e os artefatos em `.board/research/code-review-standards.md`, `standards/code-review`, `tests/acceptance/AUR-014.sh`, `docs/specs/AUR-014.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-014.sh::AC-001`.
- Red: a pesquisa existente não contém ainda o catálogo executável positivo/negativo por CR; o linter alcançável falha com `AUR-014/AC-001/missing-rule-case`, nunca por rede, compile, loader, permissão ou engine.
- Green: criar o catálogo local e o crosswalk exato `CR -> fonte/version -> positive/negative -> card/AC/mutation`, sem alegar conformidade externa.
- Refactor: remover fonte, trocar current por withdrawn e inverter cada negative verdict; o mesmo acceptance detecta cada mutação e restore reproduz digest.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: not-applicable: o comportamento é local e coberto pelo acceptance.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-014`

Expected artifact: `.board/evidence/AUR-014/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Versionar standard de code review” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover a fonte de uma regra bloqueante.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-014/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / network-source

- Change: substituir a referência pesquisada por uma URL não allowlisted ou conteúdo sem versão.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-014/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `network-source` -> contrato “Versionar standard de code review” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-014.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-014/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige o bundle de evidência recomputável ligado à identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

## Delivery record

- commit: 37d800ffbf6d93b4b6d381ea6c6ab42802743833
- review: approved
- validation: passed

## Correcao de registro do coordenador (2026-08-14)

O `commit` registrado era `85814dc396764f84a385a581aff92133dee98806`, e esse
objeto NAO EXISTE neste repositorio: nem solto, nem no `origin`, nem no reflog.
`pipeline.sh` passou a recusar o board por isso.

O commit que de fato entregou este card e `37d800ffbf6d93b4b6d381ea6c6ab42802743833` --
assunto "AUR-014: version code review standard", assinado pelo usuario, e e ele
que cria os `paths` declarados aqui (`docs/specs/AUR-014.md`,
`standards/code-review/`), verificado com `git show --stat`.

Nao houve reescrita de historia hoje. A hipotese mais provavel e que o SHA
antigo era de um candidato em branch de trabalho, registrado antes da
integracao, e o objeto se perdeu quando aquela branch foi removida -- o registro
apontava para o candidato, nao para o commit integrado. Corrigir para o commit
que existe e que entrega os paths e registrar fato; deixar apontando para um
objeto ausente e afirmar entrega sem lastro verificavel.
