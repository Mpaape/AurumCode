---
id: AUR-455
version: 1
title: Um desenvolvedor novo roda as tres features em um minuto
status: done
validation: tested
profile_owner: AUR-006
office: O11-paginas
depends_on: [AUR-006, AUR-445, AUR-451]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [README.md, docs/getting-started.md, demo.sh, docs/specs/AUR-455.md, tests/acceptance/AUR-455.sh, tests/unit/AUR-455.go, tests/integration/AUR-455.go, tests/e2e/AUR-455.sh]
read_paths: [internal/config, cmd/aurumcode, cmd/regenerate-docs, Makefile, ACTION_USAGE.md, .github/workflows/examples/code-review.yml, .github/workflows/examples/documentation.yml, tests/fixtures/repos/git-demo, tests/fixtures/review, tests/fixtures/docs/goproject, internal/analyzer, internal/prompt, internal/review, internal/llm, internal/security/redaction, internal/git/githubclient, pkg/types, internal/documentation/extractors, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/site, internal/documentation/welcome, internal/pipeline, action.yml, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Quem chega ao repositorio consegue, sem ler codigo-fonte, compilar, rodar o primeiro code review, rodar a primeira documentacao e saber como plugar no proprio pull request. Um `demo.sh` mostra as tres features funcionando offline, sem chave de LLM.



## Achados medidos (auditoria de onboarding 2026-08-14)

- README.md tem ZERO ocorrencias da palavra "review": a feature principal do
  produto e invisivel para quem chega. Nem ACTION_USAGE.md nem SETUP_GUIDE.md
  a mencionam.
- README "## Development" e `make build`/`make test` mandam rodar
  `go build ./...` e `go test ./...`, que FALHAM (exit 1) por colisao de
  pacote em tests/unit e tests/integration. O primeiro comando que um usuario
  roda nao funciona, e nenhum doc avisa. O caminho que funciona hoje e
  `go build ./cmd/... ./internal/... ./pkg/...`.
- O unico exemplo executavel de review vive em docs/specs/AUR-430.md (spec de
  card, nao doc de usuario) e esta QUEBRADO como escrito: o caminho relativo
  do fixture tem 2 niveis em vez de 3.
- docs/getting-started.md:51 manda compilar o gerador de docs com o nome
  `aurumcode`, colidindo com o CLI de review de verdade.
- .github/workflows/examples/ nunca e linkado de lugar nenhum: e invisivel.
- `aurumcode review --base HEAD~1 --seguranca` funciona sem configuracao
  nenhuma e sem chave — o melhor primeiro comando possivel — e nao esta
  documentado em lugar algum.

O demo minimo ja foi desenhado pela auditoria: quatro comandos, offline, sem
chave, mostrando docs, review deterministico, review com fixture e a linha de
publicacao. O aceite deve EXECUTAR o demo.sh e conferir que ele passa, nao
so que o arquivo existe.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Quem chega ao repositorio consegue, sem ler codigo-fonte, compilar, rodar o primeiro code review, rodar a primeira documentacao e saber como plugar no proprio pull request. Um `demo.sh` mostra as tres features funcionando offline, sem chave de LLM.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Publicar as paginas copiando um workflow que funciona

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `grep de verificacao estatica sobre a documentacao de raiz` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `grep de verificacao estatica sobre a documentacao de raiz` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-455.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-455/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-455.go::TestAUR445`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-455.go::IntegrationAUR445`.
- E2E: `tests/e2e/AUR-455.sh::E2EAUR445`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-455`

Expected artifact: `.board/evidence/AUR-455/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: quebrar um comando documentado no README faz o aceite falhar porque o demo deixa de rodar.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-455/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-455.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-455/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.



## Delivery record

- commit: 70aec9739ddb6ee363ce81fd32a7d080aae96218
- review: approved
- validation: passed
