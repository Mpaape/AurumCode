---
id: AUR-450
version: 1
title: A seguranca diz o que ela nao cobre
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-442, AUR-449]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [internal/review, cmd/aurumcode, tests/acceptance/AUR-450.sh, tests/unit/AUR-450.go, tests/integration/AUR-450.go, tests/e2e/AUR-450.sh, docs/specs/AUR-450.md]
read_paths: [internal/documentation/review, internal/config, internal/analyzer, internal/prompt, internal/llm, internal/security/redaction, internal/git/githubclient, pkg/types, cmd/regenerate-docs, internal/documentation/extractors, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/site, internal/documentation/welcome, internal/pipeline, tests/fixtures/repos/git-demo, tests/fixtures/review, tests/fixtures/docs/goproject, action.yml, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Quando a passagem de seguranca nao acha nada, ela diz sobre QUE regras a busca foi feita, para o usuario nao ler `No security findings.` como se o codigo tivesse sido varrido por completo.



## Achado medido (reteste 2026-08-14)

Codigo com `hashlib.md5(password)` (weak-crypto) e `"<b>" + name + "</b>"`
(xss) recebe `Security findings (standards/security-review): No security
findings.` com exit 0. Cinco das oito regras do catalogo sao metadata-only
e o CLI nunca diz quais — o usuario le silencio como cobertura. Nao e
falso positivo nem falso negativo de um matcher: e a saida omitindo o
proprio alcance. Conserto: a saida da passagem nomeia as regras
efetivamente aplicadas (ou o numero aplicadas/total com ponteiro para a
lista), tanto no caso com achados quanto no caso vazio.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Quando a passagem de seguranca nao acha nada, ela diz sobre QUE regras a busca foi feita, para o usuario nao ler `No security findings.` como se o codigo tivesse sido varrido por completo.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Achar falha de seguranca, nao so estilo

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --base HEAD~1 --seguranca` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --base HEAD~1 --seguranca` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-450.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-450/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-450.go::TestAUR435`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-450.go::IntegrationAUR435`.
- E2E: `tests/e2e/AUR-450.sh::E2EAUR435`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-450`

Expected artifact: `.board/evidence/AUR-450/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: esconder do usuario quais regras foram aplicadas faz o aceite falhar.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-450/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-450.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-450/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.



## Delivery record

- commit: 3634e8c9cc936e6b7d92102322ca9fe53509ff40
- review: approved
- validation: passed

## Correcao de registro do coordenador (2026-08-26)

O `commit` registrado aqui era o SHA do CANDIDATO em branch de trabalho, nunca
o do commit integrado. Esse objeto so existia porque `refs/candidates/` e a
branch `card/AUR-NNN` o seguravam; ao consolidar o repositorio numa branch so --
apagando branches mergeadas, refs de candidato e rodando `gc --prune=now` -- ele
foi coletado, e `pipeline.sh` passou a recusar o board.

Tentei recuperar do `origin` antes de reescrever: o objeto nao esta la, porque
as branches remotas de candidato tambem foram apagadas na mesma consolidacao.

O commit abaixo e o que **de fato entrega este card em `main`**, verificado por
tocar os `paths` declarados. Apontar um card `done` para um objeto que ninguem
consegue inspecionar e afirmacao de entrega sem lastro; apontar para o commit
integrado e registrar o fato.

Licao operacional: registre no card o SHA **integrado**, nunca o do candidato --
o candidato e efemero por construcao. E antes de podar objetos, verifique os
SHAs que os cards `done` declaram.
