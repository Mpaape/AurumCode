---
id: AUR-436
version: 1
title: Escolher qual IA revisa, inclusive uma local
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-430]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [cmd/aurumcode, tests/acceptance/AUR-436.sh, tests/unit/AUR-436.go, tests/integration/AUR-436.go, tests/e2e/AUR-436.sh, docs/specs/AUR-436.md]
read_paths: [internal/documentation/review, internal/config, internal/git/githubclient, tests/fixtures/scm/github, cmd/regenerate-docs, internal/documentation/extractors, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/site, internal/documentation/welcome, internal/pipeline, tests/fixtures/docs/goproject, action.yml, internal/security/redaction, internal/analyzer, internal/prompt, internal/review, internal/llm, pkg/types, .aurumcode/prompts/review.md, tests/fixtures/repos/git-demo, standards/providers, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

O usuario escolhe o modelo que revisa, incluindo um modelo local, e o comando avisa com clareza quando o modelo escolhido esta indisponivel em vez de devolver review vazio.

## Restauracao

Este comportamento ja existiu no repositorio e foi removido em 2025-11-03 pelos
commits `24dae01`, `09d6033` e `698a4ae`. Recuperar em vez de reescrever.

- Origem: commit `c12d7ab` (anterior a exclusao).
- Estado ATUALIZADO pos-AUR-430 (2026-08-13): o fallback de parse degradado e o
  conserto do repairJSON para aspas dentro de string JA FORAM entregues pelo
  AUR-430 (commit b9c9ba9) em internal/prompt, que e read_path aqui. O que
  resta para este card, todo em cmd/aurumcode: o flag de selecao de
  modelo/provider (--modelo, incluindo apontar um modelo local via provider
  existente), e erro claro e acionavel quando o provider/modelo escolhido esta
  indisponivel, em vez de review vazio com exit 0. Se restaurar um provider
  inteiro (ex.: ollama) se mostrar necessario, isso e gap de contrato: pare e
  reporte, nao escreva fora de paths.
- Regra: restaurar o que passa nos testes, corrigir o defeito nomeado acima, e
  nao reescrever do zero o que ja existe. O que nao compilar e nao servir ao
  Outcome deste card fica de fora.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

O usuario escolhe o modelo que revisa, incluindo um modelo local, e o comando avisa com clareza quando o modelo escolhido esta indisponivel em vez de devolver review vazio.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Escolher qual IA revisa, inclusive uma local

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --base HEAD~1 --modelo local` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --base HEAD~1 --modelo local` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-436.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-436/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-436.go::TestAUR436`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-436.go::IntegrationAUR436`.
- E2E: `tests/e2e/AUR-436.sh::E2EAUR436`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-436`

Expected artifact: `.board/evidence/AUR-436/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: tratar modelo indisponivel como review sem achados faz o aceite falhar.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-436/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-436.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-436/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.

## Delivery record

- commit: 38ba1c8b1738a8b654edc90cd548c3ecc0666422
- review: approved
- validation: passed
