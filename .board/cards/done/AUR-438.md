---
id: AUR-438
version: 1
title: Comentar no arquivo e na linha exata
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-437]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [cmd/aurumcode, tests/acceptance/AUR-438.sh, tests/unit/AUR-438.go, tests/integration/AUR-438.go, tests/e2e/AUR-438.sh, docs/specs/AUR-438.md]
read_paths: [cmd/regenerate-docs, internal/documentation/extractors, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/site, internal/documentation/welcome, internal/pipeline, internal/git/githubclient, internal/security/redaction, internal/analyzer, internal/prompt, internal/review, internal/llm, pkg/types, tests/fixtures/scm/github, tests/fixtures/repos/git-demo, tests/fixtures/review/known-problem-response.json, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Os problemas aparecem comentados na linha correspondente do arquivo dentro do pull request, e problema fora das linhas alteradas vira comentario geral em vez de sumir.

## Restauracao

Este comportamento ja existiu no repositorio e foi removido em 2025-11-03 pelos
commits `24dae01`, `09d6033` e `698a4ae`. Recuperar em vez de reescrever.

- Origem: commit `c12d7ab` (anterior a exclusao).
- Arquivos: internal/git/githubclient/client.go PostReviewComment
- Estado medido: O comentario inline por path e linha ja esta implementado e testado.
- Regra: restaurar o que passa nos testes, corrigir o defeito nomeado acima, e
  nao reescrever do zero o que ja existe. O que nao compilar e nao servir ao
  Outcome deste card fica de fora.

## Achados herdados (2026-08-13)

Do revisor do AUR-437: extractFilePath usa o ultimo " b/" e docs/specs/AUR-437
superdeclara a regra para caminho contendo " b/" (ex.: "a b/c.go" devolve
"c.go", identico ao historico); os tipos de diff do cliente sao locais ao
pacote, espelhados de pkg/types, e a conversao pertence a este card. Do
revisor do AUR-440: o guarda de flags das lanes so inspeciona tokens com
"--" — a grafia "-publish" (que o pacote flag do Go trata igual) escapa;
normalizar traços a esquerda antes do lookup, sem HasPrefix("-") que
reprovaria "-base" legitimo; corrigir junto a frase superdimensionada em
docs/specs/AUR-440.md:38-39.

## Revisao 1 (candidato b50ebbf): REQUEST_CHANGES

Bloqueador medido: pr.go:144 le GITHUB_SHA, que esta vazio na invocacao
exata do AC-001; o GitHub real responde 422 a commit_id vazio no endpoint
de review-comment e pr.go:157-159 aborta o loop no primeiro erro, entregando
0 de 2 achados. A suite do card nao pega porque todos os testes injetam
GITHUB_SHA e os fakes aceitam qualquer commit_id. Exigencias: fail-closed
antes de qualquer POST sem SHA; falha de um item nao engole os demais;
teste com fake que responde 422 a commit_id vazio; e mover
HasWritePermission para antes do GenerateReview. Nao bloqueantes anotados:
PostIssueComment sem chave de idempotencia (assinatura do client) e a
fragilidade latente do parser de hunk para linha vazia sem espaco lider
(AUR-437).

Nota de integracao: o codigo deste candidato foi integrado ANTES do veredito
para destravar 439/441; o card so cruza para done depois do APPROVE, e o
conserto entra por cima na mesma branch.

## Acoplamento medido (2026-08-13)

Mesma razao do AUR-426: com docs.go no mesmo pacote cmd/aurumcode, o aceite
deste card precisa materializar o fecho de documentacao para compilar o
binario. Custo de materializacao, nao ampliacao de escopo.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Os problemas aparecem comentados na linha correspondente do arquivo dentro do pull request, e problema fora das linhas alteradas vira comentario geral em vez de sumir.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Comentar no arquivo e na linha exata

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --pr 42 --repo dono/projeto --publicar --na-linha` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --pr 42 --repo dono/projeto --publicar --na-linha` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-438.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-438/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-438.go::TestAUR438`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-438.go::IntegrationAUR438`.
- E2E: `tests/e2e/AUR-438.sh::E2EAUR438`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-438`

Expected artifact: `.board/evidence/AUR-438/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: descartar em silencio o achado fora do intervalo faz o aceite falhar por contagem de comentarios.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-438/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-438.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-438/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.

## Delivery record

- commit: fc73d1d4ea1dc6b8bfd140289de100e5f629bae6
- review: approved
- validation: passed
