---
id: AUR-434
version: 1
title: Fazer o review seguir o padrao do time
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-430]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [internal/review, tests/acceptance/AUR-434.sh, tests/unit/AUR-434.go, tests/integration/AUR-434.go, tests/e2e/AUR-434.sh, docs/specs/AUR-434.md]
read_paths: [internal/security/redaction, standards/code-review, cmd/aurumcode, internal/analyzer, internal/prompt, internal/llm, pkg/types, tests/fixtures/repos/git-demo, tests/fixtures/review, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Cada problema apontado cita a regra do padrao de review do projeto que o sustenta, e achado sem regra nao chega ao usuario.

## Restauracao

Este comportamento ja existiu no repositorio e foi removido em 2025-11-03 pelos
commits `24dae01`, `09d6033` e `698a4ae`. Recuperar em vez de reescrever.

- Origem: commit `c12d7ab` (anterior a exclusao).
- Arquivos: internal/review/{reviewer,rules}.go, internal/review/iso25010/{config,scorer}.go, .aurumcode/rules/*.yml, .aurumcode/iso25010-weights.yml
- Estado medido: Existem DOIS revisores: internal/reviewer (54 linhas, sem regras e sem ISO) era o usado pelo pipeline, e internal/review (com regras e ISO) tinha zero importadores. Promover internal/review e aposentar internal/reviewer. As regras hoje sao so metadados sem matcher, e o diretorio ausente resulta em zero regras em silencio: embutir por go:embed e falhar alto.
- Regra: restaurar o que passa nos testes, corrigir o defeito nomeado acima, e
  nao reescrever do zero o que ja existe. O que nao compilar e nao servir ao
  Outcome deste card fica de fora.

## Reparo de contrato (candidato e457b98)

O aceite compila cmd/aurumcode contra o motor e roda a revisao sobre as
fixtures; o accept selado saiu 69 (missing-input) porque read_paths nao
materializava esses inputs. O builder provou por simulacao que exatamente
estes sete acrescimos levam o aceite completo a exit 0 sem tocar byte fora
de paths.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Cada problema apontado cita a regra do padrao de review do projeto que o sustenta, e achado sem regra nao chega ao usuario.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Fazer o review seguir o padrao do time

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --base HEAD~1` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --base HEAD~1` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-434.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-434/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-434.go::TestAUR434`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-434.go::IntegrationAUR434`.
- E2E: `tests/e2e/AUR-434.sh::E2EAUR434`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-434`

Expected artifact: `.board/evidence/AUR-434/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar achado sem regra citada faz o aceite falhar.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-434/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-434.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-434/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.

## Delivery record

- commit: 341f39fcf14c1463143bb4ce981f34acbf5a63a2
- review: approved
- validation: passed
