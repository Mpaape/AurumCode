---
id: AUR-009
version: 1
title: Redigir segredos em todos os sinks
status: done
validation: tested
profile_owner: AUR-006
office: O00-security
depends_on: [AUR-006]
requirements: [PR-ARCH-001, PR-EVD-001, PR-EVD-002, PR-SEC-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/security/redaction, tests/security, tests/acceptance/AUR-009.sh, docs/specs/AUR-009.md, tests/specs/AUR-009, tests/integration/AUR-009.go, tests/e2e/AUR-009.sh, tests/unit/AUR-009.go]
read_paths: [go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, secret-channel]
---

## Outcome

canários em URL, header, body, diff, erro e tool result não aparecem fora do sink autorizado.

## Non-goals

- Este card não gerencia nem rotaciona segredo algum: a redaction apenas impede o vazamento nos sinks, e a custódia das credenciais permanece fora do processo do AurumCode.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-006 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- canários em URL, header, body, diff, erro e tool result não aparecem fora do sink autorizado.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Redigir segredos em todos os sinks

- Given: `tests/specs/AUR-009/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Redigir segredos em todos os sinks”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Redigir segredos em todos os sinks” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que canários em URL, header, body, diff, erro e tool result não aparecem fora do sink autorizado.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001, PR-EVD-002, PR-SEC-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Somente o contrato “Redigir segredos em todos os sinks” e os artefatos em `internal/security/redaction`, `tests/security`, `tests/acceptance/AUR-009.sh`, `docs/specs/AUR-009.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-009.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-009/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/security/redaction` como filtro único aplicado a stdout, stderr, log, finding, diff, cache e evidência, trocando query string, header de auth e corpo por marcador estável antes de qualquer escrita.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“desabilitar redaction de query strings.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-009.go::TestAUR009`.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: `tests/integration/AUR-009.go::IntegrationAUR009`.
- E2E: `tests/e2e/AUR-009.sh::E2EAUR009`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-009`

Expected artifact: `.board/evidence/AUR-009/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Redigir segredos em todos os sinks” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: desabilitar redaction de query strings.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-009/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / secret-channel

- Change: injetar o canário AURUM_SECRET_CANARY no erro produzido pelo comportamento.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-009/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `secret-channel` -> contrato “Redigir segredos em todos os sinks” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-009.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.

## Delivery record

- commit: 4b5742bb69e1f3042f3bd25bd48c8aa8f9ebb807
- review: approved
- validation: passed
