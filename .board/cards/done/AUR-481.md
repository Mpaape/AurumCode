---
id: AUR-481
version: 1
title: A passagem enxerga o defeito na grafia idiomatica de cada linguagem
status: done
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-462, AUR-466]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [internal/review/rules, tests/acceptance/AUR-481.sh, tests/unit/AUR-481.go, tests/integration/AUR-481.go, tests/e2e/AUR-481.sh, docs/specs/AUR-481.md, tests/acceptance/AUR-462.sh]
read_paths: [cmd/aurumcode, internal/analyzer, internal/config, internal/documentation/extractors, internal/documentation/extractors/bash, internal/documentation/extractors/cpp, internal/documentation/extractors/csharp, internal/documentation/extractors/go, internal/documentation/extractors/javascript, internal/documentation/extractors/powershell, internal/documentation/extractors/python, internal/documentation/extractors/rust, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/review, internal/documentation/site, internal/documentation/welcome, internal/git/githubclient, internal/llm, internal/llm/cost, internal/llm/provider/litellm, internal/pipeline, internal/prompt, internal/review/fakeprovider.go, internal/review/reviewer.go, internal/review/rules.go, internal/review/securitypass.go, internal/review/workflow_references.go, internal/review/cache, internal/security/redaction, pkg/types, tests/fixtures/review, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

O mesmo defeito e encontrado em **Rust**, cuja grafia idiomatica nenhuma das
tres regras reconhecia -- a linguagem com extrator NATIVO de documentacao e
cobertura ZERO de seguranca.

## Achado medido (2026-08-26)

Plantei os MESMOS quatro defeitos em oito linguagens e rodei a passagem
deterministica. Resultado por regra:

| Linguagem   | secret | sql | cmd | xss |
|-------------|:------:|:---:|:---:|:---:|
| JavaScript  | sim    | sim | sim | sim |
| Python      | sim    | sim | sim | --  |
| C++         | sim    | sim | sim | --  |
| Go          | sim    | sim | **NAO** | -- |
| C#          | sim    | sim | **NAO** | -- |
| PowerShell  | sim    | sim | **NAO** | -- |
| Bash        | sim    | **NAO** | **NAO** | -- |
| Rust        | **NAO** | **NAO** | **NAO** | -- |

Causa, caso a caso, lida do pattern:

- **Go**: `exec.Command("sh", "-c", "ping "+host)` nao casa. O ramo e
  `exec[lv]p?e?`, que exige `l` ou `v` depois de `exec` -- as grafias de C
  (`execl`, `execve`). O ponto de `exec.Command` tambem quebra a sequencia.
- **C#**: `Process.Start(...)` nao esta no pattern.
- **PowerShell**: `Invoke-Expression` nao esta no pattern.
- **Bash**: `eval "ping $1"` nao esta no pattern; e SQL montado por
  interpolacao de shell (`"... '$1'"`) nao tem `+` para a regra de SQL ancorar.
- **Rust**: as tres falham. `const KEY: &str = "..."` poe um TIPO entre o nome e
  o `=`, e a regra de segredo espera aspas logo apos `:` ou `=`;
  `"...".to_owned() + name` poe uma chamada de metodo entre a aspa e o `+`, que
  a regra de SQL nao espera; e `Command::new("sh").arg(...)` nao casa nenhum
  ramo de comando.

Go e a linguagem primaria deste proprio projeto, e o command injection
idiomatico dele passa batido. Rust tem extrator NATIVO de documentacao entregue
pelo AUR-427 e cobertura ZERO na passagem de seguranca -- a ferramenta documenta
bem uma linguagem em que nao acha nada.

## Non-goals

- Nao afrouxar pattern existente para ganhar cobertura. O AUR-462 e o AUR-466
  estabeleceram que falso positivo em portao de merge ensina o time a desligar o
  portao, e essa garantia vale para toda grafia nova.
- Nao introduzir analise semantica nem AST: continua matcher deterministico
  sobre linhas adicionadas.
- Nao prometer paridade total entre linguagens. O que este card entrega e a
  grafia IDIOMATICA de cada uma, e o que ficar de fora e declarado.

## Reparo de contrato do coordenador (2026-08-26)

`paths` ganha `tests/acceptance/AUR-462.sh`.

O builder mediu e reverteu, corretamente, um ramo de command injection para Rust
porque ele quebra o `MUT-001` daquele aceite: a ancora da mutacao e a LINHA
INTEIRA do pattern de `security/command-injection`, byte a byte
(`tests/acceptance/AUR-462.sh:266`), e qualquer card que evolua aquela regra a
invalida -- medido como `MUT-001/anchor-not-found`, exit 1.

Isso e defeito de desenho do arnes, nao deste card: ancorar mutacao nos bytes de
um conteudo que outros cards vao evoluir e fragil por construcao. E a segunda
vez que aparece hoje; a primeira foi o `MUT-001` do AUR-448, cuja ancora deixou
de ser unica quando o AUR-467 acrescentou um print de forma igual.

A assercao do AUR-462 continua valendo e NAO pode ser enfraquecida: neutralizar
a cobertura de Node precisa continuar sendo detectado. O que muda e COMO a
mutacao encontra o alvo -- ancore no `- id: security/command-injection` e
reescreva a linha `pattern:` seguinte, sem depender do conteudo dela.

## Reducao de escopo do coordenador (2026-08-26)

O revisor apontou, e ele esta certo: o texto original deste card prometia as
OITO linguagens, e a entrega cobre apenas Rust. Tratar o `accept` verde como
prova do AC-001 como estava escrito seria falso positivo no nivel do card --
exatamente o defeito que este projeto existe para combater.

O escopo foi reduzido ao que foi entregue e provado. Go, C#, PowerShell e bash
seguem sem cobertura na grafia idiomatica deles, com as causas ja medidas, e
viraram o card AUR-486. Rust foi priorizado por pedido explicito do usuario e
por ser o caso mais indefensavel: extrator nativo de documentacao entregue pelo
AUR-427 e cobertura zero de seguranca.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-481`

Expected artifact: `.board/evidence/AUR-481/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

- AC-001 (nominal): sobre um fixture Rust com segredo tipado, SQL concatenado e comando via shell, cada uma das tres celulas hoje marcadas NAO passa a produzir achado com a regra correta.
- AC-002 (falso positivo nao entra junto): a forma SEGURA de cada grafia nova em Rust -- `Command::new("ping").arg(host)` em argv, `Command::new("sh")` sem `-c`, `const MAX: u32 = 5`, `const GREETING: &str = "hello world"`, `format!("user {}", name)` sem verbo SQL, e consulta parametrizada com `$1` -- NAO produz achado.
- AC-003 (regressao): os achados que as oito linguagens ja produzem hoje continuam identicos, byte a byte.

## Validation

- Unit: `tests/unit/AUR-481.go::TestAUR481`.
- Integration: `tests/integration/AUR-481.go::IntegrationAUR481`.
- Acceptance: `tests/acceptance/AUR-481.sh`.
- E2E: `tests/e2e/AUR-481.sh::E2EAUR481`.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover a grafia de Go do pattern de command injection.
- Expected: o mesmo `accept` sai diferente de zero registrando
  `AUR-481/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

### MUT-002: AC-002 / repository

- Change: alargar a grafia nova ate a forma argv segura tambem casar.
- Expected: o mesmo `accept` sai diferente de zero registrando
  `AUR-481/AC-002/MUT-002`.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Delivery record

- commit: 645e7af6ca87835cfcdf3c27aeec988127036312
- review: approved
- validation: passed

Rust ganhou as tres regras. O revisor dirigiu oito formas LEGITIMAS de Rust pelas
regras reais carregadas, e nao por regex colada: `Command::new("ping").arg(host)`,
`sh` sem `-c`, `bash` com script, `const` inteiro, `const` de saudacao, `format!`
sem verbo SQL, consulta parametrizada, e a palavra `select` em prosa. Nenhuma
disparou.

O escopo do card foi REDUZIDO antes do done: ele prometia oito linguagens e a
entrega e Rust. Fechar sobre a promessa original seria falso positivo no nivel do
card -- o defeito que este projeto combate. Go, C#, PowerShell e bash foram para
o AUR-486 com as causas ja medidas.

A reancoragem do `MUT-001` do AUR-462 preservou a forca daquela assercao: as tres
formas de Node continuam presas por arquivo e linha, e inverter o guard faz o awk
sair 1 em vez de virar no-op silencioso.
