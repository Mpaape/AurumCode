---
id: AUR-359
version: 1
title: Fixar trust root do runner OCI
status: done
office: O00-security
depends_on: []
requirements: [PR-SUP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [.board/bootstrap/locks/trust-root.yml, .board/bin/oci-run, .board/oci/profiles/trust-root-docker-v1.json, .board/locks/oci/trust-root-docker-v1.lock.json, tests/bootstrap/locks/AUR-359, tests/acceptance/AUR-359.sh, docs/specs/AUR-359.md]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: 282d2888d70e73665d776887c233ded47ea50983
spec_digest: sha256:9a181f2dd5c672ec998862fb99fc8a85adc80c2a90fb5e21befb43d9a55e5744
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine, supply-chain]
---

## Outcome

o lock registra o SHA-256 do runner, `bash@sha256:ae4668c2560999e65e89532cd2ad1b6688bb23298189f0bd229ef80fa4bd0831`, perfil non-root/read-only/network-none e o engine observado `docker`; Podman permanece `not-observed`; o próprio perfil `trust-root-docker-v1` passa a existir em `.board/oci/profiles/trust-root-docker-v1.json`, selado por `.board/locks/oci/trust-root-docker-v1.lock.json`, com flags idênticas ao vetor Docker direto declarado em `accept`.

## Non-goals

- Não observar nem selar Podman como engine e não executar cards por este lock; o trust root apenas fixa runner, imagem e perfil para `docker`.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/bootstrap/locks/AUR-359/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real. Docker 29.6.1 foi observado neste workspace; Podman não está instalado e nenhum verdict Podman é emitido.

## Postconditions

- o lock registra o SHA-256 do runner, `bash@sha256:ae4668c2560999e65e89532cd2ad1b6688bb23298189f0bd229ef80fa4bd0831`, perfil non-root/read-only/network-none e o engine observado `docker`; Podman permanece `not-observed`; o próprio perfil `trust-root-docker-v1` passa a existir em `.board/oci/profiles/trust-root-docker-v1.json`, selado por `.board/locks/oci/trust-root-docker-v1.lock.json`, com flags idênticas ao vetor Docker direto declarado em `accept`.
- O perfil e seu lock são reproduzíveis sem `.board/bin/oci-run`: o aceite deste card chama Docker direto no digest pinado, de modo que fixar o trust root não depende do wrapper que ele mesmo autoriza.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Fixar trust root do runner OCI

- Given: `tests/bootstrap/locks/AUR-359/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 256 entradas, 4 MiB e 15 s.
- When: o acceptance executa `tests/acceptance/AUR-359.sh` no runner pinado com network none, read-only e user 65532:65532, e `VerifyBootstrapLockV1` confronta `.board/bin/oci-run` e o engine observado com `.board/bootstrap/locks/trust-root.yml`.
- Then: o nominal retorna `pass` e o lock registra o SHA-256 do runner, `bash@sha256:ae4668c2560999e65e89532cd2ad1b6688bb23298189f0bd229ef80fa4bd0831`, perfil non-root/read-only/network-none e o engine observado `docker`; Podman permanece `not-observed`; ausência, tamper/symlink, campo inválido e over-limit retornam respectivamente `trust_root_mismatch`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded` antes de efeito externo.
- Covers requirements: [PR-SUP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `trust_root_mismatch|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-359.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-359/AC-001/trust_root_mismatch`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir o lock .board/bootstrap/locks/trust-root.yml e validar o lock registra o SHA-256 do runner, `bash@sha256:ae4668c2560999e65e89532cd2ad1b6688bb23298189f0bd229ef80fa4bd0831`, perfil non-root/read-only/network-none e o engine observado `docker`; Podman permanece `not-observed` antes de qualquer consumo do domínio.
- Refactor: após GREEN, reordenar entradas canonicamente e preservar identity set, error codes e manifest digest no replay; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-359.sh::ContractAUR359`.
- Integration: `tests/acceptance/AUR-359.sh::IntegrationAUR359`.
- E2E: `tests/acceptance/AUR-359.sh::E2EAUR359`.

## Acceptance

container_profile: `trust-root-docker-v1`

accept: `docker run --rm --network=none --read-only --cap-drop=ALL --security-opt=no-new-privileges --pids-limit=64 --memory=128m --cpus=0.5 --user=65532:65532 -v "${PWD}":/src:ro bash@sha256:ae4668c2560999e65e89532cd2ad1b6688bb23298189f0bd229ef80fa4bd0831 /src/tests/acceptance/AUR-359.sh`

Expected artifact: o coordinator deriva `.board/evidence/AUR-359/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: alterar um byte de `.board/bin/oci-run` depois de calcular o digest do lock.
- Expected: o aceite sai 1 com `AUR-359/AC-001/runner-digest-mismatch`; restaurar recupera o digest nominal.

### MUT-002: AC-001 / container-engine

- Change: remover `--network=none` do vetor hardened usado pelo probe Docker.
- Expected: o probe de argumentos sai 1 com `AUR-359/AC-001/egress-guard-missing`; restaurar nega rede.

### MUT-003: AC-001 / supply-chain

- Change: trocar a referência digest-pinned da imagem por `bash:latest`.
- Expected: o parser sai 1 com `AUR-359/AC-001/mutable-image`; restaurar aceita apenas o digest exato.

## Security and privacy

- Data flow: runner bootstrap e a imagem bash digest-pinned -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-359.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.
- `docs/specs/AUR-359.md` também registra os campos de `.board/oci/profiles/trust-root-docker-v1.json` e de `.board/locks/oci/trust-root-docker-v1.lock.json`, e por que o `accept` deste card usa Docker digest-pinado direto em vez do wrapper que o perfil habilita.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: consumidores migram somente depois que o agregador AUR-233 aceita todos os lock manifests.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer A: cada hunk e as dez dimensões do protocolo, priorizando correção, SOLID, compatibilidade, determinismo e sensibilidade dos testes.
- Reviewer B: cada hunk e as dez dimensões do protocolo, priorizando entradas hostis, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 selada, mas usam processos, sessões, conversas, caches, memória e relatórios separados; registrar backend real e nível I0/I1/I2/I3.
- Skeptical approver: pré-sela hipóteses, executa todas as mutações em OCI limpo, restaura e repete o aceite; falha, divergência ou inconclusivo veta.

## Evidence

Somente o coordinator grava artefatos sanitizados em `.board/evidence/AUR-359/`: spec/identity seladas, red-green-refactor, clean tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay restaurado, hashes e verdicts. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

