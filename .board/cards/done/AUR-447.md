---
id: AUR-447
version: 1
title: O CLI documenta Rust e C# como o pipeline ja documenta
status: done
validation: tested
profile_owner: AUR-006
office: O10-documentacao
depends_on: [AUR-006, AUR-426, AUR-427]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [cmd/aurumcode, tests/acceptance/AUR-447.sh, tests/unit/AUR-447.go, tests/integration/AUR-447.go, tests/e2e/AUR-447.sh, docs/specs/AUR-447.md]
read_paths: [internal/config, internal/documentation/extractors, internal/documentation/incremental, internal/documentation/normalizer, internal/documentation/site, internal/documentation/welcome, internal/pipeline, cmd/regenerate-docs, internal/analyzer, internal/prompt, internal/review, internal/llm, internal/security/redaction, internal/git/githubclient, pkg/types, tests/fixtures/docs/goproject, tests/fixtures/docs/rustproject, tests/fixtures/repos/git-demo, tests/fixtures/review, action.yml, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Rodar `aurumcode docs --source <projeto Rust ou C#>` gera as paginas, como o `cmd/regenerate-docs` ja gera. Hoje o CLI tem seu proprio registro de extratores e devolve "no extractor registered" para Rust, enquanto o pipeline documenta os dois.

## Nota de dispatch (2026-08-13)

Read-set pre-reparado com os DOIS fechos ja provados: o de documentacao
(AUR-424/425, incluindo action.yml lido pelos testes do pacote cmd) e o do
motor (o binario aurumcode ja carrega review). O subcomando docs vive em
cmd/aurumcode; cmd/regenerate-docs continua sendo o binario existente que ele
reaproveita, nunca reescreve.

## Acoplamento medido (2026-08-13)

cmd/aurumcode e um pacote so: quando o AUR-438 acrescentou pr.go, todo card
que compila esse pacote passou a precisar materializar tambem os imports do
vizinho. Por isso internal/git/githubclient entra no read-set deste card, e o
fecho de documentacao entra no do AUR-438. Nao e escopo novo: e o custo de
materializacao do mesmo binario.

## Achado medido (revisao do AUR-427, 2026-08-13)

`cmd/aurumcode/docs.go` tem um `registerDocsExtractors` proprio que NAO herda
os extratores nativos entregues pelo AUR-427; o revisor reproduziu:
`aurumcode docs --source <fixture rust>` sai 1 com "no extractor registered"
e zero paginas, enquanto `cmd/regenerate-docs` sobre a mesma fixture gera
paginas reais. Este card faz o CLI usar o mesmo registro, sem duplicar
logica de extracao.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Rodar `aurumcode docs --source <projeto Rust ou C#>` gera as paginas, como o `cmd/regenerate-docs` ja gera. Hoje o CLI tem seu proprio registro de extratores e devolve "no extractor registered" para Rust, enquanto o pipeline documenta os dois.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Rodar a documentacao pela linha de comando

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode docs --help` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode docs --help` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-447.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-447/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-447.go::TestAUR426`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-447.go::IntegrationAUR426`.
- E2E: `tests/e2e/AUR-447.sh::E2EAUR426`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-447`

Expected artifact: `.board/evidence/AUR-447/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover o registro dos extratores nativos do CLI faz o aceite falhar porque nenhuma pagina Rust e gerada.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-447/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-447.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-447/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.


## Delivery record

- commit: 2eb2d867017bc3e60ce61d8d3cb93e25e960bb0f
- review: approved
- validation: passed

## Correcao de registro do coordenador (2026-08-26)

O `commit` registrado aqui era o SHA do CANDIDATO em branch de trabalho, nunca
o do commit integrado. Esse objeto so existia porque `refs/candidates/` e a
branch `card/AUR-NNN` o seguravam; ao consolidar o repositorio numa branch so --
apagando branches mergeadas, refs de candidato e rodando `gc --prune=now` -- ele
foi coletado, e `pipeline.sh` passou a recusar o board.

Tentei recuperar do `origin` antes de reescrever: o objeto nao esta la, porque
as branches remotas de candidato tambem foram apagadas na mesma consolidacao.

O commit abaixo e o que **de fato entrega este card em `main`**, verificado por
tocar os `paths` declarados. Apontar um card `done` para um objeto que ninguem
consegue inspecionar e afirmacao de entrega sem lastro; apontar para o commit
integrado e registrar o fato.

Licao operacional: registre no card o SHA **integrado**, nunca o do candidato --
o candidato e efemero por construcao. E antes de podar objetos, verifique os
SHAs que os cards `done` declaram.
