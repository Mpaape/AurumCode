---
id: AUR-456
version: 1
title: O repositorio nao carrega copia morta nem promessa falsa
status: backlog
validation: tested
profile_owner: AUR-006
office: O11-paginas
depends_on: [AUR-006, AUR-445, AUR-446]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [.aurumcode/rules, .aurumcode/prompts, configs, index.md, _config.yml, _api, .github/actions, pages-fix.md, test-jekyll.sh, docs/specs/AUR-456.md, tests/acceptance/AUR-456.sh, tests/unit/AUR-456.go, tests/integration/AUR-456.go, tests/e2e/AUR-456.sh]
read_paths: [internal/review, internal/prompt, internal/documentation/welcome, cmd/aurumcode, cmd/regenerate-docs, action.yml, scripts, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Nenhum arquivo do repositorio afirma capacidade que o codigo nao tem, e nenhuma copia morta de regra ou prompt convive com a versao viva embutida no binario.



## Achados medidos (auditoria 2026-08-14)

Cada item abaixo foi verificado por ausencia de importador ou por divergencia
com a versao viva:
- `.aurumcode/rules/*.yml`: intocado desde o primeiro commit; a versao viva
  (internal/review/rules, go:embed) mudou em 2026-08-13 e DIVERGE. Nenhum
  codigo Go le esse caminho.
- `.aurumcode/prompts/*.md`: mortos — internal/prompt usa go:embed proprio.
  EXCECAO VIVA: `.aurumcode/prompts/documentation/welcome-page.md` E lido por
  internal/documentation/welcome/generator.go como override opcional. MANTER.
- `configs/`: zero referencia em Go ou docs.
- `index.md` e `_config.yml` da raiz: descrevem uma "plataforma" com QA
  Testing e Docker Hub que nao existe; nunca lidos (o gerador escreve
  `.aurumcode/_config.yml`).
- `_api/*.md`: documenta pipelines que nao existem (review/docs/qa).
- `.github/actions/aurumcode-docs/action.yml`: segunda definicao de Action com
  inputs incompativeis com o action.yml real.
- `pages-fix.md`, `test-jekyll.sh` (sobrescreve o Gemfile real se rodado),
  `.env copy.example`: lixo avulso.

NAO APAGAR (verificados vivos): scripts/action-entrypoint.sh e vizinhos (sao
o ENTRYPOINT real do Dockerfile e o gate de build da imagem);
tests/characterization/legacy-baseline (fixture ativa do AUR-002);
.aurumcode/_config.yml, Gemfile e .gitignore (whitelisted para o Pages deste
repo); .taskmaster (ja rotulado historico pelo CLAUDE.md).

O aceite prova que o que foi apagado nao tinha importador E que o que foi
mantido continua funcionando: o welcome-page override, o entrypoint do
Dockerfile e o accept do AUR-002.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Nenhum arquivo do repositorio afirma capacidade que o codigo nao tem, e nenhuma copia morta de regra ou prompt convive com a versao viva embutida no binario.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Publicar as paginas copiando um workflow que funciona

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `grep de verificacao estatica sobre a documentacao de raiz` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `grep de verificacao estatica sobre a documentacao de raiz` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-456.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-456/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-456.go::TestAUR445`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-456.go::IntegrationAUR445`.
- E2E: `tests/e2e/AUR-456.sh::E2EAUR445`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-456`

Expected artifact: `.board/evidence/AUR-456/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: reintroduzir uma copia morta de regra faz o aceite falhar porque ela diverge da versao viva.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-456/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-456.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-456/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.


