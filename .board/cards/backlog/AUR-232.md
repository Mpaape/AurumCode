---
id: AUR-232
version: 1
title: Provar perfil seguro no Podman
status: backlog
validation: tested
profile_owner: AUR-410
office: O08-testqa
depends_on: [AUR-228, AUR-229, AUR-230, AUR-410]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [tests/containers/engines/podman, tests/acceptance/AUR-232.sh, docs/specs/AUR-232.md, tests/unit/AUR-232.go, tests/contracts/AUR-232.go, tests/integration/AUR-232.go, tests/e2e/AUR-232.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/oci-conformance-v1.json, .board/locks/oci/oci-conformance-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

a mesma conformance suite executa no Podman rootless e detecta todos os probes sem expectativa específica de Docker.

## Non-goals

- Instalar ou configurar o Podman no host e cobrir o caminho Docker da suite: este card prova o perfil sob um Podman rootless já disponível.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-228, AUR-229, AUR-230 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a mesma conformance suite executa no Podman rootless e detecta todos os probes sem expectativa específica de Docker.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Provar perfil seguro no Podman

- Given: os manifests aprovados de AUR-228, AUR-229 e AUR-230 fixam o perfil de sandbox na mesma base, e um Podman rootless sem daemon está disponível para o perfil `oci-conformance-v1`.
- When: a suite de `tests/containers/engines/podman` executa os mesmos probes sob Podman rootless, sem consultar socket de daemon nem usar flags exclusivas do Docker.
- Then: cada probe é detectado sob Podman rootless, e a suite reprova se alguma asserção depender de mensagem, caminho de socket ou flag específica do Docker.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Provar perfil seguro no Podman” nos artefatos `tests/containers/engines/podman`, `tests/acceptance/AUR-232.sh`, `docs/specs/AUR-232.md`.

## TDD proof

- Test: `tests/acceptance/AUR-232.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-232/AC-001: Provar perfil seguro no Podman ausente`; build/loader/infra não valem.
- Green: escrever em `tests/containers/engines/podman` o runner rootless que reaproveita os probes compartilhados e normaliza as asserções para não depender da saída do Docker.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-232.go::TestAUR232`.
- Contract: `tests/contracts/AUR-232.go::ContractAUR232`.
- Integration: `tests/integration/AUR-232.go::IntegrationAUR232`.
- E2E: `tests/e2e/AUR-232.sh::E2EAUR232`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-232`

Expected artifact: `.board/evidence/AUR-232/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: substituir Podman rootless por engine rootful e mascarar o probe.
- Expected: o acceptance inalterado falha com `AUR-232/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-232/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Provar perfil seguro no Podman” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-232.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
