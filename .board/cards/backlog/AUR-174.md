---
id: AUR-174
version: 1
title: Configurar profile Azure OpenAI
status: backlog
validation: tested
profile_owner: AUR-405
office: O03-providers
depends_on: [AUR-061, AUR-067, AUR-405]
requirements: [PR-ARCH-001, PR-LLM-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]
paths: [internal/providers/azure, tests/acceptance/AUR-174.sh, docs/specs/AUR-174.md, tests/fixtures/AUR-174, tests/unit/AUR-174.go, tests/contracts/AUR-174.go, tests/integration/AUR-174.go, tests/e2e/AUR-174.sh]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: confidential
trust_boundaries: [repository, model-provider, secret-channel]
---

## Outcome

`AUR174ResultV1` é o único resultado reversível de “Configurar profile Azure OpenAI” e limita a implementação a `internal/providers/azure`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “usar model name no lugar de deployment requerido”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-061, AUR-067]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-174` contém exatamente um caso nominal e um contracaso mutado, sob 32 respostas, 1 MiB total, 10 s e 1 CPU; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR174ResultV1` canônico e o contracaso produz `AUR174_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Configurar profile Azure OpenAI

- Given: CandidateIdentityV1, digests de [AUR-061, AUR-067] e a fixture bounded `tests/fixtures/AUR-174` com caso nominal e “usar model name no lugar de deployment requerido”.
- When: o consumidor invoca “Configurar profile Azure OpenAI” uma vez no port público card-scoped, com limite de 32 respostas, 1 MiB total, 10 s e 1 CPU.
- Then: deployment, api-version e managed identity passam o provider contract sem tipos Azure no domínio; o consumidor recebe `AUR174ResultV1` com status e digest canônicos. O contracaso “usar model name no lugar de deployment requerido” retorna `AUR174_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-LLM-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]

## Public contract

- `AUR174ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Configurar profile Azure OpenAI”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-174.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-174/AC-001: AUR174_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR174ResultV1` dentro de 32 respostas, 1 MiB total, 10 s e 1 CPU, enquanto “usar model name no lugar de deployment requerido” retorna `AUR174_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-174.go::TestAUR174NominalAndMutated`.
- Contract: `tests/contracts/AUR-174.go::TestAUR174ResultV1`.
- Integration: `tests/integration/AUR-174.go::TestAUR174Boundary`.
- E2E: `tests/e2e/AUR-174.sh::AC-001`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-174`

Expected artifact: o coordenador valida `.board/evidence/AUR-174/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: usar model name no lugar de deployment requerido.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-174/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / model-provider

- Change: retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-174/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: injetar AURUM_SECRET_CANARY no erro da fronteira.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-174/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR174ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-174.md` fixa input, output, `AUR174_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR174ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
