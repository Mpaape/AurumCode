---
id: AUR-105
version: 1
title: Publicar review no GitHub
status: backlog
office: O06-scm
depends_on: [AUR-100, AUR-103, AUR-104, AUR-107, AUR-081, AUR-220, AUR-409]
requirements: [PR-ARCH-001, PR-SCM-001, PR-SCM-002, PR-REV-005]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001, CR-GH-002, CR-GH-003, CR-GH-004]
paths: [internal/scm/github/publish, tests/acceptance/AUR-105.sh, docs/specs/AUR-105.md, tests/specs/AUR-105, tests/contracts/AUR-105.go, tests/integration/AUR-105.go, tests/e2e/AUR-105.sh, tests/unit/AUR-105.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, scm, secret-channel]
---

## Outcome

uma review agrupa inline comments válidos e trata 401, 403, 422, 429, 5xx e head moved.

## Non-goals

- Não criar check run, commit status nem release: este card publica apenas a review e seus comentários inline no pull request do GitHub.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-100, AUR-103, AUR-104, AUR-081 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- uma review agrupa inline comments válidos e trata 401, 403, 422, 429, 5xx e head moved.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Publicar review no GitHub

- Given: `tests/specs/AUR-105/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Publicar review no GitHub”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Publicar review no GitHub” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact traz uma única submissão de review com os inline comments agrupados, erros tipados distintos para 401, 403, 422, 429 e 5xx e o abort registrado quando o head SHA mudou desde a leitura.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-SCM-001, PR-SCM-002, PR-REV-005]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001, CR-GH-002, CR-GH-003, CR-GH-004]

## Public contract

- Apenas “Publicar review no GitHub” e `internal/scm/github/publish`, `tests/acceptance/AUR-105.sh`, `docs/specs/AUR-105.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-105.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-105/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: implementar em `internal/scm/github/publish` o agrupamento dos inline comments numa única submissão de review, o mapeamento de 401, 403, 422, 429 e 5xx para erros tipados e o abort quando o head SHA mudou desde a leitura.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“repetir comment após retry 5xx.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-105.go::TestAUR105`.
- Contract: `tests/contracts/AUR-105.go::ContractAUR105`.
- Integration: `tests/integration/AUR-105.go::IntegrationAUR105`.
- E2E: `tests/e2e/AUR-105.sh::E2EAUR105`.

## Acceptance

container_profile: `fake-scm-offline-v1`

accept: `./.board/bin/oci-run --profile fake-scm-offline-v1 --card AUR-105`

Expected artifact: `.board/evidence/AUR-105/acceptance/AC-001.json` valida schema, prova “Publicar review no GitHub” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: repetir comment após retry 5xx.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-105/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / scm

- Change: mudar o head remoto depois da leitura ou fornecer identidade de repositório divergente.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-105/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / secret-channel

- Change: injetar o canário AURUM_SECRET_CANARY na falha devolvida pela fronteira.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-105/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `scm`, `secret-channel` -> “Publicar review no GitHub” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-105.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: todas as dez dimensões e cada hunk; prioridade em correção, SOLID, compatibilidade e testes.
- Reviewer B: todas as dez dimensões e cada hunk; prioridade em hostilidade, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 igual e selada; processo, sessão, conversation, cache e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas MUT em OCI limpo, restaurar/repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-105/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` requer evento humano autenticado. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
