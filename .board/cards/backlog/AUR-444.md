---
id: AUR-444
version: 1
title: A Action publicada roda o review que ela anuncia
status: backlog
validation: tested
profile_owner: AUR-006
office: O11-paginas
depends_on: [AUR-006, AUR-439]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [scripts/action-entrypoint.sh, Dockerfile, action.yml, tests/acceptance/AUR-444.sh, tests/unit/AUR-444.go, tests/integration/AUR-444.go, tests/e2e/AUR-444.sh, docs/specs/AUR-444.md]
read_paths: [cmd/aurumcode, cmd/regenerate-docs, .github/workflows/examples/code-review.yml, .github/workflows/examples/documentation.yml, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

A Action publicada consegue de fato executar o code review: o entrypoint invoca o binario com as flags que existem, e a imagem contem o binario que ele invoca.


## Achado medido (auditoria 2026-08-13)

- `scripts/action-entrypoint.sh:171-175` chama `review --provider= --model=
  --api-key= --github-token= --post-comments=`. NENHUMA dessas flags existe;
  as reais sao `--base --fail-on --modelo --seguranca --limite --pr --repo
  --publicar --na-linha --check`. O comando falharia no parse.
- `scripts/action-entrypoint.sh:51-56,120` e `Dockerfile:44-50` afirmam "there
  is no cmd/cli in the source tree" e a imagem nao copia `cmd/aurumcode`.
  A afirmacao e falsa e a consequencia e que o binario nao esta na imagem.
- `action.yml:209,222` declara `gomarkdoc` como necessario para Go; falso
  desde o AUR-424 (o extrator usa go/parser+go/doc).
Corrigir os tres: flags reais, binario na imagem, e a prosa do action.yml
sem exigir ferramenta que nao e mais usada.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

A Action publicada consegue de fato executar o code review: o entrypoint invoca o binario com as flags que existem, e a imagem contem o binario que ele invoca.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Publicar as paginas copiando um workflow que funciona

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `scripts/action-entrypoint.sh` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `scripts/action-entrypoint.sh` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-444.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-444/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-444.go::TestAUR428`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-444.go::IntegrationAUR428`.
- E2E: `tests/e2e/AUR-444.sh::E2EAUR428`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-444`

Expected artifact: `.board/evidence/AUR-444/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: reintroduzir uma flag inexistente no entrypoint faz o aceite falhar porque o binario recusa o parse.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-444/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-444.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-444/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.

