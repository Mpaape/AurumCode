---
id: AUR-342
version: 1
title: Servir OpenAI Responses unary determinístico
status: backlog
office: O03-providers
depends_on: [AUR-055, AUR-057, AUR-350, AUR-405]
requirements: [PR-ARCH-001, PR-LLM-001, PR-LLM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]
paths: [tests/fakes/providers/openai-responses/unary, tests/containers/providers/AUR-342, tests/specs/AUR-342, tests/contracts/AUR-342.go, tests/integration/AUR-342.go, tests/acceptance/AUR-342.sh, docs/specs/AUR-342.md, tests/unit/AUR-342.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, model-provider, network-endpoint]
---

## Outcome

Um fake `POST /v1/responses` valida o request unary Responses API declarado e
devolve exatamente um response completo com output e usage canônicos.

## Non-goals

- Chat Completions, streaming, tool calls, background mode ou estado de conversation.
- Coagir input Chat para Responses ou realizar downgrade silencioso.

## Preconditions

- Contratos AUR-055/AUR-057 aprovados e fault executor AUR-350 disponível na mesma identidade.
- Fixture contém apenas conteúdo sintético e endpoint loopback.

## Postconditions

- Request nominal retorna um `response.completed` unary representado pelo golden versionado.
- Capability ou campo fora do subset falha antes de produzir output.

## Acceptance scenarios

### AC-001: Responder Responses API unary

- Given: `tests/specs/AUR-342/cases.yaml` fixa `model`, array `input`, `max_output_tokens=64`, output_text esperado, campo desconhecido, `stream=true` e body maior que 64 KiB; deadline é 5 s.
- When: o client envia cada body para `/v1/responses` e captura request digest, status, error code, response digest e quantidade de outputs.
- Then: nominal retorna 200, `status=completed`, um output message e usage exato; campo desconhecido, streaming e oversize retornam `unsupported_responses_field`, `stream_capability_required` e `request_too_large` antes de qualquer output.
- And: repetir nominal preserva IDs sintéticos, ordenação e digest; nenhuma chamada alcança rede externa ou credential resolver.

- Covers requirements: [PR-ARCH-001, PR-LLM-001, PR-LLM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]

## Public contract

- Subset unary explícito de `/v1/responses`, sem conversão implícita para Chat e sem tipos externos no core.

## TDD proof

- Test: `tests/acceptance/AUR-342.sh::AC-001`.
- Red: listener válido sem handler Responses retorna `AUR-342/AC-001/responses-route-missing`; loader, bind, profile ou compilação não contam.
- Green: validar capabilities/campos antes do handler e emitir o golden completed bounded.
- Refactor: remover a guarda de `stream=true` faz MUT-001 sobreviver e reprova; restore recupera bytes e error code.
- Unit: `tests/unit/AUR-342.go::TestAUR342`.
- Contract: `tests/contracts/AUR-342.go::ContractAUR342`.
- Integration: `tests/integration/AUR-342.go::IntegrationAUR342`.
- E2E: not-applicable: AUR-059/AUR-332 cobrem agregação e ModelPort.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-342`

Expected artifact: `.board/evidence/AUR-342/acceptance/AC-001.json` contém case/status/output-count digests, zero-egress e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar `stream=true` no handler unary.
- Expected: acceptance falha com `AUR-342/AC-001/MUT-001/capability-downgrade`; restore volta a `stream_capability_required`.

### MUT-002: AC-001 / model-provider

- Change: omitir o único output mantendo status completed.
- Expected: contract falha com `AUR-342/AC-001/MUT-002/output-missing`; restore reproduz o golden.

### MUT-003: AC-001 / network-endpoint

- Change: aceitar `GET /v1/responses` como se fosse o POST allowlisted.
- Expected: o acceptance falha com `AUR-342/AC-001/MUT-003/method-accepted`; restore retorna `method_not_allowed` antes do handler.

## Security and privacy

- Data flow: fixture sintética -> loopback HTTP -> status/digests bounded.
- Secrets: authorization, cookies e canário são recusados e não persistidos.
- Least privilege: container non-root/read-only, network interna sem egress e sem engine socket.

## Documentation

- `docs/specs/AUR-342.md` lista subset, capability errors, limites e exemplos sintéticos.

## Compatibility, migration, rollback

- Compatibility: não promete campos Responses fora do subset versionado.
- Migration: not-applicable para fixture de teste.
- Rollback: remover handler e sua entrada do gate, sem estado persistente.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
