---
id: AUR-054
version: 1
title: Aplicar retenção e GC do índice
status: backlog
office: O02-index
depends_on: [AUR-052, AUR-403]
requirements: [PR-ARCH-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-006]
paths: [internal/index/gc, tests/acceptance/AUR-054.sh, docs/specs/AUR-054.md, tests/specs/AUR-054, tests/integration/AUR-054.go, tests/unit/AUR-054.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, database, git-metadata]
---

## Outcome

snapshots atuais e blobs referenciados sobrevivem, órfãos expiram conforme budgets de bytes/idade e o cache inteiro pode ser removido sem afetar o checkout.

## Non-goals

- Não compacta o banco nem devolve espaço ao sistema de arquivos por vacuum: o card apenas remove linhas e blobs que nenhum snapshot vivo referencia.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-052 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- snapshots atuais e blobs referenciados sobrevivem, órfãos expiram conforme budgets de bytes/idade e o cache inteiro pode ser removido sem afetar o checkout.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Aplicar retenção e GC do índice

- Given: `tests/specs/AUR-054/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Aplicar retenção e GC do índice”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Aplicar retenção e GC do índice” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que snapshots atuais e blobs referenciados sobrevivem, órfãos expiram conforme budgets de bytes/idade e o cache inteiro pode ser removido sem afetar o checkout.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-006]

## Public contract

- Apenas “Aplicar retenção e GC do índice” e `internal/index/gc`, `tests/acceptance/AUR-054.sh`, `docs/specs/AUR-054.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-054.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-054/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/index/gc` marcando a partir dos snapshots vivos, expirando órfãos que excedem os budgets de bytes e idade e mantendo o diretório de cache inteiramente removível sem tocar o checkout.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“remover blob ainda referenciado.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-054.go::TestAUR054`.
- Contract: not-applicable: nenhum port ou formato externo é criado.
- Integration: `tests/integration/AUR-054.go::IntegrationAUR054`.
- E2E: not-applicable: não entrega sozinho use case público.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-054`

Expected artifact: `.board/evidence/AUR-054/acceptance/AC-001.json` valida schema, prova “Aplicar retenção e GC do índice” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover blob ainda referenciado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-054/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / database

- Change: corromper o cabeçalho ou exceder o budget do SQLite durante a operação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-054/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / git-metadata

- Change: resolver o alvo para a raiz do worktree em vez do Git dir autorizado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-054/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `database`, `git-metadata` -> “Aplicar retenção e GC do índice” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-054.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
