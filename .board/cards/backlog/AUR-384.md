---
id: AUR-384
version: 1
title: Mapear claims do getting started
status: backlog
validation: tested
profile_owner: AUR-408
office: O14-legacy
depends_on: [AUR-001, AUR-195, AUR-408]
requirements: [PR-ARCH-001, PR-DOC-003, PR-DEMO-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [docs/getting-started.md, tests/characterization/legacy/guide-claims/AUR-384, tests/acceptance/AUR-384.sh, docs/specs/AUR-384.md]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, documentation-tool]
---

## Outcome

a matriz cobre todos os ranges de `docs/getting-started.md`, associa claim a requirement/evidence/disposition e mantém `commands_executed=0`.

## Non-goals

- Validar que o passo a passo de `docs/getting-started.md` leva um usuário novo a um build funcional; a matriz apenas mapeia cada range a requirement, evidence ou disposition.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-001, AUR-195]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/characterization/legacy/guide-claims/AUR-384/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- a matriz cobre todos os ranges de `docs/getting-started.md`, associa claim a requirement/evidence/disposition e mantém `commands_executed=0`.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Mapear claims do getting started

- Given: `tests/characterization/legacy/guide-claims/AUR-384/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 256 claims, 2 MiB e 10 s.
- When: o extrator lexical percorre `docs/getting-started.md` por ranges, sem executar fenced blocks, links ou comandos, e valida cada disposition.
- Then: o nominal retorna `pass` e a matriz cobre todos os ranges de `docs/getting-started.md`, associa claim a requirement/evidence/disposition e mantém `commands_executed=0`; ausência, tamper/symlink, campo inválido e over-limit retornam respectivamente `guide_claim_unmapped`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded` antes de efeito externo.
- Covers requirements: [PR-ARCH-001, PR-DOC-003, PR-DEMO-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `guide_claim_unmapped|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-384.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-384/AC-001/guide_claim_unmapped`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir a matriz completa de claims de `docs/getting-started.md`, qualificando cada claim sem alterar regiões manuais.
- Refactor: após GREEN, normalizar claim IDs preservando ranges, dispositions, regiões manuais e digest ordenado; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-384.sh::ContractAUR384`.
- Integration: `tests/acceptance/AUR-384.sh::IntegrationAUR384`.
- E2E: `tests/acceptance/AUR-384.sh::E2EAUR384`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-384`

Expected artifact: o coordinator deriva `.board/evidence/AUR-384/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: omitir o último range com claim de `docs/getting-started.md` da matriz.
- Expected: o aceite sai 1 com `AUR-384/AC-001/range-uncovered`; restore recupera cobertura e digest.

### MUT-002: AC-001 / documentation-tool

- Change: executar um fenced command ou resolver URL durante a extração lexical.
- Expected: o no-exec spy sai 1 com `AUR-384/AC-001/document-executed`; restore mantém zero processos e conexões.

## Security and privacy

- Data flow: pré-requisitos, comandos iniciais, outputs e links locais -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-384.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: a matriz é somente caracterização; correção de claim usa card posterior que preserva ownership manual.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
