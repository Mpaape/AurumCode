---
id: AUR-074
version: 1
title: Definir contrato de skill de linguagem
status: backlog
office: O04-agents
depends_on: [AUR-072, AUR-224, AUR-403]
requirements: [PR-ARCH-001, PR-SKL-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]
paths: [skills/builtin/languages, tests/fixtures/polyglot/skills, tests/acceptance/AUR-074.sh, docs/specs/AUR-074.md, tests/specs/AUR-074, tests/contracts/AUR-074.go, tests/integration/AUR-074.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, skill-runtime, model-provider]
---

## Outcome

um contrato único de skill de linguagem normaliza detector, regras, exemplos, capabilities e finding output sem implementar uma linguagem concreta.

## Non-goals

- não implementa a skill de nenhuma linguagem concreta como Go, Rust ou TypeScript; o card fixa somente o contrato e as fixtures que o validam.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-072, AUR-224 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- um contrato único de skill de linguagem normaliza detector, regras, exemplos, capabilities e finding output sem implementar uma linguagem concreta.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Definir contrato de skill de linguagem

- Given: `tests/specs/AUR-074/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Definir contrato de skill de linguagem”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Definir contrato de skill de linguagem” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact mostra detector, regras, exemplos, capabilities e finding output satisfeitos pelos casos de `tests/fixtures/polyglot/skills`, com o defective case de Rust registrado como reprovado.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-SKL-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]

## Public contract

- Apenas “Definir contrato de skill de linguagem” e `skills/builtin/languages`, `tests/fixtures/polyglot/skills`, `tests/acceptance/AUR-074.sh`, `docs/specs/AUR-074.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-074.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-074/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: especificar em `skills/builtin/languages` o contrato comum de detector, regras, exemplos, capabilities e finding output, e materializar em `tests/fixtures/polyglot/skills` os casos, incluindo o defective case de Rust que precisa reprovar.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“remover o defective case de Rust.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: policy/dado é provado pelo lint estrutural.
- Contract: `tests/contracts/AUR-074.go::ContractAUR074`.
- Integration: `tests/integration/AUR-074.go::IntegrationAUR074`.
- E2E: not-applicable: não entrega sozinho use case público.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-074`

Expected artifact: `.board/evidence/AUR-074/acceptance/AC-001.json` valida schema, prova “Definir contrato de skill de linguagem” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover o defective case de Rust.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-074/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / skill-runtime

- Change: incluir no manifest da skill uma capability e um path não declarados.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-074/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-074/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `skill-runtime`, `model-provider` -> “Definir contrato de skill de linguagem” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-074.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: todas as dez dimensões e cada hunk; prioridade em correção, SOLID, compatibilidade e testes.
- Reviewer B: todas as dez dimensões e cada hunk; prioridade em hostilidade, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 igual e selada; processo, sessão, conversation, cache e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas MUT em OCI limpo, restaurar/repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-074/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` requer evento humano autenticado. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
