---
id: AUR-418
version: 1
title: Verificar proposta de unit test Shell
status: backlog
office: O08-testqa
depends_on: [AUR-133, AUR-140, AUR-141, AUR-142, AUR-143, AUR-139, AUR-257, AUR-411]
requirements: [PR-TST-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/application/testverify/shell, tests/fixtures/testgen/shell/verification, tests/unit/AUR-418.go, tests/contracts/AUR-418.go, tests/integration/AUR-418.go, tests/e2e/AUR-418.sh, tests/acceptance/AUR-418.sh, docs/specs/AUR-418.md]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, toolchain, container-engine]
---

## Outcome

Uma TestProposalV1 de Shell só recebe verified após aplicação em worktree descartável, harness Shell allowlisted, repetição anti-flake, delta de cobertura normalizado e prova de que reverter a mudança funcional torna o teste vermelho pelo motivo esperado.

## Non-goals

- Verificar script que exija sudo, systemd, rede ou binário fora do allowlist; `internal/application/testverify/shell` reprova a proposta que invoque comando não pinado no profile.
- Instalar package, consultar registry, usar cache mutável ou executar fora do profile polyglot-toolchain-v1.

## Preconditions

- A proposta de AUR-257, o aplicador seguro e os gates de teste estão ligados à mesma CandidateIdentityV1.
- Toolchain, package cache, comandos e fixtures estão pinados; o worktree é descartável e sem segredo.

## Postconditions

- O TestVerificationV1 registra digests de proposta, worktree e interpretador Shell, os comandos allowlisted do harness, os resultados green das repetições anti-flake, o delta de cobertura normalizado e a prova revert-red.
- Comando fora da allowlist, interpretador ou engine indisponível, timeout, suíte vazia, caso skip, divergência entre repetições ou teste que permanece verde após o revert produz incomplete ou fail com código próprio, nunca verified.

## Acceptance scenarios

### AC-001: Verificar proposta de unit test Shell

- Given: três TestProposalV1 de Shell em `tests/fixtures/testgen/shell/verification`: uma nominal, uma vacuous e uma que continua verde com a mudança funcional revertida, todas restritas ao harness allowlisted.
- When: o verifier aplica cada proposta em clone descartável e executa harness Shell allowlisted no profile pinado.
- Then: o registro marca `verified` só para a nominal, com o harness Shell em exit 0 nas repetições e revert-red no caso esperado; as demais saem 1, a insensível com `AUR-418/AC-001/revert-insensitive`.
- Covers requirements: [PR-TST-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Adiciona somente TestVerificationV1 para Shell; a observação do toolchain não é aprovação e não modifica a proposta original.

## TDD proof

- Test: `tests/acceptance/AUR-418.sh::AC-001`.
- Red: o verifier alcança a fixture nominal, mas não distingue proposta vacuous/revert-insensitive e falha com `AUR-418/AC-001/non-vacuity-missing`; bootstrap ou compile do harness não conta.
- Green: aplicar o patch Shell em worktree descartável, executar o harness allowlisted sem nenhum comando fora da lista, normalizar o coverage delta por linha e só então gravar `verified`.
- Refactor: preservar digests, códigos, ordem de fases, número de repetições e destruição do worktree em todos os exits.
- Unit: `tests/unit/AUR-418.go::TestAUR418`.
- Contract: `tests/contracts/AUR-418.go::ContractAUR418`.
- Integration: `tests/integration/AUR-418.go::IntegrationAUR418`.
- E2E: `tests/e2e/AUR-418.sh::E2EAUR418`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-418`

Expected artifact: `.board/evidence/AUR-418/acceptance/AC-001.json` contém proposal/worktree/toolchain digests, resultados green/repeat/coverage/revert, cleanup e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar teste que continua verde depois de reverter a mudança funcional.
- Expected: o mesmo `accept` falha com `AUR-418/AC-001/MUT-001/revert-insensitive`; restore passa.

### MUT-002: AC-001 / toolchain

- Change: trocar um tool digest ou habilitar package install/network fallback.
- Expected: o mesmo `accept` falha com `AUR-418/AC-001/MUT-002/toolchain-drift` antes de executar o teste.

### MUT-003: AC-001 / container-engine

- Change: executar como root, com checkout host writable, socket, device, capability ou egress.
- Expected: o mesmo `accept` falha com `AUR-418/AC-001/MUT-003/unsafe-worker`; nenhum byte do repo host muda.

## Security and privacy

- Proposta, source e output do toolchain são dados não confiáveis; comandos vêm da allowlist do profile, não da resposta do modelo.
- Worktree é descartável, mounts do host são zero, rootfs é read-only e escrita existe apenas em tmpfs bounded.
- Credentials, env, prompts/respostas brutos e canários não entram em stdout, logs, cache ou evidência.

## Documentation

- `docs/specs/AUR-418.md` fixa toolchain, comandos, matriz green/repeat/coverage/revert, estados inconclusivos e diagnóstico.

## Compatibility, migration, rollback

- TestProposalV1 permanece inalterada; versões incompatíveis de TestVerification criam novo schema.
- Nenhum teste é publicado automaticamente; consumidor decide aplicar artefato verificado por outro card.
- Rollback remove apenas verifier e fixtures shell, preservando propostas e source.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
