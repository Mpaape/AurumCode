---
id: AUR-109
version: 1
title: Executar analyzer job sem escrita
status: backlog
validation: tested
profile_owner: AUR-410
office: O06-scm
depends_on: [AUR-107, AUR-108, AUR-410]
requirements: [PR-ARCH-001, PR-SCM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001, CR-GH-002, CR-GH-003]
paths: [deployments/github-action/analyze, tests/e2e/action, tests/acceptance/AUR-109.sh, docs/specs/AUR-109.md, tests/specs/AUR-109, tests/contracts/AUR-109.go, tests/integration/AUR-109.go, tests/e2e/AUR-109.sh]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, scm, secret-channel]
---

## Outcome

fork hostil produz bundle sem token de escrita e usa workspace consumidor.

## Non-goals

- Não comentar, aprovar nem escrever no pull request de origem, mesmo quando o fork é confiável; toda escrita permanece no job publisher.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-107, AUR-108 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- fork hostil produz bundle sem token de escrita e usa workspace consumidor.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Executar analyzer job sem escrita

- Given: `tests/specs/AUR-109/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Executar analyzer job sem escrita”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Executar analyzer job sem escrita” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` com o fork hostil, o job termina no upload do bundle assinado e o artifact registra `permissions: contents: read`, nenhum segredo de escrita no ambiente e o workspace do repositório consumidor.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-SCM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001, CR-GH-002, CR-GH-003]

## Public contract

- Apenas “Executar analyzer job sem escrita” e `deployments/github-action/analyze`, `tests/e2e/action`, `tests/acceptance/AUR-109.sh`, `docs/specs/AUR-109.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-109.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-109/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: escrever `deployments/github-action/analyze` com `permissions: contents: read`, sem segredo de escrita no ambiente do job, operando sobre o workspace do repositório consumidor e terminando no upload do bundle assinado.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“injetar GITHUB_TOKEN write no analyzer.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: policy/dado é provado pelo lint estrutural.
- Contract: `tests/contracts/AUR-109.go::ContractAUR109`.
- Integration: `tests/integration/AUR-109.go::IntegrationAUR109`.
- E2E: `tests/e2e/AUR-109.sh::E2EAUR109`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-109`

Expected artifact: `.board/evidence/AUR-109/acceptance/AC-001.json` valida schema, prova “Executar analyzer job sem escrita” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: injetar GITHUB_TOKEN write no analyzer.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-109/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / scm

- Change: mudar o head remoto depois da leitura ou fornecer identidade de repositório divergente.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-109/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / secret-channel

- Change: injetar o canário AURUM_SECRET_CANARY na falha devolvida pela fronteira.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-109/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `scm`, `secret-channel` -> “Executar analyzer job sem escrita” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-109.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
