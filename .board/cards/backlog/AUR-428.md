---
id: AUR-428
version: 1
title: Respeitar teto de custo e falhar fechado ao estourar
status: backlog
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-424]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [tests/acceptance/AUR-428.sh, tests/unit/AUR-428.go, tests/integration/AUR-428.go, tests/e2e/AUR-428.sh, docs/specs/AUR-428.md]
read_paths: [internal/llm/cost, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

`aurumcode review --budget-usd` reserva, confirma e reporta custo por execução, e recusa a chamada com exit code próprio quando a estimativa excede o teto, sem enviar o prompt.

## Non-goals

- Qualquer comportamento que não seja observável pela execução do comando declarado neste card.
- Reescrever pacote existente que já cumpre a função; este card integra o que existe em vez de recriar.

## Preconditions

- As dependências declaradas estão em `done` e seus artefatos existem no repositório.
- O aceite roda offline: o provider de modelo é uma fixture determinística, porque o sandbox nega rede.

## Postconditions

`aurumcode review --budget-usd` reserva, confirma e reporta custo por execução, e recusa a chamada com exit code próprio quando a estimativa excede o teto, sem enviar o prompt.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Respeitar teto de custo e falhar fechado ao estourar

- Given: a fixture declarada em `paths` e o provider determinístico offline.
- When: o comando `./aurumcode review --diff tests/fixtures/review/sample.patch --budget-usd 0.001` é executado dentro do profile pinado.
- Then: a saída contém exatamente o resultado prometido no Outcome e o exit code é o declarado.
- And: repetir a execução sobre a mesma entrada produz a mesma saída.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `./aurumcode review --diff tests/fixtures/review/sample.patch --budget-usd 0.001` e o formato da saída que ele produz. Nenhum outro contrato do repositório muda.

## TDD proof

- Test: `tests/acceptance/AUR-428.sh::AC-001`.
- Red: sem a implementação, o comando não existe ou não produz a saída prometida, e o aceite falha com `AUR-428/AC-001/behavior-missing`. Falha de compilação, imagem, loader ou rede não conta como RED.
- Green: implementar o comportamento reutilizando os pacotes já existentes listados em `read_paths`.
- Refactor: preservar saída, exit codes e digests.
- Unit: `tests/unit/AUR-428.go::TestAUR428`.
- Contract: not-applicable: nenhum port novo é publicado.
- Integration: `tests/integration/AUR-428.go::IntegrationAUR428`.
- E2E: `tests/e2e/AUR-428.sh::E2EAUR428`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-428`

Expected artifact: `.board/evidence/AUR-428/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saída.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: permitir a chamada acima do teto faz o aceite falhar porque o contador de chamadas fica diferente de zero.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-428/AC-001/MUT-001`, e a restauração reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositório -> comando declarado -> saída sanitizada.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, prompt, cache, artefato ou evidência.
- Least privilege: somente `paths` é gravável; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-428.md` documenta o comando, suas flags, os exit codes, um exemplo executável sem segredo e o diagnóstico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos já publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-428/`: comando executado, exit code bruto, digest da saída, resultado da mutação e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.
