---
id: AUR-079
version: 1
title: Normalizar scanner de segredos
status: backlog
office: O05-review
depends_on: [AUR-078, AUR-009, AUR-410]
requirements: [PR-ARCH-001, PR-REV-003, PR-SEC-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005, CR-TRUST-004]
paths: [internal/analyzers/secrets, tests/acceptance/AUR-079.sh, docs/specs/AUR-079.md, tests/specs/AUR-079, tests/contracts/AUR-079.go, tests/integration/AUR-079.go, tests/e2e/AUR-079.sh, tests/unit/AUR-079.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

segredos em add, context e delete viram finding redigido com allowlist explícita.

## Non-goals

- não revoga, rotaciona nem notifica o proprietário do segredo encontrado e não varre o histórico completo do repositório; o escopo é o ChangeSet em análise.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-078, AUR-009 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- segredos em add, context e delete viram finding redigido com allowlist explícita.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Normalizar scanner de segredos

- Given: `tests/specs/AUR-079/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Normalizar scanner de segredos”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Normalizar scanner de segredos” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact traz os findings das linhas add, context e delete com valor redigido e motivo (padrão ou entropia); toda supressão cita a entrada de allowlist usada e nenhum valor em claro consta no artifact.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-003, PR-SEC-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005, CR-TRUST-004]

## Public contract

- Apenas “Normalizar scanner de segredos” e `internal/analyzers/secrets`, `tests/acceptance/AUR-079.sh`, `docs/specs/AUR-079.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-079.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-079/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: implementar em `internal/analyzers/secrets` a varredura das linhas add, context e delete por padrão e entropia, emitindo finding com o valor redigido e consultando a allowlist explícita antes de suprimir qualquer ocorrência.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“incluir valor do segredo no finding.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-079.go::TestAUR079`.
- Contract: `tests/contracts/AUR-079.go::ContractAUR079`.
- Integration: `tests/integration/AUR-079.go::IntegrationAUR079`.
- E2E: `tests/e2e/AUR-079.sh::E2EAUR079`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-079`

Expected artifact: `.board/evidence/AUR-079/acceptance/AC-001.json` valida schema, prova “Normalizar scanner de segredos” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: incluir valor do segredo no finding.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-079/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-079/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-079/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Normalizar scanner de segredos” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-079.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: todas as dez dimensões e cada hunk; prioridade em correção, SOLID, compatibilidade e testes.
- Reviewer B: todas as dez dimensões e cada hunk; prioridade em hostilidade, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 igual e selada; processo, sessão, conversation, cache e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas MUT em OCI limpo, restaurar/repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-079/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` requer evento humano autenticado. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
