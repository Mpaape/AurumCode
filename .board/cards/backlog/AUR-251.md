---
id: AUR-251
version: 1
title: Executar testes PowerShell isolados
status: backlog
validation: tested
profile_owner: AUR-411
office: O08-testqa
depends_on: [AUR-130, AUR-133, AUR-233, AUR-411]
requirements: [PR-TST-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/testgen/runners/powershell, tests/containers/runners/powershell, tests/acceptance/AUR-251.sh, docs/specs/AUR-251.md, tests/unit/AUR-251.go, tests/contracts/AUR-251.go, tests/integration/AUR-251.go, tests/e2e/AUR-251.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

uma proposta PowerShell roda em worker pinado sem rede/host e retorna resultado normalizado.

## Non-goals

- Gerar as propostas de teste PowerShell ou executar runners de outras linguagens: este card apenas executa a proposta recebida e normaliza o resultado.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-130, AUR-133, AUR-233 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- uma proposta PowerShell roda em worker pinado sem rede/host e retorna resultado normalizado.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Executar testes PowerShell isolados

- Given: a fixture traz uma proposta de teste PowerShell já gerada sob os manifests de AUR-130 e AUR-133, e o worker de `tests/containers/runners/powershell` está pinado por digest, sem rede e sem mount do perfil do usuário do host.
- When: `internal/testgen/runners/powershell` executa a proposta de teste da fixture dentro do worker pinado de `tests/containers/runners/powershell`.
- Then: o resultado normalizado traz status por caso, duração e stdout truncado no limite declarado, e nem o perfil do usuário do host nem qualquer interface de rede ficam alcançáveis de dentro do worker.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-TST-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Executar testes PowerShell isolados” nos artefatos `internal/testgen/runners/powershell`, `tests/containers/runners/powershell`, `tests/acceptance/AUR-251.sh`, `docs/specs/AUR-251.md`.

## TDD proof

- Test: `tests/acceptance/AUR-251.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-251/AC-001: Executar testes PowerShell isolados ausente`; build/loader/infra não valem.
- Green: escrever em `internal/testgen/runners/powershell` a invocação do worker pinado sem rede nem host mounts e a normalização da saída do PowerShell em resultado por caso.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-251.go::TestAUR251`.
- Contract: `tests/contracts/AUR-251.go::ContractAUR251`.
- Integration: `tests/integration/AUR-251.go::IntegrationAUR251`.
- E2E: `tests/e2e/AUR-251.sh::E2EAUR251`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-251`

Expected artifact: `.board/evidence/AUR-251/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: montar perfil do usuário ou permitir network no runner.
- Expected: o acceptance inalterado falha com `AUR-251/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-251/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Executar testes PowerShell isolados” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-251.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
