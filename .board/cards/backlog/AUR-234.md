---
id: AUR-234
version: 1
title: Validar TLS e destino HTTP
status: backlog
validation: tested
profile_owner: AUR-405
office: O03-providers
depends_on: [AUR-057, AUR-233, AUR-405]
requirements: [PR-LLM-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]
paths: [internal/httpx/destination.go, internal/httpx/destination_test.go, tests/acceptance/AUR-234.sh, docs/specs/AUR-234.md, tests/unit/AUR-234.go, tests/contracts/AUR-234.go, tests/integration/AUR-234.go, tests/e2e/AUR-234.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/fake-provider-v1.json, .board/locks/oci/fake-provider-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, network-endpoint]
---

## Outcome

o transporte permite somente HTTPS e destinos resolvidos pela allowlist confiável, bloqueando redirect, userinfo, loopback e endereço privado não autorizado.

## Non-goals

- Aplicar deadline, cancelamento ou política de retry sobre uma requisição já admitida: o ciclo de vida da conexão pertence a outro card do transporte.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-057, AUR-233 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- o transporte permite somente HTTPS e destinos resolvidos pela allowlist confiável, bloqueando redirect, userinfo, loopback e endereço privado não autorizado.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Validar TLS e destino HTTP

- Given: os manifests aprovados de AUR-057 e AUR-233 fixam o cliente HTTP e o lockset de bootstrap na mesma base, e o fake provider serve destino http, https allowlisted, redirect para loopback e URL com userinfo.
- When: `internal/httpx/destination.go` valida cada URL da fixture e, para as aceitas, resolve o host antes de abrir a conexão TLS contra o fake provider.
- Then: só o destino https cujo IP resolvido consta na allowlist confiável abre conexão; esquema http, userinfo na URL, redirect para 127.0.0.1 e endereço privado não autorizado retornam erro de destino sem socket aberto.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-LLM-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]

## Public contract

- Introduz somente “Validar TLS e destino HTTP” nos artefatos `internal/httpx/destination.go`, `internal/httpx/destination_test.go`, `tests/acceptance/AUR-234.sh`, `docs/specs/AUR-234.md`.

## TDD proof

- Test: `tests/acceptance/AUR-234.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-234/AC-001: Validar TLS e destino HTTP ausente`; build/loader/infra não valem.
- Green: escrever em `internal/httpx/destination.go` a exigência de esquema https, a rejeição de userinfo, a checagem do IP resolvido contra a allowlist e a revalidação do destino a cada redirect.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-234.go::TestAUR234`.
- Contract: `tests/contracts/AUR-234.go::ContractAUR234`.
- Integration: `tests/integration/AUR-234.go::IntegrationAUR234`.
- E2E: `tests/e2e/AUR-234.sh::E2EAUR234`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-234`

Expected artifact: `.board/evidence/AUR-234/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: redirecionar um host allowlisted para 127.0.0.1.
- Expected: o acceptance inalterado falha com `AUR-234/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / network-endpoint

- Change: redirecionar o destino para endpoint privado não autorizado.
- Expected: o acceptance inalterado falha com `AUR-234/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `network-endpoint` -> contrato “Validar TLS e destino HTTP” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-234.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
