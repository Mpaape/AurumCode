---
id: AUR-282
version: 1
title: Documentar contratos SCM
status: backlog
office: O12-release
depends_on: [AUR-112, AUR-196, AUR-408]
requirements: [PR-SCM-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [docs/contracts/scm, examples/contracts/scm, tests/acceptance/AUR-282.sh, docs/specs/AUR-282.md, tests/unit/AUR-282.go, tests/contracts/AUR-282.go, tests/integration/AUR-282.go, tests/e2e/AUR-282.sh]
read_paths: [go.mod, go.sum, internal, docs, .board/oci/profiles/registry.v1.json, .board/oci/profiles/docs-tool-offline-v1.json, .board/locks/oci/docs-tool-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

ChangeSource e Publisher possuem exemplos separados de least privilege para local, GitHub, Gitea e GitLab.

## Non-goals

- Implementar ou alterar adapters SCM reais e cobrir forjas fora de local, GitHub, Gitea e GitLab, como Bitbucket.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-112, AUR-196 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- ChangeSource e Publisher possuem exemplos separados de least privilege para local, GitHub, Gitea e GitLab.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Documentar contratos SCM

- Given: `docs/contracts/scm` e `examples/contracts/scm` cobrem ChangeSource e Publisher para local, GitHub, Gitea e GitLab, com o profile `docs-tool-offline-v1` ativo.
- When: a contract suite de SCM valida uma vez cada par ChangeSource/Publisher declarado em `examples/contracts/scm`, sem acesso de rede.
- Then: os quatro pares saem aprovados e `.board/evidence/AUR-282/acceptance/AC-001.json` mostra, por exemplo, o scope de credencial do analyzer disjunto do scope do publisher.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-SCM-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Documentar contratos SCM” em `docs/contracts/scm`, `examples/contracts/scm`, `tests/acceptance/AUR-282.sh`, `docs/specs/AUR-282.md`.

## TDD proof

- Test: `tests/acceptance/AUR-282.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-282/AC-001: Documentar contratos SCM ausente`; build/loader/infra não valem.
- Green: descrever ChangeSource e Publisher em `docs/contracts/scm` e criar quatro exemplos em `examples/contracts/scm`, cada um com token de leitura do analyzer separado do token de escrita do publisher.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-282.go::TestAUR282`.
- Contract: `tests/contracts/AUR-282.go::ContractAUR282`.
- Integration: `tests/integration/AUR-282.go::IntegrationAUR282`.
- E2E: `tests/e2e/AUR-282.sh::E2EAUR282`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-282`

Expected artifact: `.board/evidence/AUR-282/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: usar a mesma credencial no exemplo analyzer/publisher.
- Expected: o acceptance falha com `AUR-282/AC-001/MUT-001`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository` -> “Documentar contratos SCM” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-282.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
