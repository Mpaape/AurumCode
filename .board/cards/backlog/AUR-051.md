---
id: AUR-051
version: 1
title: Indexar e consultar FTS5
status: backlog
office: O02-index
depends_on: [AUR-043, AUR-050, AUR-407]
requirements: [PR-ARCH-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-006]
paths: [internal/index/search, tests/acceptance/AUR-051.sh, docs/specs/AUR-051.md, tests/specs/AUR-051, tests/integration/AUR-051.go, tests/unit/AUR-051.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, database, git-metadata]
---

## Outcome

identifiers, paths e docs aceitam busca Unicode parametrizada e ranking determinístico.

## Non-goals

- Não faz busca vetorial nem ranking semântico por embeddings: `internal/index/search` entrega apenas correspondência lexical FTS5 sobre o snapshot corrente.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-043, AUR-050 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- identifiers, paths e docs aceitam busca Unicode parametrizada e ranking determinístico.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Indexar e consultar FTS5

- Given: `tests/specs/AUR-051/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Indexar e consultar FTS5”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Indexar e consultar FTS5” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que identifiers, paths e docs aceitam busca Unicode parametrizada e ranking determinístico.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-006]

## Public contract

- Apenas “Indexar e consultar FTS5” e `internal/index/search`, `tests/acceptance/AUR-051.sh`, `docs/specs/AUR-051.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-051.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-051/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/index/search` populando a tabela FTS5 com identifiers, paths e docs normalizados em Unicode e consultando por bind parameters, com desempate estável por rank e chave da entidade.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“concatenar query diretamente no SQL.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-051.go::TestAUR051`.
- Contract: not-applicable: nenhum port ou formato externo é criado.
- Integration: `tests/integration/AUR-051.go::IntegrationAUR051`.
- E2E: not-applicable: não entrega sozinho use case público.

## Acceptance

container_profile: `sqlite-offline-v1`

accept: `./.board/bin/oci-run --profile sqlite-offline-v1 --card AUR-051`

Expected artifact: `.board/evidence/AUR-051/acceptance/AC-001.json` valida schema, prova “Indexar e consultar FTS5” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: concatenar query diretamente no SQL.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-051/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / database

- Change: corromper o cabeçalho ou exceder o budget do SQLite durante a operação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-051/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / git-metadata

- Change: resolver o alvo para a raiz do worktree em vez do Git dir autorizado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-051/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `database`, `git-metadata` -> “Indexar e consultar FTS5” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-051.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: todas as dez dimensões e cada hunk; prioridade em correção, SOLID, compatibilidade e testes.
- Reviewer B: todas as dez dimensões e cada hunk; prioridade em hostilidade, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 igual e selada; processo, sessão, conversation, cache e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas MUT em OCI limpo, restaurar/repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-051/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` requer evento humano autenticado. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
