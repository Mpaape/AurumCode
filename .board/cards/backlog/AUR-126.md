---
id: AUR-126
version: 1
title: Isolar compatibilidade temporária Jekyll v2
status: backlog
office: O07-docs
depends_on: [AUR-124, AUR-033, AUR-408]
requirements: [PR-ARCH-001, PR-DOC-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-COV-003]
paths: [internal/docs/site/jekyll, tests/acceptance/AUR-126.sh, docs/specs/AUR-126.md, tests/fixtures/AUR-126, tests/unit/AUR-126.go, tests/contracts/AUR-126.go, tests/integration/AUR-126.go, tests/e2e/AUR-126.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository, filesystem, documentation-tool]
---

## Outcome

`AUR126ResultV1` é o único resultado reversível de “Isolar compatibilidade temporária Jekyll v2” e limita a implementação a `internal/docs/site/jekyll`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “tornar Jekyll dependência do domínio”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-124, AUR-033]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-126` contém exatamente um caso nominal e um contracaso mutado, sob 32 arquivos, 2 MiB total, 30 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR126ResultV1` canônico e o contracaso produz `AUR126_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Isolar compatibilidade temporária Jekyll v2

- Given: CandidateIdentityV1, digests de [AUR-124, AUR-033] e a fixture bounded `tests/fixtures/AUR-126` com caso nominal e “tornar Jekyll dependência do domínio”.
- When: o consumidor invoca “Isolar compatibilidade temporária Jekyll v2” uma vez no port público card-scoped, com limite de 32 arquivos, 2 MiB total, 30 s e 2 CPUs.
- Then: fixture legado gera warning e site sem contaminar core ou novo output; o consumidor recebe `AUR126ResultV1` com status e digest canônicos. O contracaso “tornar Jekyll dependência do domínio” retorna `AUR126_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-DOC-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-COV-003]

## Public contract

- `AUR126ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Isolar compatibilidade temporária Jekyll v2”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-126.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-126/AC-001: AUR126_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR126ResultV1` dentro de 32 arquivos, 2 MiB total, 30 s e 2 CPUs, enquanto “tornar Jekyll dependência do domínio” retorna `AUR126_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-126.go::TestAUR126NominalAndMutated`.
- Contract: `tests/contracts/AUR-126.go::TestAUR126ResultV1`.
- Integration: `tests/integration/AUR-126.go::TestAUR126Boundary`.
- E2E: `tests/e2e/AUR-126.sh::AC-001`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-126`

Expected artifact: o coordenador valida `.board/evidence/AUR-126/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: tornar Jekyll dependência do domínio.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-126/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / filesystem

- Change: trocar o destino por symlink que escape do staging.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-126/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / documentation-tool

- Change: fazer a ferramenta escrever fora do staging ou gerar bytes diferentes no replay.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-126/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR126ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-126.md` fixa input, output, `AUR126_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR126ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer A: cobre cada hunk e as dez dimensões; prioriza contrato, SOLID, compatibilidade, rollback e sensibilidade dos testes.
- Reviewer B: cobre cada hunk e as dez dimensões; prioriza input hostil, segredos, permissões, supply chain, limites e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 em processo, sessão, conversation, cache e memória isolados; o manifest registra I0-I3 e backend family sem inflar independência.
- Skeptical approver: sela hipóteses antes dos reviews, executa cada MUT em OCI limpo, restaura e repete; sobrevivência, erro de teste ou inconclusivo veta.

## Evidence

Somente o coordenador escreve `.board/evidence/AUR-126/`; builder e testes emitem observações não confiáveis. O bundle contém identity/spec digests, RED/GREEN/refactor, gates, mapas completos A/B, challenge pré-selado, mutações, restore/replay e verdicts sanitizados. `done` requer evento humano autenticado; segredo, env, prompt/resposta brutos e chain-of-thought são proibidos.
