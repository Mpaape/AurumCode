---
id: AUR-230
version: 1
title: Limitar privilégios e recursos do worker
status: backlog
office: O08-testqa
depends_on: [AUR-227, AUR-410]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/sandbox/profile/resources.go, tests/containers/sandbox/resources, tests/acceptance/AUR-230.sh, docs/specs/AUR-230.md, tests/unit/AUR-230.go, tests/contracts/AUR-230.go, tests/integration/AUR-230.go, tests/e2e/AUR-230.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/oci-conformance-v1.json, .board/locks/oci/oci-conformance-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

o worker aplica cap-drop ALL, no-new-privileges, limites de CPU, memória, PIDs, tempo e output com falha tipada ao exceder.

## Non-goals

- Escolher o engine, negar rede e host mounts ou fixar o UID não-root: este card apenas aplica privilégio mínimo e tetos de recurso ao worker.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-227 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- o worker aplica cap-drop ALL, no-new-privileges, limites de CPU, memória, PIDs, tempo e output com falha tipada ao exceder.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Limitar privilégios e recursos do worker

- Given: o manifest aprovado de AUR-227 fixa o selector de engine na mesma base, e `tests/containers/sandbox/resources` traz fixtures que estouram CPU, memória, PIDs, tempo de parede e volume de saída.
- When: o perfil de `internal/sandbox/profile/resources.go` executa cada fixture de estouro sob os limites declarados e o worker é observado até terminar.
- Then: o argv carrega cap-drop total e no-new-privileges, e cada estouro termina com erro tipado nomeando o limite excedido de CPU, memória, PIDs, tempo ou output, em vez de saída truncada em silêncio.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Limitar privilégios e recursos do worker” nos artefatos `internal/sandbox/profile/resources.go`, `tests/containers/sandbox/resources`, `tests/acceptance/AUR-230.sh`, `docs/specs/AUR-230.md`.

## TDD proof

- Test: `tests/acceptance/AUR-230.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-230/AC-001: Limitar privilégios e recursos do worker ausente`; build/loader/infra não valem.
- Green: preencher `internal/sandbox/profile/resources.go` com cap-drop total, no-new-privileges, os limites de CPU, memória, PIDs, tempo e output, e o mapeamento de cada estouro para seu erro tipado.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-230.go::TestAUR230`.
- Contract: `tests/contracts/AUR-230.go::ContractAUR230`.
- Integration: `tests/integration/AUR-230.go::IntegrationAUR230`.
- E2E: `tests/e2e/AUR-230.sh::E2EAUR230`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-230`

Expected artifact: `.board/evidence/AUR-230/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover cap-drop ou um resource limit obrigatório.
- Expected: o acceptance inalterado falha com `AUR-230/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-230/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Limitar privilégios e recursos do worker” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-230.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
