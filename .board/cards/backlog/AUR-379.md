---
id: AUR-379
version: 1
title: Qualificar claims do quickstart LiteLLM
status: backlog
validation: tested
profile_owner: AUR-408
office: O14-legacy
depends_on: [AUR-001, AUR-195, AUR-408]
requirements: [PR-DEMO-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [LITELLM_QUICKSTART.md, tests/migration/legacy/public-docs/AUR-379, tests/acceptance/AUR-379.sh, docs/specs/AUR-379.md]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, secret-channel]
---

## Outcome

cada claim executável em `LITELLM_QUICKSTART.md` possui claim_id, range, requirement/evidence ou disposition `qualify|remove|keep`, preservando regiões manuais não relacionadas.

## Non-goals

- Contactar o proxy LiteLLM ou validar as chaves e endpoints citados em `LITELLM_QUICKSTART.md`; a extração é lexical e mantém os valores redigidos fora de `claims.tsv`.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-001, AUR-195]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/migration/legacy/public-docs/AUR-379/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- cada claim executável em `LITELLM_QUICKSTART.md` possui claim_id, range, requirement/evidence ou disposition `qualify|remove|keep`, preservando regiões manuais não relacionadas.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Qualificar claims do quickstart LiteLLM

- Given: `tests/migration/legacy/public-docs/AUR-379/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 256 claims, 2 MiB e 10 s.
- When: o extrator lexical percorre `LITELLM_QUICKSTART.md` por ranges, sem executar fenced blocks, links ou comandos, e valida cada disposition.
- Then: o nominal sai 0 com `pass`, toda claim executável de `LITELLM_QUICKSTART.md` resolve para requirement/evidence ou `qualify|remove|keep`, e nenhum endpoint ou chave literal entra em `claims.tsv`; claim sem disposition, symlink/tamper, campo inválido e over-limit saem 1 com `claim_without_disposition`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded`.
- Covers requirements: [PR-DEMO-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `claim_without_disposition|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-379.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-379/AC-001/claim_without_disposition`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir a matriz completa de claims de `LITELLM_QUICKSTART.md`, qualificando cada claim sem alterar regiões manuais.
- Refactor: após GREEN, normalizar claim IDs preservando ranges, dispositions, regiões manuais e digest ordenado; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-379.sh::ContractAUR379`.
- Integration: `tests/acceptance/AUR-379.sh::IntegrationAUR379`.
- E2E: `tests/acceptance/AUR-379.sh::E2EAUR379`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-379`

Expected artifact: o coordinator deriva `.board/evidence/AUR-379/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: marcar como suportada uma claim de `LITELLM_QUICKSTART.md` sem requirement nem evidence.
- Expected: o aceite sai 1 com `AUR-379/AC-001/unsupported-claim` e identifica somente path/range/claim_id; restore recupera a disposição.

### MUT-002: AC-001 / secret-channel

- Change: copiar para claims.tsv o valor do canário presente na fixture redigida.
- Expected: o sink scan sai 1 com `AUR-379/AC-001/secret-in-claims` sem ecoar o valor; restore mantém apenas a classe.

## Security and privacy

- Data flow: providers, base URL, chaves e compatibilidade OpenAI-format -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-379.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: somente claims com disposition aprovada podem ser qualificadas ou removidas; texto manual fora dos ranges permanece intacto.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
