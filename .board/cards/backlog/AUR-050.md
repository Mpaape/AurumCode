---
id: AUR-050
version: 1
title: Derivar relações entre entidades
status: backlog
office: O02-index
depends_on: [AUR-047, AUR-049, AUR-403]
requirements: [PR-ARCH-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-006]
paths: [internal/index/graph, tests/acceptance/AUR-050.sh, docs/specs/AUR-050.md, tests/specs/AUR-050, tests/contracts/AUR-050.go, tests/integration/AUR-050.go, tests/unit/AUR-050.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, database, git-metadata]
---

## Outcome

imports, references, test-of e doc-of guardam confiança e proveniência sem dangling edges.

## Non-goals

- Não expõe consulta ou travessia do grafo para consumidores: `internal/index/graph` só deriva e grava arestas, e a leitura pertence ao card de ContextPack.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-047, AUR-049 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- imports, references, test-of e doc-of guardam confiança e proveniência sem dangling edges.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Derivar relações entre entidades

- Given: `tests/specs/AUR-050/cases.yaml` traz um snapshot com um import resolvido, um par test-of, um doc-of e uma referência para símbolo que não existe no snapshot, cada aresta com confiança e proveniência esperadas.
- When: `tests/acceptance/AUR-050.sh::AC-001` roda o derivador de `internal/index/graph` sobre o snapshot selado e coleta as arestas gravadas, seus campos de confiança e proveniência e os erros de gravação.
- Then: as três arestas resolvíveis são gravadas com confiança e proveniência preenchidas; a referência ao símbolo ausente é recusada com `AUR050_DANGLING_EDGE` e a tabela `edges` fica sem origem ou destino faltante.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-006]

## Public contract

- Somente o contrato “Derivar relações entre entidades” e os artefatos em `internal/index/graph`, `tests/acceptance/AUR-050.sh`, `docs/specs/AUR-050.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-050.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-050/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/index/graph` derivando arestas import, reference, test-of e doc-of com confiança e proveniência por aresta, recusando a gravação quando origem ou destino não existe no snapshot.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“persistir edge para entidade inexistente.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-050.go::TestAUR050`.
- Contract: `tests/contracts/AUR-050.go::ContractAUR050`.
- Integration: `tests/integration/AUR-050.go::IntegrationAUR050`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-050`

Expected artifact: `.board/evidence/AUR-050/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Derivar relações entre entidades” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: persistir edge para entidade inexistente.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-050/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / database

- Change: corromper o cabeçalho ou exceder o budget do SQLite durante a operação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-050/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-003: AC-001 / git-metadata

- Change: resolver o alvo para a raiz do worktree em vez do Git dir autorizado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-050/AC-001/MUT-003`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `database`, `git-metadata` -> contrato “Derivar relações entre entidades” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-050.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-050/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige ainda evento humano autenticado para a identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

