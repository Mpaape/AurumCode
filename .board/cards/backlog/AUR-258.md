---
id: AUR-258
version: 1
title: Gerar proposta de unit test PowerShell
status: backlog
office: O08-testqa
depends_on: [AUR-131, AUR-132, AUR-405]
requirements: [PR-TST-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/application/testgen/powershell, tests/acceptance/AUR-258.sh, docs/specs/AUR-258.md, tests/unit/AUR-258.go, tests/contracts/AUR-258.go, tests/integration/AUR-258.go, tests/e2e/AUR-258.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/fake-provider-v1.json, .board/locks/oci/fake-provider-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, model-provider]
---

## Outcome

Uma mudança PowerShell elegível produz uma proposta unitária estruturada, bounded e vinculada ao change digest; geração não executa o teste nem afirma cobertura, não vacuidade, ausência de flake ou aprovação.

## Non-goals

- Carregar o módulo PowerShell, executar `Invoke-Pester` ou afirmar ausência de flake no teste proposto.
- Tratar saída do modelo como código confiável, evidência de teste ou autorização de escrita.

## Preconditions

- AUR-131, AUR-132 e o profile fake-provider-v1 estão aprovados para a mesma CandidateIdentityV1.
- Contexto, schema de proposta e fake responses estão pinados e não contêm credenciais.

## Postconditions

- A proposta PowerShell contém arquivos, patch, comandos sugeridos e rationale bounded, todos ligados ao change digest.
- O resultado é apenas candidato não confiável; nenhum arquivo do consumidor é aplicado ou executado.

## Acceptance scenarios

### AC-001: Gerar proposta de unit test PowerShell

- Given: contexto PowerShell pinado (módulo `.psm1` alvo e harness Pester declarado), change digest e duas respostas do fake-provider-v1, uma nominal e outra com rationale acima do limite de caracteres.
- When: o gerador powershell solicita e valida exatamente uma TestProposalV1.
- Then: `.board/evidence/AUR-258/acceptance/AC-001.json` guarda a TestProposalV1 PowerShell com arquivos `*.Tests.ps1`; o rationale excedente devolve erro tipado e a proposta inteira é descartada.
- Covers requirements: [PR-TST-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Adiciona somente o gerador powershell de TestProposalV1; execução e verdict pertencem ao card de verificação dependente.

## TDD proof

- Test: `tests/acceptance/AUR-258.sh::AC-001`.
- Red: com fake nominal alcançável, o gerador falha com `AUR-258/AC-001/proposal-generator-missing`; erro de provider bootstrap ou execução de toolchain não conta.
- Green: implementar em `internal/application/testgen/powershell` o gerador que produz arquivos `*.Tests.ps1`, limita o rationale ao bound declarado, fixa o comando em `Invoke-Pester` e liga a proposta ao change digest.
- Refactor: preservar proposal digest, ordenação, códigos de rejeição e zero execução ou escrita no consumer repo.
- Unit: `tests/unit/AUR-258.go::TestAUR258`.
- Contract: `tests/contracts/AUR-258.go::ContractAUR258`.
- Integration: `tests/integration/AUR-258.go::IntegrationAUR258`.
- E2E: `tests/e2e/AUR-258.sh::E2EAUR258`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-258`

Expected artifact: `.board/evidence/AUR-258/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: trocar o change digest da proposta mantendo o contexto original.
- Expected: o mesmo `accept` falha com `AUR-258/AC-001/MUT-001/stale-proposal`; restore passa e nenhum patch é aplicado.

### MUT-002: AC-001 / model-provider

- Change: o fake retorna path `../escape`, comando não allowlisted e payload acima do limite.
- Expected: o mesmo `accept` falha com `AUR-258/AC-001/MUT-002/invalid-proposal`; nenhum conteúdo hostil é executado ou persistido.

## Security and privacy

- Repository context e model output são dados não confiáveis; somente campos do TestProposalV1 bounded atravessam o adapter.
- API keys reais, env, prompts/respostas brutos e `AURUM_SECRET_CANARY` não entram em fixtures, logs ou evidência.
- O gerador não possui capability de aplicar patch, executar toolchain, publicar ou aprovar.

## Documentation

- `docs/specs/AUR-258.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
