---
id: AUR-096
version: 1
title: Aprovar review local agnóstico
status: backlog
office: O00-governance
depends_on: [AUR-094, AUR-095, AUR-291, AUR-292, AUR-293, AUR-410]
requirements: [PR-ARCH-001, PR-REV-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [tests/acceptance/gates/AUR-096, tests/acceptance/AUR-096.sh, docs/specs/AUR-096.md, tests/specs/AUR-096, tests/contracts/AUR-096.go, tests/integration/AUR-096.go, tests/e2e/AUR-096.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

corpus local produz findings válidos, determinísticos, sem duplicação nem vazamento.

## Non-goals

- Não ampliar o corpus com repositórios externos ou linguagens fora do conjunto local; cobertura multi-linguagem pertence a cards próprios.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-094, AUR-095, AUR-291, AUR-292, AUR-293 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- corpus local produz findings válidos, determinísticos, sem duplicação nem vazamento.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Aprovar review local agnóstico

- Given: `tests/specs/AUR-096/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Aprovar review local agnóstico”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Aprovar review local agnóstico” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o gate registra findings iguais por chave estável nas duas execuções do corpus local, zero duplicata e nenhum trecho de segredo na saída.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- Apenas “Aprovar review local agnóstico” e `tests/acceptance/gates/AUR-096`, `.board/evidence/AUR-096`, `tests/acceptance/AUR-096.sh`, `docs/specs/AUR-096.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-096.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-096/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: escrever o gate `tests/acceptance/gates/AUR-096` que roda o corpus local duas vezes, compara os conjuntos de findings por chave estável, reprova duplicata e falha quando qualquer trecho de segredo aparece na saída.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“ignorar um comentário candidato fora do diff.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: policy/dado é provado pelo lint estrutural.
- Contract: `tests/contracts/AUR-096.go::ContractAUR096`.
- Integration: `tests/integration/AUR-096.go::IntegrationAUR096`.
- E2E: `tests/e2e/AUR-096.sh::E2EAUR096`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-096`

Expected artifact: `.board/evidence/AUR-096/acceptance/AC-001.json` valida schema, prova “Aprovar review local agnóstico” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: ignorar um comentário candidato fora do diff.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-096/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-096/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-096/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Aprovar review local agnóstico” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-096.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
