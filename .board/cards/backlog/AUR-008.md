---
id: AUR-008
version: 1
title: Exigir dupla revisão independente
status: backlog
office: O00-governance
depends_on: [AUR-005, AUR-007]
requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [internal/approval/doublereview, .board/REVIEW_PROTOCOL.md, tests/acceptance/AUR-008.sh, docs/specs/AUR-008.md, tests/specs/AUR-008, tests/integration/AUR-008.go, tests/e2e/AUR-008.sh, tests/unit/AUR-008.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, authorization-source]
---

## Outcome

autor, Reviewer A, Reviewer B e skeptic obedecem isolamento e poder de veto.

## Non-goals

- Este card não avalia o mérito técnico dos achados de A e de B: impõe só o isolamento das identidades e o poder de veto; a rubrica de conteúdo da revisão vive nos standards.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-005, AUR-007 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- autor, Reviewer A, Reviewer B e skeptic obedecem isolamento e poder de veto.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Exigir dupla revisão independente

- Given: `tests/specs/AUR-008/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Exigir dupla revisão independente”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Exigir dupla revisão independente” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que autor, Reviewer A, Reviewer B e skeptic obedecem isolamento e poder de veto.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Exigir dupla revisão independente” e os artefatos em `internal/approval/doublereview`, `.board/REVIEW_PROTOCOL.md`, `tests/acceptance/AUR-008.sh`, `docs/specs/AUR-008.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-008.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-008/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/approval/doublereview` para exigir role nonces distintos de autor, Reviewer A, Reviewer B e skeptic, recusar aliases do mesmo backend como diversidade e propagar o veto de qualquer revisor sobre o verdict.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“reutilizar o mesmo role nonce nos dois reviewers.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-008.go::TestAUR008`.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: `tests/integration/AUR-008.go::IntegrationAUR008`.
- E2E: `tests/e2e/AUR-008.sh::E2EAUR008`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-008`

Expected artifact: `.board/evidence/AUR-008/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Exigir dupla revisão independente” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: reutilizar o mesmo role nonce nos dois reviewers.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-008/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / authorization-source

- Change: forjar um evento de aprovação com identidade de agente.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-008/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `authorization-source` -> contrato “Exigir dupla revisão independente” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-008.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-008/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige o bundle de evidência recomputável ligado à identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

