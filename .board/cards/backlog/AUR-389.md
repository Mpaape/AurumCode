---
id: AUR-389
version: 1
title: Caracterizar settings locais do Claude
status: backlog
office: O14-legacy
depends_on: [AUR-001, AUR-009, AUR-021, AUR-068, AUR-408]
requirements: [PR-ARCH-001, PR-CFG-002, PR-MCP-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [.claude/settings.local.json, tests/characterization/legacy/agent-settings/AUR-389, tests/acceptance/AUR-389.sh, docs/specs/AUR-389.md]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: restricted
trust_boundaries: [repository, mcp, secret-channel]
---

## Outcome

o manifesto contém uma linha para `.claude/settings.local.json` com dialect `claude-local-json`, key-path digest, identifiers, `secret_values=0` e `commands_executed=0`.

## Non-goals

- Interpretar a semântica do allowlist local, mesclá-lo com `.claude/settings.json` compartilhado ou decidir quais permissões seguem válidas; o manifesto registra apenas key paths, identifiers e counts.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-001, AUR-009, AUR-021, AUR-068]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/characterization/legacy/agent-settings/AUR-389/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- o manifesto contém uma linha para `.claude/settings.local.json` com dialect `claude-local-json`, key-path digest, identifiers, `secret_values=0` e `commands_executed=0`.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Caracterizar settings locais do Claude

- Given: a fixture `tests/characterization/legacy/agent-settings/AUR-389/cases.tsv` sela o digest de `.claude/settings.local.json`, as permission entries esperadas e o código de cada caso, com teto de 256 key paths, 1 MiB e 10 s.
- When: o parser non-executing abre literalmente `.claude/settings.local.json`, rejeita symlink/duplicate key/campo desconhecido e emite somente paths/classes/digests.
- Then: o nominal retorna `pass` e o manifesto contém uma linha para `.claude/settings.local.json` com dialect `claude-local-json`, key-path digest, identifiers, `secret_values=0` e `commands_executed=0`; ausência, tamper/symlink, campo inválido e over-limit retornam respectivamente `settings_parse_inconclusive`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded` antes de efeito externo.
- Covers requirements: [PR-ARCH-001, PR-CFG-002, PR-MCP-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `settings_parse_inconclusive|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-389.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-389/AC-001/settings_parse_inconclusive`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir uma linha sanitizada para `.claude/settings.local.json` com dialect/status/digests/counts e nenhum dispatch MCP.
- Refactor: após GREEN, normalizar key paths preservando dialect, identifiers, counts zero e manifest digest; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-389.sh::ContractAUR389`.
- Integration: `tests/acceptance/AUR-389.sh::IntegrationAUR389`.
- E2E: `tests/acceptance/AUR-389.sh::E2EAUR389`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-389`

Expected artifact: o coordinator deriva `.board/evidence/AUR-389/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: substituir `.claude/settings.local.json` por symlink que escape do checkout.
- Expected: o resolver sai 1 com `AUR-389/AC-001/source-escape` antes da leitura externa; restore recupera o source hash.

### MUT-002: AC-001 / mcp

- Change: executar o command sintético da fixture ou iniciar um MCP server durante parse.
- Expected: o process spy sai 1 com `AUR-389/AC-001/mcp-executed`; restore mantém `commands_executed=0`.

### MUT-003: AC-001 / secret-channel

- Change: persistir o valor do canário inline em vez de sua classe.
- Expected: o sink scan sai 1 com `AUR-389/AC-001/secret-value` sem imprimir o valor; restore mantém `secret_values=0`.

## Security and privacy

- Data flow: dialect `claude-local-json`, key paths, server/tool identifiers e classes de referência -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-389.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: nenhum setting local vira default; uma config de produto nova exige schema e autorização próprios.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer A: cada hunk e as dez dimensões do protocolo, priorizando correção, SOLID, compatibilidade, determinismo e sensibilidade dos testes.
- Reviewer B: cada hunk e as dez dimensões do protocolo, priorizando entradas hostis, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 selada, mas usam processos, sessões, conversas, caches, memória e relatórios separados; registrar backend real e nível I0/I1/I2/I3.
- Skeptical approver: pré-sela hipóteses, executa todas as mutações em OCI limpo, restaura e repete o aceite; falha, divergência ou inconclusivo veta.

## Evidence

Somente o coordinator grava artefatos sanitizados em `.board/evidence/AUR-389/`: spec/identity seladas, red-green-refactor, clean tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay restaurado, hashes e verdicts. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.
