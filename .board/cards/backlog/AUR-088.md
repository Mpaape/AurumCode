---
id: AUR-088
version: 1
title: Verificar findings com agente independente
status: backlog
validation: tested
profile_owner: AUR-405
office: O05-review
depends_on: [AUR-087, AUR-073, AUR-405]
requirements: [PR-ARCH-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005, CR-FIND-001, CR-FIND-002, CR-FIND-003, CR-FIND-004]
paths: [internal/review/verify, tests/acceptance/AUR-088.sh, docs/specs/AUR-088.md, tests/specs/AUR-088, tests/contracts/AUR-088.go, tests/integration/AUR-088.go, tests/e2e/AUR-088.sh, tests/unit/AUR-088.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

verifier recebe spec, diff, finding e evidence sem rationale privado e veta falso claim.

## Non-goals

- Não corrigir nem reescrever o finding vetado: este card apenas emite o veto, e reformular o achado continua sendo trabalho do reviewer autor.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-087, AUR-073 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- verifier recebe spec, diff, finding e evidence sem rationale privado e veta falso claim.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Verificar findings com agente independente

- Given: `tests/specs/AUR-088/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Verificar findings com agente independente”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Verificar findings com agente independente” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e `.board/evidence/AUR-088/acceptance/AC-001.json` lista o pacote entregue ao verifier com spec, diff, finding e evidence, sem campo de rationale do reviewer autor, e registra o veto tipado do claim não sustentado.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005, CR-FIND-001, CR-FIND-002, CR-FIND-003, CR-FIND-004]

## Public contract

- Apenas “Verificar findings com agente independente” e `internal/review/verify`, `tests/acceptance/AUR-088.sh`, `docs/specs/AUR-088.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-088.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-088/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: escrever em `internal/review/verify` o montador do pacote de verificação que entrega spec, diff, finding e evidence ao verifier, omite o rationale privado do reviewer autor e converte claim não sustentado em veto tipado gravado no artifact.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“fornecer ao verifier o transcript do reviewer autor.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-088.go::TestAUR088`.
- Contract: `tests/contracts/AUR-088.go::ContractAUR088`.
- Integration: `tests/integration/AUR-088.go::IntegrationAUR088`.
- E2E: `tests/e2e/AUR-088.sh::E2EAUR088`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-088`

Expected artifact: `.board/evidence/AUR-088/acceptance/AC-001.json` valida schema, prova “Verificar findings com agente independente” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: fornecer ao verifier o transcript do reviewer autor.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-088/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-088/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-088/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Verificar findings com agente independente” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-088.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
