---
id: AUR-285
version: 1
title: Documentar contratos operacionais e release
status: backlog
validation: tested
profile_owner: AUR-408
office: O12-release
depends_on: [AUR-196, AUR-200, AUR-408]
requirements: [PR-REL-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [docs/contracts/operations, examples/contracts/operations, tests/acceptance/AUR-285.sh, docs/specs/AUR-285.md, tests/unit/AUR-285.go, tests/contracts/AUR-285.go, tests/integration/AUR-285.go, tests/e2e/AUR-285.sh]
read_paths: [go.mod, go.sum, internal, docs, .board/oci/profiles/registry.v1.json, .board/oci/profiles/docs-tool-offline-v1.json, .board/locks/oci/docs-tool-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

run state, evidence audit, image, rollback e approval humana possuem um exemplo reproduzível.

## Non-goals

- Executar release real ou publicar imagem em registry; os cinco exemplos operacionais permanecem sintéticos e locais ao profile.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-196, AUR-200 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- run state, evidence audit, image, rollback e approval humana possuem um exemplo reproduzível.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Documentar contratos operacionais e release

- Given: `examples/contracts/operations` traz um exemplo reproduzível de run state, evidence audit, image, rollback e approval humana, sob o profile `docs-tool-offline-v1`.
- When: cada um dos cinco exemplos operacionais de `examples/contracts/operations` é reexecutado uma vez a partir do estado inicial versionado.
- Then: os cinco exemplos reproduzem byte a byte a saída esperada e `.board/evidence/AUR-285/acceptance/AC-001.json` traz a CandidateIdentityV1 recalculada, recusando identidade stale.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-REL-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Documentar contratos operacionais e release” em `docs/contracts/operations`, `examples/contracts/operations`, `tests/acceptance/AUR-285.sh`, `docs/specs/AUR-285.md`.

## TDD proof

- Test: `tests/acceptance/AUR-285.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-285/AC-001: Documentar contratos operacionais e release ausente`; build/loader/infra não valem.
- Green: documentar em `docs/contracts/operations` os cinco contratos operacionais e criar em `examples/contracts/operations` um exemplo reproduzível para cada, com identidade recalculada a cada execução.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-285.go::TestAUR285`.
- Contract: `tests/contracts/AUR-285.go::ContractAUR285`.
- Integration: `tests/integration/AUR-285.go::IntegrationAUR285`.
- E2E: `tests/e2e/AUR-285.sh::E2EAUR285`.

## Acceptance

container_profile: `docs-tool-offline-v1`

accept: `./.board/bin/oci-run --profile docs-tool-offline-v1 --card AUR-285`

Expected artifact: `.board/evidence/AUR-285/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: reutilizar CandidateIdentityV1 stale no exemplo.
- Expected: o acceptance falha com `AUR-285/AC-001/MUT-001`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository` -> “Documentar contratos operacionais e release” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-285.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
