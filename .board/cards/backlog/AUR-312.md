---
id: AUR-312
version: 1
title: Verificar migração dos doze arquivos de script
status: backlog
office: O14-legacy
depends_on: [AUR-001, AUR-233, AUR-365, AUR-366, AUR-367, AUR-368, AUR-369, AUR-370, AUR-371, AUR-372, AUR-373, AUR-374, AUR-375, AUR-376]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [tests/gates/legacy/AUR-312, tests/acceptance/AUR-312.sh, docs/specs/AUR-312.md]
read_paths: [tests/characterization/legacy/script-files]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

`LegacyScriptSuiteV1` aceita exatamente [AUR-365, AUR-366, AUR-367, AUR-368, AUR-369, AUR-370, AUR-371, AUR-372, AUR-373, AUR-374, AUR-375, AUR-376] quando todos os child manifests possuem verdict pass, digests íntegros e a mesma CandidateIdentityV1.

## Non-goals

- Não possuir nem editar os doze arquivos; cada source e sua caracterização pertencem a exatamente um child AUR-365–398.
- Não reinterpretar child failure como sucesso parcial nem fundir payloads dos children.

## Preconditions

- Os doze manifests registram source digest, formato, effects, disposition e `commands_executed=0` sob a mesma CandidateIdentityV1.
- O gate recebe no máximo 128 manifests/4 MiB e termina em 20 s, sem rede, instalação ou resolução de credencial.

## Postconditions

- `LegacyScriptSuiteV1` contém o conjunto exato ordenado, spec/artifact digests, verdicts e uma CandidateIdentityV1 comum.
- Nenhum path de implementação dos children é owned pelo agregador; somente IDs, digests, counts e errors sanitizados são emitidos.

## Acceptance scenarios

### AC-001: Fechar LegacyScriptSuiteV1

- Given: `tests/gates/legacy/AUR-312/cases.tsv` contém suite nominal, child ausente, duplicado, digest adulterado, identity divergente e verdict não-pass para o conjunto [AUR-365, AUR-366, AUR-367, AUR-368, AUR-369, AUR-370, AUR-371, AUR-372, AUR-373, AUR-374, AUR-375, AUR-376].
- When: `VerifyLegacyScriptSuiteV1` lê somente os manifests, valida o conjunto exato e ordena por card ID; não executa comportamento de child.
- Then: nominal retorna pass e digest estável; inválidos retornam respectivamente `script_manifest_missing`, `child_duplicate`, `child_digest_mismatch`, `candidate_identity_mismatch` e `child_not_pass` antes de emitir suite aprovada.
- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente `VerifyLegacyScriptSuiteV1`, schema `LegacyScriptSuiteV1` e os cinco error codes nos paths exclusivos deste gate; children mantêm ownership integral de seus artifacts.

## TDD proof

- Test: `tests/acceptance/AUR-312.sh::AC-001`.
- Red: com o parser alcançável e manifests sintéticos válidos, retirar o primeiro child obrigatório sai 1 com `AUR-312/AC-001/script_manifest_missing`; engine indisponível, permissão ou erro de loader invalidam a prova.
- Green: ler os doze manifests de `tests/characterization/legacy/script-files`, comparar o conjunto com [AUR-365..AUR-376], conferir digest, identity e verdict de cada child e emitir `LegacyScriptSuiteV1` ordenado por card ID.
- Refactor: reordenar input sem alterar set/identity/digests e preservar output digest; executar MUT-001, restaurar e repetir o GREEN.
- Unit: not-applicable: o agregador é um gate de manifests provado integralmente por matrizes de contrato.
- Contract: `tests/acceptance/AUR-312.sh::ContractAUR312`.
- Integration: `tests/acceptance/AUR-312.sh::IntegrationAUR312`.
- E2E: `tests/acceptance/AUR-312.sh::E2EAUR312`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-312`

Expected artifact: o coordinator deriva `.board/evidence/AUR-312/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover AUR-369 do conjunto mantendo os outros onze.
- Expected: o gate sai 1 com `AUR-312/AC-001/script_manifest_missing`; restore recupera doze entradas.

### MUT-002: AC-001 / container-engine

- Change: aceitar um child com `commands_executed=1`.
- Expected: o gate sai 1 com `AUR-312/AC-001/source_executed`; restore exige zero para todos.

## Security and privacy

- Data flow: doze manifests de script legado -> verifier bounded -> IDs/digests/verdicts sanitizados; payloads de child são sinks proibidos.
- Secrets: `AURUM_SECRET_CANARY`, env e credential values não aparecem em stdout, stderr, suite manifest, logs ou evidence.
- Least privilege: worker non-root, read-only, network none, ALL capabilities dropped, no-new-privileges, recursos/deadline bounded e nenhuma publicação.

## Documentation

- `docs/specs/AUR-312.md` documenta schema, conjunto exato, bounds, error codes, exemplo sintético e diagnóstico offline.

## Compatibility, migration, rollback

- Compatibility: child contracts permanecem inalterados; qualquer diferença no set/identity/digest falha AC-001.
- Migration: consumidores passam a depender do suite manifest somente depois que todos os children são aprovados.
- Rollback: remover somente o suite manifest e derivados, preservar os child artifacts e repetir seus gates; nenhum estado externo é publicado.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
