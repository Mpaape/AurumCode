---
id: AUR-294
version: 1
title: Empacotar adapter Cloud Run
status: backlog
validation: tested
profile_owner: AUR-412
office: O13-delivery
depends_on: [AUR-226, AUR-412]
requirements: [PR-REL-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [deploy/gcp/cloud-run, tests/e2e/deploy/cloud-run, tests/acceptance/AUR-294.sh, docs/specs/AUR-294.md, tests/unit/AUR-294.go, tests/contracts/AUR-294.go, tests/integration/AUR-294.go, tests/e2e/AUR-294.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/release-build-v1.json, .board/locks/oci/release-build-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, cloud-runtime]
---

## Outcome

um manifest Cloud Run referencia a mesma imagem OCI e contrato HTTP, sem SDK ou lógica cloud no core.

## Non-goals

- Provisionar projeto GCP, IAM ou deploy real no Cloud Run; entrega o manifest versionado e sua verificação local.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-226 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- um manifest Cloud Run referencia a mesma imagem OCI e contrato HTTP, sem SDK ou lógica cloud no core.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Empacotar adapter Cloud Run

- Given: `deploy/gcp/cloud-run` declara o manifest de serviço e a imagem OCI aprovada por digest, com o profile `release-build-v1` ativo.
- When: `tests/e2e/deploy/cloud-run` renderiza o manifest Cloud Run e sobe o container do digest declarado contra o contrato HTTP.
- Then: o digest do manifest é idêntico ao da imagem OCI aprovada, o endpoint HTTP devolve o contrato esperado e nenhum import de SDK cloud aparece no domain core.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-REL-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Empacotar adapter Cloud Run” em `deploy/gcp/cloud-run`, `tests/e2e/deploy/cloud-run`, `tests/acceptance/AUR-294.sh`, `docs/specs/AUR-294.md`.

## TDD proof

- Test: `tests/acceptance/AUR-294.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-294/AC-001: Empacotar adapter Cloud Run ausente`; build/loader/infra não valem.
- Green: escrever o manifest de `deploy/gcp/cloud-run` apontando para o digest aprovado e o e2e de `tests/e2e/deploy/cloud-run` que compara digests e exercita o handler HTTP pelo composition root existente.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-294.go::TestAUR294`.
- Contract: `tests/contracts/AUR-294.go::ContractAUR294`.
- Integration: `tests/integration/AUR-294.go::IntegrationAUR294`.
- E2E: `tests/e2e/AUR-294.sh::E2EAUR294`.

## Acceptance

container_profile: `release-build-v1`

accept: `./.board/bin/oci-run --profile release-build-v1 --card AUR-294`

Expected artifact: `.board/evidence/AUR-294/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: trocar command para composition root diferente.
- Expected: o acceptance falha com `AUR-294/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / cloud-runtime

- Change: injetar config cloud no domain core.
- Expected: o acceptance falha com `AUR-294/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `cloud-runtime` -> “Empacotar adapter Cloud Run” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-294.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
