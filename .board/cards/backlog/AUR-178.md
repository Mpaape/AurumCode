---
id: AUR-178
version: 1
title: Ler merge requests GitLab
status: backlog
validation: tested
profile_owner: AUR-409
office: O06-scm
depends_on: [AUR-018, AUR-057, AUR-097, AUR-234, AUR-235, AUR-236, AUR-409]
requirements: [PR-ARCH-001, PR-SCM-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003]
paths: [internal/scm/gitlab/read, tests/acceptance/AUR-178.sh, docs/specs/AUR-178.md, tests/fixtures/AUR-178, tests/unit/AUR-178.go, tests/contracts/AUR-178.go, tests/integration/AUR-178.go, tests/e2e/AUR-178.sh]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: confidential
trust_boundaries: [repository, scm, secret-channel]
---

## Outcome

`AUR178ResultV1` é o único resultado reversível de “Ler merge requests GitLab” e limita a implementação a `internal/scm/gitlab/read`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “ignorar removed file no diff GitLab”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-018, AUR-057, AUR-097, AUR-234, AUR-235, AUR-236]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-178` contém exatamente um caso nominal e um contracaso mutado, sob 32 objetos SCM, 2 MiB total, 20 s e 1 CPU; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR178ResultV1` canônico e o contracaso produz `AUR178_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Ler merge requests GitLab

- Given: CandidateIdentityV1, digests de [AUR-018, AUR-057, AUR-097, AUR-234, AUR-235, AUR-236] e a fixture bounded `tests/fixtures/AUR-178` com caso nominal e “ignorar removed file no diff GitLab”.
- When: o consumidor invoca “Ler merge requests GitLab” uma vez no port público card-scoped, com limite de 32 objetos SCM, 2 MiB total, 20 s e 1 CPU.
- Then: o ChangeSource GitLab lê merge request por polling autenticado read-only, pagina e normaliza o mesmo ChangeSet, sem webhook ou permissão de publicação; o consumidor recebe `AUR178ResultV1` com status e digest canônicos. O contracaso “ignorar removed file no diff GitLab” retorna `AUR178_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-SCM-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003]

## Public contract

- `AUR178ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Ler merge requests GitLab”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-178.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-178/AC-001: AUR178_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR178ResultV1` dentro de 32 objetos SCM, 2 MiB total, 20 s e 1 CPU, enquanto “ignorar removed file no diff GitLab” retorna `AUR178_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-178.go::TestAUR178NominalAndMutated`.
- Contract: `tests/contracts/AUR-178.go::TestAUR178ResultV1`.
- Integration: `tests/integration/AUR-178.go::TestAUR178Boundary`.
- E2E: `tests/e2e/AUR-178.sh::AC-001`.

## Acceptance

container_profile: `fake-scm-offline-v1`

accept: `./.board/bin/oci-run --profile fake-scm-offline-v1 --card AUR-178`

Expected artifact: o coordenador valida `.board/evidence/AUR-178/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: ignorar removed file no diff GitLab.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-178/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / scm

- Change: alterar o head remoto após leitura ou divergir repository identity.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-178/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: injetar AURUM_SECRET_CANARY no erro da fronteira.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-178/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR178ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-178.md` fixa input, output, `AUR178_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR178ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
