---
id: AUR-397
version: 1
title: Definir FileEdit seguro
status: backlog
office: O08-testqa
depends_on: [AUR-027, AUR-090, AUR-129, AUR-403]
requirements: [PR-ARCH-001, PR-TST-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-005, CR-COV-005]
paths: [internal/testgen/model/fileedit, tests/acceptance/AUR-397.sh, docs/specs/AUR-397.md, tests/fixtures/AUR-397, tests/unit/AUR-397.go, tests/contracts/AUR-397.go, tests/integration/AUR-397.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine, filesystem]
---

## Outcome

`AUR456ResultV1` é o único resultado reversível de “Definir FileEdit seguro” e limita a implementação a `internal/testgen/model/fileedit`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “aceitar FileEdit com path traversal”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-027, AUR-090, AUR-129]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-397` contém exatamente um caso nominal e um contracaso mutado, sob 32 arquivos, 4 MiB total, 60 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR456ResultV1` canônico e o contracaso produz `AUR456_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Definir FileEdit seguro

- Given: CandidateIdentityV1, digests de [AUR-027, AUR-090, AUR-129] e a fixture bounded `tests/fixtures/AUR-397` com caso nominal e “aceitar FileEdit com path traversal”.
- When: o consumidor invoca “Definir FileEdit seguro” uma vez no port público card-scoped, com limite de 32 arquivos, 4 MiB total, 60 s e 2 CPUs.
- Then: FileEdit vincula path relativo, mode, before digest e after digest a uma TestProposal selada e rejeita traversal, symlink e target não relacionado; o consumidor recebe `AUR456ResultV1` com status e digest canônicos. O contracaso “aceitar FileEdit com path traversal” retorna `AUR456_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-TST-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-005, CR-COV-005]

## Public contract

- `AUR456ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Definir FileEdit seguro”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-397.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-397/AC-001: AUR456_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR456ResultV1` dentro de 32 arquivos, 4 MiB total, 60 s e 2 CPUs, enquanto “aceitar FileEdit com path traversal” retorna `AUR456_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-397.go::TestAUR456NominalAndMutated`.
- Contract: `tests/contracts/AUR-397.go::TestAUR456ResultV1`.
- Integration: `tests/integration/AUR-397.go::TestAUR456Boundary`.
- E2E: not-applicable: o contrato atômico de AUR-397 é totalmente observado no port e o fluxo consumidor pertence a card dependente.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-397`

Expected artifact: o coordenador valida `.board/evidence/AUR-397/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar FileEdit com path traversal.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-397/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / container-engine

- Change: seguir symlink para fora do worktree.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-397/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / filesystem

- Change: copiar valor de ambiente para o conteúdo editado.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-397/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR456ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-397.md` fixa input, output, `AUR456_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR456ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
