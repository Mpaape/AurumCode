---
id: AUR-112
version: 1
title: Aprovar SCM e publicação segura
status: backlog
office: O00-governance
depends_on: [AUR-097, AUR-098, AUR-099, AUR-100, AUR-101, AUR-102, AUR-103, AUR-104, AUR-105, AUR-106, AUR-107, AUR-108, AUR-109, AUR-110, AUR-111, AUR-178, AUR-179, AUR-395, AUR-396, AUR-409]
requirements: [PR-ARCH-001, PR-SCM-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001]
paths: [tests/acceptance/gates/AUR-112, tests/acceptance/AUR-112.sh, docs/specs/AUR-112.md, tests/fixtures/AUR-112, tests/unit/AUR-112.go, tests/contracts/AUR-112.go, tests/integration/AUR-112.go, tests/e2e/AUR-112.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, scm, secret-channel]
---

## Outcome

`AUR112ResultV1` é um manifesto determinístico que decide “Aprovar SCM e publicação segura” somente a partir de digests e verdicts dos produtores dependentes; este card não reexecuta nem reúne comportamentos implementáveis de forma independente.

## Non-goals

- Não relaxar o contrato para aceitar “juntar analyzer e publisher no mesmo token e processo”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-097, AUR-098, AUR-099, AUR-100, AUR-101, AUR-102, AUR-103, AUR-104, AUR-105, AUR-106, AUR-107, AUR-108, AUR-109, AUR-110, AUR-111, AUR-178, AUR-179, AUR-395, AUR-396]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-112` contém exatamente um caso nominal e um contracaso mutado, sob 64 manifests, 4 MiB total, 60 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR112ResultV1` canônico e o contracaso produz `AUR112_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Aprovar SCM e publicação segura

- Given: CandidateIdentityV1, digests de [AUR-097, AUR-098, AUR-099, AUR-100, AUR-101, AUR-102, AUR-103, AUR-104, AUR-105, AUR-106, AUR-107, AUR-108, AUR-109, AUR-110, AUR-111, AUR-178, AUR-179, AUR-395, AUR-396] e a fixture bounded `tests/fixtures/AUR-112` com caso nominal e “juntar analyzer e publisher no mesmo token e processo”.
- When: o gate `tests/acceptance/gates/AUR-112` ordena por card id os manifests selados de SCM e publicação, confere path e digest de cada um sem invocar produtor algum e compõe o aggregate digest sob 64 manifests, 4 MiB, 60 s e 2 CPUs no perfil `fake-scm-offline-v1`.
- Then: o coordenador recebe `AUR112ResultV1` com uma linha por input, aggregate digest e verdict tipado; o contracaso “juntar analyzer e publisher no mesmo token e processo” retorna `AUR112_INVALID_INPUT` sem manifesto aprovado.
- Covers requirements: [PR-ARCH-001, PR-SCM-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001]

## Public contract

- `AUR112ResultV1` contém apenas input IDs, digests, verdicts, aggregate digest e erro tipado; não contém payload, comportamento reexecutado, segredo ou autoridade humana.

## TDD proof

- Test: `tests/acceptance/AUR-112.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-112/AC-001: AUR112_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR112ResultV1` dentro de 64 manifests, 4 MiB total, 60 s e 2 CPUs, enquanto “juntar analyzer e publisher no mesmo token e processo” retorna `AUR112_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-112.go::TestAUR112NominalAndMutated`.
- Contract: `tests/contracts/AUR-112.go::TestAUR112ResultV1`.
- Integration: `tests/integration/AUR-112.go::TestAUR112Boundary`.
- E2E: `tests/e2e/AUR-112.sh::AC-001`.

## Acceptance

container_profile: `fake-scm-offline-v1`

accept: `./.board/bin/oci-run --profile fake-scm-offline-v1 --card AUR-112`

Expected artifact: o coordenador valida `.board/evidence/AUR-112/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: juntar analyzer e publisher no mesmo token e processo.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-112/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / scm

- Change: mudar o head remoto depois da leitura ou fornecer identidade de repositório divergente.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-112/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: injetar o canário AURUM_SECRET_CANARY na falha devolvida pela fronteira.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-112/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR112ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-112.md` fixa input, output, `AUR112_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR112ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer A: cobre cada hunk e as dez dimensões; prioriza contrato, SOLID, compatibilidade, rollback e sensibilidade dos testes.
- Reviewer B: cobre cada hunk e as dez dimensões; prioriza input hostil, segredos, permissões, supply chain, limites e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 em processo, sessão, conversation, cache e memória isolados; o manifest registra I0-I3 e backend family sem inflar independência.
- Skeptical approver: sela hipóteses antes dos reviews, executa cada MUT em OCI limpo, restaura e repete; sobrevivência, erro de teste ou inconclusivo veta.

## Evidence

Somente o coordenador escreve `.board/evidence/AUR-112/`; builder e testes emitem observações não confiáveis. O bundle contém identity/spec digests, RED/GREEN/refactor, gates, mapas completos A/B, challenge pré-selado, mutações, restore/replay e verdicts sanitizados. `done` requer evento humano autenticado; segredo, env, prompt/resposta brutos e chain-of-thought são proibidos.
