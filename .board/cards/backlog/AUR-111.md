---
id: AUR-111
version: 1
title: Expor health liveness sanitizado
status: backlog
office: O08-runtime
depends_on: [AUR-099, AUR-101, AUR-034, AUR-403]
requirements: [PR-ARCH-001, PR-SCM-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001]
paths: [internal/server/health, tests/acceptance/AUR-111.sh, docs/specs/AUR-111.md, tests/fixtures/AUR-111, tests/unit/AUR-111.go, tests/contracts/AUR-111.go, tests/integration/AUR-111.go, tests/e2e/AUR-111.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: confidential
trust_boundaries: [repository, scm, secret-channel]
---

## Outcome

`AUR111ResultV1` é o único resultado reversível de “Expor health liveness sanitizado” e limita a implementação ao port `internal/server/health`; wiring HTTP pertence ao card de servidor.

## Non-goals

- Não relaxar o contrato para aceitar “misturar readiness no status de liveness”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-099, AUR-101, AUR-034]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-111` contém exatamente um caso nominal e um contracaso mutado, sob 64 eventos, 1 MiB total, 10 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR111ResultV1` canônico e o contracaso produz `AUR111_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Expor health liveness sanitizado

- Given: CandidateIdentityV1, digests de [AUR-099, AUR-101, AUR-034] e a fixture bounded `tests/fixtures/AUR-111` com caso nominal e “misturar readiness no status de liveness”.
- When: o consumidor invoca “Expor health liveness sanitizado” uma vez no port público card-scoped, com limite de 64 eventos, 1 MiB total, 10 s e 2 CPUs.
- Then: o endpoint de liveness confirma somente que o processo responde e nunca incorpora readiness, métricas, configuração ou valores de provider; o consumidor recebe `AUR111ResultV1` com status e digest canônicos. O contracaso “misturar readiness no status de liveness” retorna `AUR111_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-SCM-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-003, CR-GH-001]

## Public contract

- `AUR111ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Expor health liveness sanitizado”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-111.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-111/AC-001: AUR111_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR111ResultV1` dentro de 64 eventos, 1 MiB total, 10 s e 2 CPUs, enquanto “misturar readiness no status de liveness” retorna `AUR111_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-111.go::TestAUR111NominalAndMutated`.
- Contract: `tests/contracts/AUR-111.go::TestAUR111ResultV1`.
- Integration: `tests/integration/AUR-111.go::TestAUR111Boundary`.
- E2E: `tests/e2e/AUR-111.sh::AC-001`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-111`

Expected artifact: o coordenador valida `.board/evidence/AUR-111/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: misturar readiness no status de liveness.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-111/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / scm

- Change: devolver liveness com head remoto divergente.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-111/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: incluir AURUM_SECRET_CANARY no payload de health.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-111/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR111ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-111.md` fixa input, output, `AUR111_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR111ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
