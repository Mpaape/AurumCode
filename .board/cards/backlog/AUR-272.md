---
id: AUR-272
version: 1
title: Executar cenário demo Python
status: backlog
validation: tested
profile_owner: AUR-411
office: O09-demo
depends_on: [AUR-187, AUR-241, AUR-252, AUR-411]
requirements: [PR-DEMO-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [demo-repo/scenarios/python, tests/acceptance/AUR-272.sh, docs/specs/AUR-272.md, tests/unit/AUR-272.go, tests/contracts/AUR-272.go, tests/integration/AUR-272.go, tests/e2e/AUR-272.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

o consumer demo Python prova review, docs e testgen sem depender de outra linguagem.

## Non-goals

- Cobrir defeitos de outras linguagens ou depender de runtime não Python para o cenário passar.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-187, AUR-241, AUR-252 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- o consumer demo Python prova review, docs e testgen sem depender de outra linguagem.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Executar cenário demo Python

- Given: os manifests aprovados de AUR-187, AUR-241, AUR-252 estão disponíveis na mesma base, fixture bounded e perfil OCI ativo.
- When: o cenário demo Python é executado uma vez sobre `demo-repo/scenarios/python`, com o defeito plantado presente no arquivo selecionado.
- Then: o review aponta o defeito Python plantado, docs e testgen gravam os artifacts do cenário e nenhum toolchain de outra linguagem é acionado segundo a evidência AC-001.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-DEMO-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Executar cenário demo Python” nos artefatos `demo-repo/scenarios/python`, `tests/acceptance/AUR-272.sh`, `docs/specs/AUR-272.md`.

## TDD proof

- Test: `tests/acceptance/AUR-272.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-272/AC-001: Executar cenário demo Python ausente`; build/loader/infra não valem.
- Green: montar `demo-repo/scenarios/python` com o defeito plantado, o oracle que o referencia por arquivo e linha, e o runner que aciona review, docs e testgen usando apenas o toolchain Python do perfil.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-272.go::TestAUR272`.
- Contract: `tests/contracts/AUR-272.go::ContractAUR272`.
- Integration: `tests/integration/AUR-272.go::IntegrationAUR272`.
- E2E: `tests/e2e/AUR-272.sh::E2EAUR272`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-272`

Expected artifact: `.board/evidence/AUR-272/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: trocar o defeito Python plantado por arquivo nunca selecionado.
- Expected: o acceptance inalterado falha com `AUR-272/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-272/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Executar cenário demo Python” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-272.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
