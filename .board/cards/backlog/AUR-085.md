---
id: AUR-085
version: 1
title: Executar reviewers especializados em paralelo
status: backlog
office: O04-agents
depends_on: [AUR-083, AUR-072, AUR-073, AUR-074, AUR-077, AUR-405]
requirements: [PR-ARCH-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [internal/agents/reviewers, tests/acceptance/AUR-085.sh, docs/specs/AUR-085.md, tests/specs/AUR-085, tests/contracts/AUR-085.go, tests/integration/AUR-085.go, tests/e2e/AUR-085.sh, tests/unit/AUR-085.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

reviewers respeitam skills, budgets e Partial explícito em timeout.

## Non-goals

- não sintetiza nem reconcilia os relatórios produzidos pelos reviewers; a saída é o conjunto de reports com o respectivo status de completude.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-083, AUR-072, AUR-073, AUR-074, AUR-077 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- reviewers respeitam skills, budgets e Partial explícito em timeout.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Executar reviewers especializados em paralelo

- Given: `tests/specs/AUR-085/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Executar reviewers especializados em paralelo”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Executar reviewers especializados em paralelo” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact traz o resultado agregado dentro do deadline com o budget consumido por reviewer; o caso com reviewer estourando o prazo sai marcado parcial nomeando quem expirou, sem espera adicional.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- Apenas “Executar reviewers especializados em paralelo” e `internal/agents/reviewers`, `tests/acceptance/AUR-085.sh`, `docs/specs/AUR-085.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-085.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-085/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: implementar em `internal/agents/reviewers` o fan-out com budget por reviewer, contexto cancelável no deadline e agregação que marca resultado parcial nomeando quem expirou, sem aguardar reviewer além do prazo.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“marcar Complete quando um reviewer obrigatório falha.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-085.go::TestAUR085`.
- Contract: `tests/contracts/AUR-085.go::ContractAUR085`.
- Integration: `tests/integration/AUR-085.go::IntegrationAUR085`.
- E2E: `tests/e2e/AUR-085.sh::E2EAUR085`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-085`

Expected artifact: `.board/evidence/AUR-085/acceptance/AC-001.json` valida schema, prova “Executar reviewers especializados em paralelo” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: marcar Complete quando um reviewer obrigatório falha.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-085/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-085/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-085/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Executar reviewers especializados em paralelo” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-085.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
