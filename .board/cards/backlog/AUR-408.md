---
id: AUR-408
version: 2
title: Definir perfil docs tool offline
status: backlog
validation: tested
profile_owner: AUR-006
office: O00-governance
depends_on: [AUR-006, AUR-402, AUR-403, AUR-407]
requirements: [PR-ARCH-001, PR-SEC-001, PR-SUP-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002, CR-TRUST-005]
paths: [.board/schemas/docs-tool-offline-profile.schema.json, .board/oci/profiles/registry.v1.json, .board/oci/profiles/docs-tool-offline-v1.json, .board/locks/oci/docs-tool-offline-v1.lock.json, tests/unit/AUR-402.go, tests/unit/AUR-403.go, tests/unit/AUR-404.go, tests/unit/AUR-405.go, tests/unit/AUR-406.go, tests/unit/AUR-407.go, tests/unit/AUR-408.go, tests/contracts/AUR-408.go, tests/integration/AUR-408.go, tests/e2e/AUR-408.sh, tests/acceptance/AUR-408.sh, docs/specs/AUR-408.md]
read_paths: [.board/schemas/container-profile.schema.json, .board/locks/oci/registry-v1.lock.json, tests/acceptance/AUR-402.sh, go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, supply-chain, container-engine]
---

## Outcome

o profile docs-tool-offline-v1 fixa gerador, renderer e plugins por digest, usa fixtures locais e nega plugin dinâmico, fetch remoto, execução de snippet não allowlisted e escrita fora do output efêmero.

## Non-goals

- Gerar ou renderizar qualquer documento sob `docs/`; `docs-tool-offline-v1.json` só pina gerador, renderer e plugins por digest e nega plugin dinâmico, fetch remoto e escrita fora do output efêmero.
- Agregar outro profile ou aceitar fallback para profile, lock ou imagem não registrados.

## Preconditions

- As invariantes de segurança de AUR-006 e o registry extensível entregue por AUR-403 estão aprovados; os inputs do registry continuam dados não confiáveis.
- O acceptance está selado e nenhuma credential ou valor de ambiente integra as fixtures.

## Postconditions

- o profile docs-tool-offline-v1 fixa gerador, renderer e plugins por digest, usa fixtures locais e nega plugin dinâmico, fetch remoto, execução de snippet não allowlisted e escrita fora do output efêmero.
- Nenhum engine é invocado para profile inválido e nenhum byte fora de `paths` muda.

## Acceptance scenarios

### AC-001: Definir perfil docs tool offline

- Given: profile nominal e mutantes para plugin mutável, fetch, snippet executável, output host e tool sem digest.
- When: o registry resolve docs-tool-offline-v1 e valida a cadeia documental.
- Then: somente a toolchain pinada e offline produz plano; cada mutante falha antes do renderer.
- Covers requirements: [PR-ARCH-001, PR-SEC-001, PR-SUP-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002, CR-TRUST-005]

## Public contract

- Adiciona somente a chave `docs-tool-offline-v1`, seu documento versionado, lock content-addressed e códigos de rejeição descritos neste TaskSpec.
- O schema específico pertence a este card, a entrada é persistida no registry canônico e o acceptance de AUR-402 permanece verde como regressão de compatibilidade.
- O runner continua fail-closed para `docs-tool-offline-v1` até um card de implementação dependente habilitá-lo explicitamente.

## TDD proof

- Test: `tests/acceptance/AUR-408.sh::AC-001`.
- Red: o loader alcançável rejeita o documento nominal com `AUR-408/AC-001/profile-unregistered`; erro de ambiente, compilação, engine ou rede não conta.
- Green: registrar apenas `docs-tool-offline-v1` com schema, lock e image-set digests exatos, preservando a rejeição de todos os mutantes declarados.
- Refactor: preservar registry digest, códigos de erro, ordem canônica e zero engine calls em replay.
- Unit: `tests/unit/AUR-408.go::TestAUR408`.
- Contract: `tests/contracts/AUR-408.go::ContractAUR408`.
- Integration: `tests/integration/AUR-408.go::IntegrationAUR408`.
- E2E: `tests/e2e/AUR-408.sh::E2EAUR408`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-408`

Expected artifact: `.board/evidence/AUR-408/acceptance/AC-001.json` contém registry digest, profile digest, lock digest, image-set digest, códigos dos mutantes e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: duplicar a chave `docs-tool-offline-v1` com documento de bytes diferentes.
- Expected: o mesmo `accept` falha com `AUR-408/AC-001/MUT-001/duplicate-profile`; restore passa.

### MUT-002: AC-001 / supply-chain

- Change: substituir exatamente um image ou tool digest por tag mutável.
- Expected: o mesmo `accept` falha com `AUR-408/AC-001/MUT-002/mutable-input`; nenhum fetch ocorre.

### MUT-003: AC-001 / container-engine

- Change: remover um limite obrigatório ou habilitar rede, mount, socket, device, root ou capability.
- Expected: o mesmo `accept` falha com `AUR-408/AC-001/MUT-003/unsafe-plan`; engine call count permanece zero.

## Security and privacy

- Registry, profile e locks são dados não confiáveis; somente digests e aliases públicos entram no artefato.
- Credentials, env, prompts, respostas, source privado e `AURUM_SECRET_CANARY` não entram em logs ou evidência.
- Validação ocorre antes do engine; nenhum profile concede autoridade, publicação, rede ou acesso ao host.

## Documentation

- `docs/specs/AUR-408.md` documenta schema, locks, imagens pinadas, comandos permitidos, limites, códigos e diagnóstico offline.

## Compatibility, migration, rollback

- Profiles existentes conservam chave e digest; mudança incompatível cria nova chave versionada.
- Migração altera consumidores por card dependente; nunca redireciona uma chave existente.
- Rollback remove somente esta entrada e seus arquivos quando nenhum consumidor depender dela.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
