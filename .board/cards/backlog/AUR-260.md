---
id: AUR-260
version: 1
title: Expor cancel de run na CLI
status: backlog
office: O08-runtime
depends_on: [AUR-150, AUR-151, AUR-152, AUR-407]
requirements: [PR-OPS-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [cmd/aurumcode/cancel.go, cmd/aurumcode/cancel_test.go, tests/acceptance/AUR-260.sh, docs/specs/AUR-260.md, tests/unit/AUR-260.go, tests/contracts/AUR-260.go, tests/integration/AUR-260.go, tests/e2e/AUR-260.sh]
read_paths: [go.mod, go.sum, internal, .board/oci/profiles/registry.v1.json, .board/oci/profiles/sqlite-offline-v1.json, .board/locks/oci/sqlite-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, state-store]
---

## Outcome

aurumcode cancel grava uma transição idempotente e faz workers observarem cancellation sem novo efeito.

## Non-goals

- Interromper à força processos de worker já em execução ou reverter efeitos que o run tenha produzido antes do cancel.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-150, AUR-151, AUR-152 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- aurumcode cancel grava uma transição idempotente e faz workers observarem cancellation sem novo efeito.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Expor cancel de run na CLI

- Given: manifests aprovados de AUR-150, AUR-151 e AUR-152 na mesma base, um run em execução com worker ativo observando o CAS e o perfil `sqlite-offline-v1` ativo.
- When: `aurumcode cancel` é invocado duas vezes seguidas sobre o mesmo run, a segunda chamada após o worker já ter lido a transição.
- Then: existe exatamente uma transição `cancelled` no CAS após as duas chamadas, o worker encerra sem novo efeito e `.board/evidence/AUR-260/acceptance/AC-001.json` registra ambos os exit codes 0.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-OPS-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente “Expor cancel de run na CLI” nos artefatos `cmd/aurumcode/cancel.go`, `cmd/aurumcode/cancel_test.go`, `tests/acceptance/AUR-260.sh`, `docs/specs/AUR-260.md`.

## TDD proof

- Test: `tests/acceptance/AUR-260.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-260/AC-001: Expor cancel de run na CLI ausente`; build/loader/infra não valem.
- Green: implementar `cmd/aurumcode/cancel.go` gravando a transição `cancelled` sob compare-and-swap, tratando a repetição como sucesso sem novo evento e publicando o sinal que o worker consulta antes de cada efeito.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-260.go::TestAUR260`.
- Contract: `tests/contracts/AUR-260.go::ContractAUR260`.
- Integration: `tests/integration/AUR-260.go::IntegrationAUR260`.
- E2E: `tests/e2e/AUR-260.sh::E2EAUR260`.

## Acceptance

container_profile: `sqlite-offline-v1`

accept: `./.board/bin/oci-run --profile sqlite-offline-v1 --card AUR-260`

Expected artifact: `.board/evidence/AUR-260/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar transição cancelled para succeeded.
- Expected: o acceptance inalterado falha com `AUR-260/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / state-store

- Change: repetir evento com versão CAS stale.
- Expected: o acceptance inalterado falha com `AUR-260/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `state-store` -> contrato “Expor cancel de run na CLI” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-260.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
