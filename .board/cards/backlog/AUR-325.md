---
id: AUR-325
version: 1
title: Parsear blob TypeScript sem estado
status: backlog
validation: tested
profile_owner: AUR-406
office: O02-index
depends_on: [AUR-224, AUR-406]
requirements: [PR-REV-001, PR-MEM-002, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-COV-002, CR-COV-005]
paths: [internal/parsers/grammars/typescript, tests/fixtures/polyglot/typescript/entities, tests/acceptance/AUR-325.sh, docs/specs/AUR-325.md, tests/unit/AUR-325.go, tests/contracts/AUR-325.go, tests/integration/AUR-325.go, tests/e2e/AUR-325.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/parser-worker-v1.json, .board/locks/oci/parser-worker-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, parser-worker, filesystem]
---

## Outcome

uma entrada TypeScript válida adiciona interface, type e enum ao contrato JavaScript sem perder ranges.

## Non-goals

- Não fazer type checking, inferência de tipos nem emitir JavaScript transpilado; o adapter apenas extrai declarações do blob TypeScript.

## Preconditions

- O contrato AUR-224 e o worker pinado estão aprovados na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- uma entrada TypeScript válida adiciona interface, type e enum ao contrato JavaScript sem perder ranges.
- Nenhum processo do repo é iniciado e nenhum path fora de `paths` muda.

## Acceptance scenarios

### AC-001: Parsear blob TypeScript sem estado

- Given: um blob typescript golden e um hostil, ambos bounded e ligados a blob SHA.
- When: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-325` submete o blob TypeScript ao worker com language tag `typescript` e grammar digest e compara os ranges com os já emitidos para JavaScript.
- Then: `.board/evidence/AUR-325/acceptance/AC-001.json` contém as entidades JavaScript mais interface, type e enum com os mesmos ranges da golden de `tests/fixtures/polyglot/typescript/entities`; range deslocado reprova o caso.
- And: replay devolve entidades byte-idênticas em ordem canônica.

- Covers requirements: [PR-REV-001, PR-MEM-002, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-COV-002, CR-COV-005]

## Public contract

- Adiciona somente o adapter typescript ao ParserPort e os artifacts `internal/parsers/grammars/typescript`, `tests/fixtures/polyglot/typescript/entities`, `tests/acceptance/AUR-325.sh`, `docs/specs/AUR-325.md`.

## TDD proof

- Test: `tests/acceptance/AUR-325.sh::AC-001`.
- Red: o contract suite falha com `AUR-325/AC-001: Parsear blob TypeScript sem estado ausente`, não por grammar/image indisponível.
- Green: estender o contrato JavaScript no adapter `typescript` de `internal/parsers/grammars/typescript` com interface, type e enum preservando os ranges já emitidos e gravar a golden.
- Refactor: preservar entity IDs, ranges, ordenação e typed errors.
- Unit: `tests/unit/AUR-325.go::TestAUR325`.
- Contract: `tests/contracts/AUR-325.go::ContractAUR325`.
- Integration: `tests/integration/AUR-325.go::IntegrationAUR325`.
- E2E: `tests/e2e/AUR-325.sh::E2EAUR325`.

## Acceptance

container_profile: `parser-worker-v1`

accept: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-325`

Expected artifact: `.board/evidence/AUR-325/acceptance/AC-001.json` contém golden digest, parser digest e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: descartar interface da golden fixture.
- Expected: o acceptance falha com `AUR-325/AC-001/MUT-001`; restore passa e compile/infra não conta.

### MUT-002: AC-001 / parser-worker

- Change: trocar grammar digest ou responder depois do timeout.
- Expected: o acceptance falha com `AUR-325/AC-001/MUT-002`; não aceita entidades parciais como sucesso.

### MUT-003: AC-001 / filesystem

- Change: fornecer symlink/source relation que escape do snapshot.
- Expected: o acceptance falha com `AUR-325/AC-001/MUT-003`, sem leitura externa; restore passa.

## Security and privacy

- Data flow: bytes do Git blob -> worker pinado -> Entity records bounded.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: worker não-root, read-only, network none, sem socket/mount/device e sem execução do blob.

## Documentation

- `docs/specs/AUR-325.md` fixa grammar/version, entidades, unsupported syntax, limites e erro.

## Compatibility, migration, rollback

- Compatibility: ParserPort e outras linguagens permanecem inalterados.
- Migration: not-applicable; parser version nova invalida derived entities por provenance.
- Rollback: remover registro typescript e derived entities daquela parser version; fonte não muda.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
