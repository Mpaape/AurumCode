---
id: AUR-226
version: 1
title: Empacotar servidor Go em imagem OCI
status: backlog
office: O13-delivery
depends_on: [AUR-111, AUR-191, AUR-412]
requirements: [PR-ARCH-001, PR-REL-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [deploy/oci, deploy/gcp, cmd/server, tests/e2e/deploy, tests/acceptance/AUR-226.sh, docs/specs/AUR-226.md, tests/unit/AUR-226.go, tests/contracts/AUR-226.go, tests/integration/AUR-226.go, tests/e2e/AUR-226.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/release-build-v1.json, .board/locks/oci/release-build-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, supply-chain, container-engine]
---

## Outcome

O servidor Go é empacotado numa imagem OCI não-root, com rootfs read-only e o mesmo composition root do binário local, sem lógica de cloud no domínio.

## Non-goals

- Provisionar conta cloud real por padrão ou embutir credenciais na imagem: este card entrega o empacotamento OCI e o manifest de deploy do servidor Go.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-111, AUR-191 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- O servidor Go é empacotado numa imagem OCI não-root, com rootfs read-only e o mesmo composition root do binário local, sem lógica de cloud no domínio.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Empacotar servidor Go em imagem OCI

- Given: os manifests aprovados de AUR-111, AUR-191 estão disponíveis e vinculados à mesma base, fixture determinística e perfil OCI aplicado.
- When: o acceptance constrói a imagem de `deploy/oci`, sobe o container e compara a resposta do health endpoint com a do binário `cmd/server` executado localmente sobre a mesma fixture.
- Then: a imagem inicia como usuário não-root, expõe o health contract esperado e produz o mesmo response digest do servidor local para a fixture nominal.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-ARCH-001, PR-REL-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Apenas “Empacotar servidor Go em imagem OCI” e `deploy/oci`, `deploy/gcp`, `cmd/server`, `tests/e2e/deploy`, `tests/acceptance/AUR-226.sh`, `docs/specs/AUR-226.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-226.sh::AC-001`.
- Red: no base, alcança comportamento e falha com `AUR-226/AC-001: Empacotar servidor Go em imagem OCI ausente`; compilação, dependência, loader ou ambiente não valem.
- Green: produzir somente a imagem funcional não-root/read-only com composition root idêntico; reprodutibilidade, SBOM e provenance pertencem a AUR-421.
- Refactor: rerodar AC-001 e perfis, preservando contrato, exit code, ordem e digest.
- Unit: `tests/unit/AUR-226.go::TestAUR226`.
- Contract: `tests/contracts/AUR-226.go::ContractAUR226`.
- Integration: `tests/integration/AUR-226.go::IntegrationAUR226`.
- E2E: `tests/e2e/AUR-226.sh::E2EAUR226`.

## Acceptance

container_profile: `release-build-v1`

accept: `./.board/bin/oci-run --profile release-build-v1 --card AUR-226`

Expected artifact: `.board/evidence/AUR-226/acceptance/AC-001.json` valida schema, prova “Empacotar servidor Go em imagem OCI” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: iniciar um handler GCP com composition root diferente do servidor OCI.
- Expected: o mesmo `accept` falha com `AUR-226/AC-001/MUT-001`; restaurado volta a passar; compilação/infra não conta.

### MUT-002: AC-001 / supply-chain

- Change: alterar digest pinado ou dependency lock.
- Expected: o mesmo `accept` falha com `AUR-226/AC-001/MUT-002`; restaurado volta a passar; compilação/infra não conta.

### MUT-003: AC-001 / container-engine

- Change: solicitar root, privileged, host mount, device ou engine socket.
- Expected: o mesmo `accept` falha com `AUR-226/AC-001/MUT-003`; restaurado volta a passar; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `supply-chain`, `container-engine` -> “Empacotar servidor Go em imagem OCI” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-226.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: every-hunk review nas dez dimensões, priorizando correção, SOLID, compatibilidade, documentação e sensibilidade dos testes.
- Reviewer B: every-hunk review nas dez dimensões, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 idêntica e selada; processos, sessões, conversations, caches e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas as MUT no profile `release-build-v1`, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-226/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` requer evento humano autenticado. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
