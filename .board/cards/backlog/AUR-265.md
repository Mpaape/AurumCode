---
id: AUR-265
version: 1
title: Expor testqa por MCP read-only
status: backlog
office: O11-mcp
depends_on: [AUR-154, AUR-169, AUR-411]
requirements: [PR-MCP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]
paths: [internal/mcp/tools/read/testqa.go, tests/acceptance/AUR-265.sh, docs/specs/AUR-265.md, tests/unit/AUR-265.go, tests/contracts/AUR-265.go, tests/integration/AUR-265.go, tests/e2e/AUR-265.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, mcp-client]
---

## Outcome

a tool testqa devolve proposta e resultado de sandbox, nunca aplica patch ao worktree consumidor.

## Non-goals

- Aplicar o patch de teste no worktree consumidor ou promover o resultado do sandbox a veredito de aceitação.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-154, AUR-169 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a tool testqa devolve proposta e resultado de sandbox, nunca aplica patch ao worktree consumidor.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Expor testqa por MCP read-only

- Given: os manifests aprovados de AUR-154, AUR-169 estão disponíveis na mesma base, fixture bounded e perfil OCI ativo.
- When: a tool MCP `testqa` é chamada uma vez com `apply=true` e uma vez sem esse parâmetro, ambas sobre a mesma proposta de teste.
- Then: a chamada sem `apply` devolve proposta e resultado do sandbox, a chamada com `apply=true` é rejeitada e o `git status` do worktree consumidor segue vazio na evidência AC-001.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-MCP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]

## Public contract

- Introduz somente “Expor testqa por MCP read-only” nos artefatos `internal/mcp/tools/read/testqa.go`, `tests/acceptance/AUR-265.sh`, `docs/specs/AUR-265.md`.

## TDD proof

- Test: `tests/acceptance/AUR-265.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-265/AC-001: Expor testqa por MCP read-only ausente`; build/loader/infra não valem.
- Green: implementar `internal/mcp/tools/read/testqa.go` expondo somente leitura da proposta e do resultado de sandbox, com schema que rejeita `apply` e sem capability de escrita no worktree.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-265.go::TestAUR265`.
- Contract: `tests/contracts/AUR-265.go::ContractAUR265`.
- Integration: `tests/integration/AUR-265.go::IntegrationAUR265`.
- E2E: `tests/e2e/AUR-265.sh::E2EAUR265`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-265`

Expected artifact: `.board/evidence/AUR-265/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: pedir apply=true em tool read-only.
- Expected: o acceptance inalterado falha com `AUR-265/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / mcp-client

- Change: injetar instrução de elevação num tool result.
- Expected: o acceptance inalterado falha com `AUR-265/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `mcp-client` -> contrato “Expor testqa por MCP read-only” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-265.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
