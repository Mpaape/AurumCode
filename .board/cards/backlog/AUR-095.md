---
id: AUR-095
version: 1
title: Expor aurumcode review na CLI
status: backlog
office: O05-review
depends_on: [AUR-037, AUR-094, AUR-097, AUR-403]
requirements: [PR-ARCH-001, PR-REV-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [cmd/aurumcode/review.go, cmd/aurumcode/review_test.go, tests/acceptance/AUR-095.sh, docs/specs/AUR-095.md, tests/specs/AUR-095, tests/contracts/AUR-095.go, tests/integration/AUR-095.go, tests/unit/AUR-095.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

refs, diff ou stdin geram bundle proposal-only com cancel e erros estáveis.

## Non-goals

- Não expor flags de publicação nem credenciais de SCM neste comando; publicar depende dos cards de publisher e do seu próprio subcomando.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-037, AUR-094, AUR-097 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- refs, diff ou stdin geram bundle proposal-only com cancel e erros estáveis.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Expor aurumcode review na CLI

- Given: `tests/specs/AUR-095/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Expor aurumcode review na CLI”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Expor aurumcode review na CLI” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` para refs, arquivo de diff e stdin, o cancelamento por contexto devolve o código de saída estável documentado e o artifact registra bundle proposal-only sem efeito de publicação.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- Apenas “Expor aurumcode review na CLI” e `cmd/aurumcode/review.go`, `cmd/aurumcode/review_test.go`, `tests/acceptance/AUR-095.sh`, `docs/specs/AUR-095.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-095.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-095/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: implementar `cmd/aurumcode/review.go` com as três entradas de mudança (refs, arquivo de diff e stdin), propagação de cancelamento por contexto e códigos de saída estáveis, encerrando sempre em bundle proposal-only.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“publicar automaticamente quando flag publish não foi fornecida.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-095.go::TestAUR095`.
- Contract: `tests/contracts/AUR-095.go::ContractAUR095`.
- Integration: `tests/integration/AUR-095.go::IntegrationAUR095`.
- E2E: not-applicable: não entrega sozinho use case público.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-095`

Expected artifact: `.board/evidence/AUR-095/acceptance/AC-001.json` valida schema, prova “Expor aurumcode review na CLI” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: publicar automaticamente quando flag publish não foi fornecida.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-095/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-095/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-095/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Expor aurumcode review na CLI” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-095.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: todas as dez dimensões e cada hunk; prioridade em correção, SOLID, compatibilidade e testes.
- Reviewer B: todas as dez dimensões e cada hunk; prioridade em hostilidade, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 igual e selada; processo, sessão, conversation, cache e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas MUT em OCI limpo, restaurar/repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-095/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` requer evento humano autenticado. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
