---
id: AUR-290
version: 1
title: Executar drill de rollback de release
status: backlog
office: O12-release
depends_on: [AUR-198, AUR-201, AUR-412]
requirements: [PR-REL-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-003]
paths: [docs/runbooks/release-rollback, tests/operations/release-rollback, tests/acceptance/AUR-290.sh, docs/specs/AUR-290.md, tests/unit/AUR-290.go, tests/contracts/AUR-290.go, tests/integration/AUR-290.go, tests/e2e/AUR-290.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/release-build-v1.json, .board/locks/oci/release-build-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine, authorization-source]
---

## Outcome

o drill reverte um release sintético para o artifact anterior exato, sem schema downgrade destrutivo.

## Non-goals

- Rollback de release já publicado em registry externo e reversão de migrações de dados aplicadas em produção.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-198, AUR-201 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- o drill reverte um release sintético para o artifact anterior exato, sem schema downgrade destrutivo.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Executar drill de rollback de release

- Given: existem dois releases sintéticos com digests distintos e `docs/runbooks/release-rollback` nomeia o artifact anterior aprovado, sob o profile `release-build-v1`.
- When: o drill de `tests/operations/release-rollback` executa o rollback do release corrente para o artifact anterior aprovado.
- Then: o digest ativo após o rollback é igual ao digest aprovado anterior, nenhum schema é reduzido e a evidence de AC-001 grava digest antes/depois mais a approval humana.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-REL-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-003]

## Public contract

- Somente “Executar drill de rollback de release” em `docs/runbooks/release-rollback`, `tests/operations/release-rollback`, `tests/acceptance/AUR-290.sh`, `docs/specs/AUR-290.md`.

## TDD proof

- Test: `tests/acceptance/AUR-290.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-290/AC-001: Executar drill de rollback de release ausente`; build/loader/infra não valem.
- Green: escrever o runbook `docs/runbooks/release-rollback` e o drill `tests/operations/release-rollback` que fixa os dois digests sintéticos, reverte pelo digest aprovado e checa compatibilidade de schema.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-290.go::TestAUR290`.
- Contract: `tests/contracts/AUR-290.go::ContractAUR290`.
- Integration: `tests/integration/AUR-290.go::IntegrationAUR290`.
- E2E: `tests/e2e/AUR-290.sh::E2EAUR290`.

## Acceptance

container_profile: `release-build-v1`

accept: `./.board/bin/oci-run --profile release-build-v1 --card AUR-290`

Expected artifact: `.board/evidence/AUR-290/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: apontar rollback para digest diferente do aprovado.
- Expected: o acceptance falha com `AUR-290/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance falha com `AUR-290/AC-001/MUT-002`, restore passa; compilação/infra não conta.

### MUT-003: AC-001 / authorization-source

- Change: forjar approval com agent identity.
- Expected: o acceptance falha com `AUR-290/AC-001/MUT-003`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `container-engine`, `authorization-source` -> “Executar drill de rollback de release” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-290.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
