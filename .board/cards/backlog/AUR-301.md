---
id: AUR-301
version: 1
title: Provar delta de cobertura de vinte pontos
status: backlog
validation: tested
profile_owner: AUR-411
office: O09-demo
depends_on: [AUR-144, AUR-184, AUR-411]
requirements: [PR-TST-002, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [tests/corpus/testgen, tests/benchmark/testgen-coverage, tests/acceptance/AUR-301.sh, docs/specs/AUR-301.md, tests/unit/AUR-301.go, tests/contracts/AUR-301.go, tests/integration/AUR-301.go, tests/e2e/AUR-301.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, labelled-corpus, container-engine]
---

## Outcome

a mediana do corpus elegível cresce ao menos 20 pontos percentuais de line coverage em código mudado sem regressão.

## Non-goals

- Melhorar o gerador de testes para alcançar a meta e medir cobertura de branch ou mutação; o card fica na cobertura de linha do código mudado.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-144, AUR-184 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a mediana do corpus elegível cresce ao menos 20 pontos percentuais de line coverage em código mudado sem regressão.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Provar delta de cobertura de vinte pontos

- Given: `tests/corpus/testgen` declara os casos elegíveis com cobertura de linha inicial medida, incluindo casos que já partem de cobertura maior que zero, no profile `polyglot-toolchain-v1`.
- When: o benchmark de `tests/benchmark/testgen-coverage` gera testes para cada caso elegível e remede a cobertura de linha do código mudado.
- Then: a mediana do delta de cobertura sobre os casos elegíveis é de ao menos 20 pontos percentuais e nenhum caso individual registra delta negativo na evidence.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-TST-002, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Somente “Provar delta de cobertura de vinte pontos” em `tests/corpus/testgen`, `tests/benchmark/testgen-coverage`, `tests/acceptance/AUR-301.sh`, `docs/specs/AUR-301.md`.

## TDD proof

- Test: `tests/acceptance/AUR-301.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-301/AC-001: Provar delta de cobertura de vinte pontos ausente`; build/loader/infra não valem.
- Green: versionar o corpus elegível em `tests/corpus/testgen` e implementar em `tests/benchmark/testgen-coverage` a medição antes/depois por caso e o cálculo da mediana do delta.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-301.go::TestAUR301`.
- Contract: `tests/contracts/AUR-301.go::ContractAUR301`.
- Integration: `tests/integration/AUR-301.go::IntegrationAUR301`.
- E2E: `tests/e2e/AUR-301.sh::E2EAUR301`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-301`

Expected artifact: `.board/evidence/AUR-301/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: medir apenas casos com cobertura inicial zero.
- Expected: o acceptance falha com `AUR-301/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / labelled-corpus

- Change: alterar o denominador sem atualizar manifest.
- Expected: o acceptance falha com `AUR-301/AC-001/MUT-002`, restore passa; compilação/infra não conta.

### MUT-003: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance falha com `AUR-301/AC-001/MUT-003`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `labelled-corpus`, `container-engine` -> “Provar delta de cobertura de vinte pontos” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-301.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
