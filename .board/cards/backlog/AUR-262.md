---
id: AUR-262
version: 1
title: Expor repo context por MCP
status: backlog
office: O11-mcp
depends_on: [AUR-169, AUR-210, AUR-404]
requirements: [PR-MCP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]
paths: [internal/mcp/tools/read/repo_context.go, tests/acceptance/AUR-262.sh, docs/specs/AUR-262.md, tests/unit/AUR-262.go, tests/contracts/AUR-262.go, tests/integration/AUR-262.go, tests/e2e/AUR-262.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/go-git-offline-v1.json, .board/locks/oci/go-git-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, mcp-client]
---

## Outcome

a tool repo_context lê contexto bounded do snapshot sem memória obrigatória nem escrita.

## Non-goals

- Consultar ou exigir a camada de memória para enriquecer o contexto retornado pela tool.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-169, AUR-210 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a tool repo_context lê contexto bounded do snapshot sem memória obrigatória nem escrita.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Expor repo context por MCP

- Given: os manifests aprovados de AUR-169, AUR-210 estão disponíveis na mesma base, fixture bounded e perfil OCI ativo.
- When: a tool MCP `repo_context` é chamada uma vez com um path dentro do snapshot, com o backend de memória desligado no perfil.
- Then: o resultado traz trechos truncados no bound declarado, nenhum arquivo do worktree é modificado e `.board/evidence/AUR-262/acceptance/AC-001.json` registra a chamada sem acesso à memória.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-MCP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-002]

## Public contract

- Introduz somente “Expor repo context por MCP” nos artefatos `internal/mcp/tools/read/repo_context.go`, `tests/acceptance/AUR-262.sh`, `docs/specs/AUR-262.md`.

## TDD proof

- Test: `tests/acceptance/AUR-262.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-262/AC-001: Expor repo context por MCP ausente`; build/loader/infra não valem.
- Green: implementar `internal/mcp/tools/read/repo_context.go` resolvendo o path contra a raiz do snapshot, truncando o retorno no bound declarado e abrindo o repositório em modo somente leitura.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-262.go::TestAUR262`.
- Contract: `tests/contracts/AUR-262.go::ContractAUR262`.
- Integration: `tests/integration/AUR-262.go::IntegrationAUR262`.
- E2E: `tests/e2e/AUR-262.sh::E2EAUR262`.

## Acceptance

container_profile: `go-git-offline-v1`

accept: `./.board/bin/oci-run --profile go-git-offline-v1 --card AUR-262`

Expected artifact: `.board/evidence/AUR-262/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: pedir path fora do snapshot por argumento MCP.
- Expected: o acceptance inalterado falha com `AUR-262/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / mcp-client

- Change: injetar instrução de elevação num tool result.
- Expected: o acceptance inalterado falha com `AUR-262/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `mcp-client` -> contrato “Expor repo context por MCP” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-262.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer A: every-hunk review nas dez dimensões, priorizando correção, SOLID, compatibilidade, documentação e sensibilidade dos testes.
- Reviewer B: every-hunk review nas dez dimensões, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 idêntica e selada; processos, sessões, conversations, caches e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas as MUT no profile `go-git-offline-v1`, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-262/`, sanitizado: locked spec/identity, red-green-refactor, gates, clean tree, coverage maps A/B, challenge, mutations, replay, hashes e verdicts. `done` requer evento humano autenticado; nunca segredo, env, prompt/resposta brutos ou chain-of-thought.

