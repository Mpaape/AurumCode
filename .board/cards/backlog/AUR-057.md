---
id: AUR-057
version: 1
title: Definir contrato de transporte HTTP
status: backlog
office: O03-providers
depends_on: [AUR-055, AUR-009, AUR-405]
requirements: [PR-ARCH-001, PR-LLM-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]
paths: [internal/httpx, tests/acceptance/AUR-057.sh, docs/specs/AUR-057.md, tests/specs/AUR-057, tests/contracts/AUR-057.go, tests/integration/AUR-057.go, tests/e2e/AUR-057.sh, tests/unit/AUR-057.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, model-provider, secret-channel]
---

## Outcome

um port HTTP neutro recebe request bounded e devolve response/status tipados, com context cancellation, sem decidir TLS, retry, credencial, provider ou SCM.

## Non-goals

- Não implementa retry, backoff, circuit breaker ou pinning de TLS: `internal/httpx` expõe apenas o contrato de uma requisição e essas políticas ficam nos chamadores.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-055, AUR-009 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- um port HTTP neutro recebe request bounded e devolve response/status tipados, com context cancellation, sem decidir TLS, retry, credencial, provider ou SCM.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Definir contrato de transporte HTTP

- Given: `tests/specs/AUR-057/cases.yaml` traz uma requisição dentro dos limites de corpo e cabeçalhos, uma requisição com contexto já cancelado e uma resposta 302 apontando para `http://169.254.169.254/`.
- When: `tests/acceptance/AUR-057.sh::AC-001` chama o port de `internal/httpx` contra um servidor local determinístico e registra status tipado, bytes lidos, erro devolvido e destino final efetivamente seguido.
- Then: a requisição dentro dos limites devolve status e corpo tipados; o contexto cancelado devolve erro de cancelamento sem ler corpo, e o 302 privado devolve `AUR057_REDIRECT_BLOCKED` sem abrir nova conexão.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-LLM-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]

## Public contract

- Apenas “Definir contrato de transporte HTTP” e `internal/httpx`, `tests/acceptance/AUR-057.sh`, `docs/specs/AUR-057.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-057.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-057/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Definir em `internal/httpx` o port que recebe request com corpo e cabeçalhos limitados, propaga cancelamento por contexto, devolve status e resposta tipados e recusa redirect para destino não público.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“seguir redirect para endereço privado.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-057.go::TestAUR057`.
- Contract: `tests/contracts/AUR-057.go::ContractAUR057`.
- Integration: `tests/integration/AUR-057.go::IntegrationAUR057`.
- E2E: `tests/e2e/AUR-057.sh::E2EAUR057`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-057`

Expected artifact: `.board/evidence/AUR-057/acceptance/AC-001.json` valida schema, prova “Definir contrato de transporte HTTP” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: seguir redirect para endereço privado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-057/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-057/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / secret-channel

- Change: injetar o canário AURUM_SECRET_CANARY na falha devolvida pela fronteira.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-057/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `secret-channel` -> “Definir contrato de transporte HTTP” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-057.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
