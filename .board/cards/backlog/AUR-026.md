---
id: AUR-026
version: 1
title: Definir Finding e Evidence verificáveis
status: backlog
validation: tested
profile_owner: AUR-403
office: O01-core
depends_on: [AUR-025, AUR-014, AUR-015, AUR-016, AUR-017, AUR-403]
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-FIND-001, CR-FIND-002]
paths: [internal/domain/finding, tests/acceptance/AUR-026.sh, docs/specs/AUR-026.md, tests/specs/AUR-026, tests/contracts/AUR-026.go, tests/integration/AUR-026.go, tests/e2e/AUR-026.sh, tests/unit/AUR-026.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

finding possui fingerprint, proveniência e localização validável ou vira observação geral.

## Non-goals

- Este card não produz finding algum nem julga severidade: entrega só a estrutura verificável e o rebaixamento por localização inválida, sem nenhuma regra de análise.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-025, AUR-014, AUR-015, AUR-016, AUR-017 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- finding possui fingerprint, proveniência e localização validável ou vira observação geral.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Definir Finding e Evidence verificáveis

- Given: `tests/specs/AUR-026/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Definir Finding e Evidence verificáveis”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Definir Finding e Evidence verificáveis” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que finding possui fingerprint, proveniência e localização validável ou vira observação geral.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-FIND-001, CR-FIND-002]

## Public contract

- Somente o contrato “Definir Finding e Evidence verificáveis” e os artefatos em `internal/domain/finding`, `tests/acceptance/AUR-026.sh`, `docs/specs/AUR-026.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-026.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-026/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar em `internal/domain/finding` o fingerprint estável e a validação de localização contra o ChangeSet, rebaixando para observação geral todo finding cuja âncora não exista nas linhas tocadas pelo diff.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“aceitar finding inline fora do diff.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-026.go::TestAUR026`.
- Contract: `tests/contracts/AUR-026.go::ContractAUR026`.
- Integration: `tests/integration/AUR-026.go::IntegrationAUR026`.
- E2E: `tests/e2e/AUR-026.sh::E2EAUR026`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-026`

Expected artifact: `.board/evidence/AUR-026/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Definir Finding e Evidence verificáveis” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar finding inline fora do diff.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-026/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Definir Finding e Evidence verificáveis” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-026.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
