---
id: AUR-058
version: 1
title: Resolver credenciais por provider
status: backlog
validation: tested
profile_owner: AUR-405
office: O03-providers
depends_on: [AUR-031, AUR-057, AUR-405]
requirements: [PR-ARCH-001, PR-LLM-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]
paths: [internal/providers/credentials, tests/acceptance/AUR-058.sh, docs/specs/AUR-058.md, tests/specs/AUR-058, tests/contracts/AUR-058.go, tests/integration/AUR-058.go, tests/e2e/AUR-058.sh, tests/unit/AUR-058.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, model-provider, secret-channel]
---

## Outcome

env e identity chains resolvem com precedência explícita sem chegar ao domínio ou logs.

## Non-goals

- Não renova nem faz refresh de credencial expirada: a resolução acontece por chamada e a rotação de token pertence ao card do adapter de provider.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-031, AUR-057 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- env e identity chains resolvem com precedência explícita sem chegar ao domínio ou logs.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Resolver credenciais por provider

- Given: `tests/specs/AUR-058/cases.yaml` declara a chain `env` antes de `identity` para o mesmo provider, um caso com apenas `identity` disponível e um caso sem nenhuma fonte, com o valor sensível só no ambiente do processo.
- When: `tests/acceptance/AUR-058.sh::AC-001` resolve as credenciais por `internal/providers/credentials`, propaga o handle até o struct de domínio e captura stdout, stderr e o erro devolvido.
- Then: com as duas fontes presentes vence `env` conforme a precedência declarada; sem nenhuma fonte sai não-zero com `AUR058_CREDENTIAL_UNRESOLVED`, e stdout, stderr e o struct de domínio não contêm o valor.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-LLM-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-004]

## Public contract

- Apenas “Resolver credenciais por provider” e `internal/providers/credentials`, `tests/acceptance/AUR-058.sh`, `docs/specs/AUR-058.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-058.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-058/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/providers/credentials` resolvendo as chains env e identity na precedência declarada e devolvendo handle opaco cujo valor não aparece em erro, log ou struct do domínio.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“incluir bearer token num erro.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-058.go::TestAUR058`.
- Contract: `tests/contracts/AUR-058.go::ContractAUR058`.
- Integration: `tests/integration/AUR-058.go::IntegrationAUR058`.
- E2E: `tests/e2e/AUR-058.sh::E2EAUR058`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-058`

Expected artifact: `.board/evidence/AUR-058/acceptance/AC-001.json` valida schema, prova “Resolver credenciais por provider” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: incluir bearer token num erro.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-058/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-058/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / secret-channel

- Change: injetar o canário AURUM_SECRET_CANARY na falha devolvida pela fronteira.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-058/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `secret-channel` -> “Resolver credenciais por provider” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-058.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
