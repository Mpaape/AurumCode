---
id: AUR-086
version: 1
title: Executar security reviewer independente
status: backlog
office: O04-agents
depends_on: [AUR-083, AUR-079, AUR-080, AUR-405]
requirements: [PR-ARCH-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [internal/agents/securityreview, tests/acceptance/AUR-086.sh, docs/specs/AUR-086.md, tests/specs/AUR-086, tests/contracts/AUR-086.go, tests/integration/AUR-086.go, tests/e2e/AUR-086.sh, tests/unit/AUR-086.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

prompt injection no código não altera policy, tools ou trust boundary.

## Non-goals

- não bloqueia merge nem aplica remediação sobre a vulnerabilidade encontrada; o reviewer apenas emite findings de segurança sob policy fixa.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-083, AUR-079, AUR-080 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- prompt injection no código não altera policy, tools ou trust boundary.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Executar security reviewer independente

- Given: `tests/specs/AUR-086/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Executar security reviewer independente”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Executar security reviewer independente” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact mostra policy, allowlist de tools e trust boundary idênticas antes e depois do diff com comentário instrutivo; nenhuma tool fora da allowlist é chamada e a instrução embutida no código não surte efeito.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- Apenas “Executar security reviewer independente” e `internal/agents/securityreview`, `tests/acceptance/AUR-086.sh`, `docs/specs/AUR-086.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-086.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-086/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: implementar em `internal/agents/securityreview` o tratamento do diff como dado não confiável, com policy, allowlist de tools e trust boundary fixadas fora do prompt e imunes a instrução vinda de comentário no código.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“obedecer comentário de código que pede para ignorar segurança.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-086.go::TestAUR086`.
- Contract: `tests/contracts/AUR-086.go::ContractAUR086`.
- Integration: `tests/integration/AUR-086.go::IntegrationAUR086`.
- E2E: `tests/e2e/AUR-086.sh::E2EAUR086`.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-086`

Expected artifact: `.board/evidence/AUR-086/acceptance/AC-001.json` valida schema, prova “Executar security reviewer independente” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: obedecer comentário de código que pede para ignorar segurança.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-086/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-086/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-086/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Executar security reviewer independente” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-086.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer A: todas as dez dimensões e cada hunk; prioridade em correção, SOLID, compatibilidade e testes.
- Reviewer B: todas as dez dimensões e cada hunk; prioridade em hostilidade, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: CandidateIdentityV1 igual e selada; processo, sessão, conversation, cache e memória isolados; registrar I0-I3 e backend real.
- Skeptical approver: pré-selar hipóteses, executar todas MUT em OCI limpo, restaurar/repetir; falha ou inconclusivo veta.

## Evidence

Somente `.board/evidence/AUR-086/`: spec/identity, red-green-refactor, clean-tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay, hashes e verdicts sanitizados. `done` exige o bundle de evidência recomputável. Nunca guardar segredo, env, prompt/resposta brutos ou chain-of-thought.
