---
id: AUR-030
version: 1
title: Definir config v3 estrita
status: backlog
office: O01-core
depends_on: [AUR-029, AUR-019, AUR-403]
requirements: [PR-ARCH-001, PR-CFG-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [internal/config, configs/schema, tests/acceptance/AUR-030.sh, docs/specs/AUR-030.md, tests/specs/AUR-030, tests/contracts/AUR-030.go, tests/integration/AUR-030.go, tests/e2e/AUR-030.sh, tests/unit/AUR-030.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

workflows, providers, budgets, skills, memória, toolchains, outputs e segurança rejeitam campos desconhecidos; `memory.mode` tem default explícito `off`.

## Non-goals

- Não converte arquivos v2 existentes nem emite diff de migração: `configs/schema` descreve apenas a forma v3 e a conversão pertence ao card de migração em dry-run.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-029, AUR-019 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- workflows, providers, budgets, skills, memória, toolchains, outputs e segurança rejeitam campos desconhecidos; `memory.mode` tem default explícito `off`.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Definir config v3 estrita

- Given: `tests/specs/AUR-030/cases.yaml` traz um `config.v3.yaml` válido sem `memory.mode`, um vetor com a chave desconhecida `security.allow_all` e um vetor de fronteira com uma nona seção não declarada.
- When: `tests/acceptance/AUR-030.sh::AC-001` decodifica cada vetor pelo loader estrito de `internal/config` contra o schema de `configs/schema` no perfil OCI pinado e registra exit code, erro tipado e digest do effective config.
- Then: o vetor válido sai `0` com `memory.mode: off` no effective config; `security.allow_all` sai não-zero com `AUR030_UNKNOWN_FIELD` nomeando seção e chave, e nenhum arquivo v2 do repo consumidor é reescrito.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-CFG-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Definir config v3 estrita” e os artefatos em `internal/config`, `configs/schema`, `tests/acceptance/AUR-030.sh`, `docs/specs/AUR-030.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-030.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-030/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Escrever o schema estrito em `configs/schema` e o decoder de `internal/config` com rejeição de campo desconhecido nas oito seções, fixando `memory.mode` como `off` quando a chave é omitida.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“permitir campo desconhecido em security.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-030.go::TestAUR030`.
- Contract: `tests/contracts/AUR-030.go::ContractAUR030`.
- Integration: `tests/integration/AUR-030.go::IntegrationAUR030`.
- E2E: `tests/e2e/AUR-030.sh::E2EAUR030`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-030`

Expected artifact: `.board/evidence/AUR-030/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Definir config v3 estrita” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: permitir campo desconhecido em security.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-030/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Definir config v3 estrita” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-030.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
