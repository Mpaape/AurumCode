---
id: AUR-043
version: 1
title: Criar schema SQLite do entity index
status: backlog
validation: tested
profile_owner: AUR-407
office: O02-index
depends_on: [AUR-024, AUR-020, AUR-407]
requirements: [PR-ARCH-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-006]
paths: [internal/index/sqlite, tests/acceptance/AUR-043.sh, docs/specs/AUR-043.md, tests/specs/AUR-043, tests/contracts/AUR-043.go, tests/integration/AUR-043.go, tests/e2e/AUR-043.sh, tests/unit/AUR-043.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, database, git-metadata]
---

## Outcome

snapshots, blobs, files, entities, edges, FTS e tombstones têm constraints e migração reversível em store efêmero ou cache local ignorado do repo consumidor, nunca em artefato Git.

## Non-goals

- Não popula o índice nem executa parsing de código: este card entrega somente o schema e as migrações reversíveis do store SQLite.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-024, AUR-020 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- snapshots, blobs, files, entities, edges, FTS e tombstones têm constraints e migração reversível em store efêmero ou cache local ignorado do repo consumidor, nunca em artefato Git.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Criar schema SQLite do entity index

- Given: `tests/specs/AUR-043/cases.yaml` traz o DDL alvo de snapshots, blobs, files, entities, edges, FTS e tombstones, um insert em `edges` apontando para blob inexistente e o par `0001_up.sql` e `0001_down.sql`.
- When: `tests/acceptance/AUR-043.sh::AC-001` aplica up e down num banco efêmero fora do checkout sob o perfil `sqlite-offline-v1`, compara `sqlite_master` antes e depois e tenta o insert órfão.
- Then: o down restaura `sqlite_master` byte a byte, o insert órfão falha por violação de foreign key com `AUR043_INVALID_INPUT`, e nenhum arquivo de banco aparece entre os paths rastreados pelo Git.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005, CR-TRUST-006]

## Public contract

- Somente o contrato “Criar schema SQLite do entity index” e os artefatos em `internal/index/sqlite`, `tests/acceptance/AUR-043.sh`, `docs/specs/AUR-043.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-043.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-043/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Criar em `internal/index/sqlite` o DDL de snapshots, blobs, files, entities, edges, FTS e tombstones com foreign keys e uniques, mais migrações up e down aplicadas em banco efêmero fora do checkout.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“remover foreign key de entity para blob.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-043.go::TestAUR043`.
- Contract: `tests/contracts/AUR-043.go::ContractAUR043`.
- Integration: `tests/integration/AUR-043.go::IntegrationAUR043`.
- E2E: `tests/e2e/AUR-043.sh::E2EAUR043`.

## Acceptance

container_profile: `sqlite-offline-v1`

accept: `./.board/bin/oci-run --profile sqlite-offline-v1 --card AUR-043`

Expected artifact: `.board/evidence/AUR-043/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Criar schema SQLite do entity index” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover foreign key de entity para blob.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-043/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / database

- Change: corromper o cabeçalho ou exceder o budget do SQLite durante a operação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-043/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-003: AC-001 / git-metadata

- Change: resolver o alvo para a raiz do worktree em vez do Git dir autorizado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-043/AC-001/MUT-003`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `database`, `git-metadata` -> contrato “Criar schema SQLite do entity index” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-043.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
