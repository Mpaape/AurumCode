---
id: AUR-231
version: 1
title: Provar perfil seguro no Docker
status: backlog
office: O08-testqa
depends_on: [AUR-228, AUR-229, AUR-230, AUR-410]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [tests/containers/engines/docker, tests/acceptance/AUR-231.sh, docs/specs/AUR-231.md, tests/unit/AUR-231.go, tests/contracts/AUR-231.go, tests/integration/AUR-231.go, tests/e2e/AUR-231.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/oci-conformance-v1.json, .board/locks/oci/oci-conformance-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

a conformance suite executa no Docker e detecta todos os probes de root, escrita, rede, socket, mount, device, capability e resource escape.

## Non-goals

- Executar a mesma suite sob Podman rootless ou reconciliar diferenças entre engines: a cobertura Podman é entregue por um card próprio.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-228, AUR-229, AUR-230 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- a conformance suite executa no Docker e detecta todos os probes de root, escrita, rede, socket, mount, device, capability e resource escape.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Provar perfil seguro no Docker

- Given: os manifests aprovados de AUR-228, AUR-229 e AUR-230 fixam nonroot, isolamento e limites na mesma base, e um Docker daemon está disponível para o perfil `oci-conformance-v1`.
- When: a suite de `tests/containers/engines/docker` executa, sobre o Docker, cada probe de root, escrita, rede, socket, mount, device, capability e resource escape.
- Then: os oito probes retornam detectado com o código de escape correspondente, e a suite reprova assim que qualquer probe passa sem detecção, gravando o resultado por probe no artefato de aceitação.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Provar perfil seguro no Docker” nos artefatos `tests/containers/engines/docker`, `tests/acceptance/AUR-231.sh`, `docs/specs/AUR-231.md`.

## TDD proof

- Test: `tests/acceptance/AUR-231.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-231/AC-001: Provar perfil seguro no Docker ausente`; build/loader/infra não valem.
- Green: escrever em `tests/containers/engines/docker` o runner que aplica o perfil sob Docker, executa os oito probes e reprova a suite quando algum escape não é detectado.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-231.go::TestAUR231`.
- Contract: `tests/contracts/AUR-231.go::ContractAUR231`.
- Integration: `tests/integration/AUR-231.go::IntegrationAUR231`.
- E2E: `tests/e2e/AUR-231.sh::E2EAUR231`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-231`

Expected artifact: `.board/evidence/AUR-231/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: fazer um probe Docker privilegiado retornar sucesso.
- Expected: o acceptance inalterado falha com `AUR-231/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-231/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Provar perfil seguro no Docker” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-231.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
