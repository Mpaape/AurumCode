---
id: AUR-038
version: 1
title: Aprovar contratos do core
status: backlog
office: O01-core
depends_on: [AUR-023, AUR-024, AUR-025, AUR-026, AUR-027, AUR-028, AUR-029, AUR-030, AUR-031, AUR-032, AUR-033, AUR-034, AUR-035, AUR-036, AUR-403]
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [tests/acceptance/gates/AUR-038, tests/acceptance/AUR-038.sh, docs/specs/AUR-038.md, tests/specs/AUR-038, tests/contracts/AUR-038.go, tests/integration/AUR-038.go, tests/e2e/AUR-038.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

core compila e testa apenas com fakes e schemas públicos versionados.

## Non-goals

- Não corrige as violações de camada que encontrar: o gate reporta o import proibido e a remoção da dependência cabe ao card dono do pacote infrator.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-023, AUR-024, AUR-025, AUR-026, AUR-027, AUR-028, AUR-029, AUR-030, AUR-031, AUR-032, AUR-033, AUR-034, AUR-035, AUR-036 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- core compila e testa apenas com fakes e schemas públicos versionados.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Aprovar contratos do core

- Given: `tests/specs/AUR-038/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Aprovar contratos do core”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Aprovar contratos do core” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que core compila e testa apenas com fakes e schemas públicos versionados.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Aprovar contratos do core” e os artefatos em `tests/acceptance/gates/AUR-038`, `.board/evidence/AUR-038`, `tests/acceptance/AUR-038.sh`, `docs/specs/AUR-038.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-038.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-038/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Adicionar em `tests/acceptance/gates/AUR-038` o gate que compila e testa o core apenas com fakes e falha ao encontrar import de adapter concreto ou schema público sem versão no grafo de dependências.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“adicionar dependência de adapter ao domínio.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: `tests/contracts/AUR-038.go::ContractAUR038`.
- Integration: `tests/integration/AUR-038.go::IntegrationAUR038`.
- E2E: `tests/e2e/AUR-038.sh::E2EAUR038`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-038`

Expected artifact: `.board/evidence/AUR-038/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Aprovar contratos do core” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: adicionar dependência de adapter ao domínio.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-038/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Aprovar contratos do core” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-038.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-038/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige ainda evento humano autenticado para a identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.
