---
id: AUR-223
version: 1
title: Executar parser worker stateless
status: backlog
validation: tested
profile_owner: AUR-406
office: O02-index
depends_on: [AUR-020, AUR-222, AUR-406]
requirements: [PR-ARCH-001, PR-REV-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [internal/parsers/worker, tests/containers/parsers, tests/acceptance/AUR-223.sh, docs/specs/AUR-223.md, tests/unit/AUR-223.go, tests/contracts/AUR-223.go, tests/integration/AUR-223.go, tests/e2e/AUR-223.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/parser-worker-v1.json, .board/locks/oci/parser-worker-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

Um worker JSON/stdin rootless converte bytes em entidades ou fallback limitado sem acessar SQLite, rede ou checkout gravável.

## Non-goals

- Persistir entidades, gravar graph edges ou selecionar contexto de review: o worker de `internal/parsers/worker` apenas devolve o response JSON pelo stdout.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-020, AUR-222 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- Um worker JSON/stdin rootless converte bytes em entidades ou fallback limitado sem acessar SQLite, rede ou checkout gravável.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Executar parser worker stateless

- Given: o worker de `internal/parsers/worker` executa no profile `parser-worker-v1` pinado por `.board/locks/oci/parser-worker-v1.lock.json`, com o blob da fixture montado read-only e sem arquivo SQLite, socket de rede ou checkout gravável no container.
- When: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-223` envia um request JSON pelo stdin do worker `internal/parsers/worker` com o blob da fixture e lê o response único em stdout.
- Then: `.board/evidence/AUR-223/acceptance/AC-001.json` mostra o response único ecoando o request ID enviado, com os spans da fixture nas entidades ou o marcador de fallback e seu motivo, e contagem zero de sockets abertos e de escritas fora de `internal/parsers/worker`.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-ARCH-001, PR-REV-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- Apenas “Executar parser worker stateless” e `internal/parsers/worker`, `tests/containers/parsers`, `tests/acceptance/AUR-223.sh`, `docs/specs/AUR-223.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-223.sh::AC-001`.
- Red: no base, alcança comportamento e falha com `AUR-223/AC-001: Executar parser worker stateless ausente`; compilação, dependência, loader ou ambiente não valem.
- Green: escrever em `internal/parsers/worker` o loop que lê um request JSON por stdin, invoca o parser e emite exatamente um response JSON com o mesmo request ID no stdout, sem abrir conexão, arquivo de banco ou escrita fora do diretório temporário do container.
- Refactor: rerodar AC-001 e perfis, preservando contrato, exit code, ordem e digest.
- Unit: `tests/unit/AUR-223.go::TestAUR223`.
- Contract: `tests/contracts/AUR-223.go::ContractAUR223`.
- Integration: `tests/integration/AUR-223.go::IntegrationAUR223`.
- E2E: `tests/e2e/AUR-223.sh::E2EAUR223`.

## Acceptance

container_profile: `parser-worker-v1`

accept: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-223`

Expected artifact: `.board/evidence/AUR-223/acceptance/AC-001.json` valida schema, prova “Executar parser worker stateless” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar response cujo request ID pertence a outro blob.
- Expected: o mesmo `accept` falha com `AUR-223/AC-001/MUT-001`; restaurado volta a passar; compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` falha com `AUR-223/AC-001/MUT-002`; restaurado volta a passar; compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar thread/cache do peer ou revelar report antes da selagem.
- Expected: o mesmo `accept` falha com `AUR-223/AC-001/MUT-003`; restaurado volta a passar; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Executar parser worker stateless” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-223.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
