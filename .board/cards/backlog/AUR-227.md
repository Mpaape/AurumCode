---
id: AUR-227
version: 1
title: Selecionar engine OCI por contrato
status: backlog
office: O08-testqa
depends_on: [AUR-006, AUR-410]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]
paths: [internal/sandbox/engine/selector.go, internal/sandbox/engine/selector_test.go, tests/acceptance/AUR-227.sh, docs/specs/AUR-227.md, tests/unit/AUR-227.go, tests/contracts/AUR-227.go, tests/integration/AUR-227.go, tests/e2e/AUR-227.sh]
read_paths: [go.mod, go.sum, internal, Dockerfile, .board/oci/profiles/registry.v1.json, .board/oci/profiles/oci-conformance-v1.json, .board/locks/oci/oci-conformance-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, container-engine]
---

## Outcome

um adapter seleciona explicitamente Docker ou Podman disponível, constrói argv sem shell e recusa engine desconhecido sem alterar o perfil de segurança.

## Non-goals

- Construir a imagem, aplicar limites de recurso ou executar o container: este card apenas decide qual engine usar e monta o argv correspondente.
- Publicação e credenciais, salvo quando explicitamente nomeadas no outcome.

## Preconditions

- os manifests aprovados de AUR-006 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- um adapter seleciona explicitamente Docker ou Podman disponível, constrói argv sem shell e recusa engine desconhecido sem alterar o perfil de segurança.
- Nenhum arquivo fora de `paths` ou capability não declarada muda.

## Acceptance scenarios

### AC-001: Selecionar engine OCI por contrato

- Given: os manifests aprovados de AUR-006 estão disponíveis na mesma base, fixture bounded e perfil OCI ativo.
- When: o acceptance invoca `internal/sandbox/engine/selector.go` três vezes: com Docker presente, com apenas Podman presente e com um nome de engine fora do conjunto conhecido.
- Then: o argv retornado é um slice sem interpretação de shell e nomeia o binário docker ou podman efetivamente disponível; o engine desconhecido devolve erro de seleção e nenhum argv, mantendo as flags de segurança do perfil intactas.
- And: replay da mesma CandidateIdentityV1 é estruturalmente idêntico.

- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-005]

## Public contract

- Introduz somente “Selecionar engine OCI por contrato” nos artefatos `internal/sandbox/engine/selector.go`, `internal/sandbox/engine/selector_test.go`, `tests/acceptance/AUR-227.sh`, `docs/specs/AUR-227.md`.

## TDD proof

- Test: `tests/acceptance/AUR-227.sh::AC-001`.
- Red: no base, alcança a ausência do comportamento e falha com `AUR-227/AC-001: Selecionar engine OCI por contrato ausente`; build/loader/infra não valem.
- Green: escrever em `internal/sandbox/engine/selector.go` a detecção ordenada de docker e podman, a montagem de argv como slice literal e o retorno de erro tipado para engine fora da lista conhecida.
- Refactor: rerodar AC-001 preservando API, exit, ordem e artifact digest.
- Unit: `tests/unit/AUR-227.go::TestAUR227`.
- Contract: `tests/contracts/AUR-227.go::ContractAUR227`.
- Integration: `tests/integration/AUR-227.go::IntegrationAUR227`.
- E2E: `tests/e2e/AUR-227.sh::E2EAUR227`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-227`

Expected artifact: `.board/evidence/AUR-227/acceptance/AC-001.json` prova o outcome e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: fazer a seleção aceitar um binário arbitrário vindo do repo.
- Expected: o acceptance inalterado falha com `AUR-227/AC-001/MUT-001`, a restauração passa e erro de compilação/infra não é detecção.

### MUT-002: AC-001 / container-engine

- Change: fazer o engine aceitar privileged, root, host mount, device, socket ou egress.
- Expected: o acceptance inalterado falha com `AUR-227/AC-001/MUT-002`, a restauração passa e erro de compilação/infra não é detecção.

## Security and privacy

- Data flow: `repository`, `container-engine` -> contrato “Selecionar engine OCI por contrato” -> resultado bounded sanitizado.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em qualquer sink.
- Least privilege: somente `paths`; rede, publicação, socket, mounts, devices e capabilities negados salvo contrato explícito.

## Documentation

- `docs/specs/AUR-227.md` inclui contrato, exemplo executável, limites, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: contratos fora deste card permanecem iguais.
- Migration: not-applicable; mudança de legado/estado usa card dedicado.
- Rollback: reverter apenas `paths`, remover derivados card-scoped e provar o base sem efeito externo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
