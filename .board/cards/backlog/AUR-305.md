---
id: AUR-305
version: 1
title: Migrar action.yml legado
status: backlog
office: O14-legacy
depends_on: [AUR-001, AUR-108, AUR-409]
requirements: [PR-SCM-002, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-003]
paths: [action.yml, tests/migration/legacy/action-yml, tests/acceptance/AUR-305.sh, docs/specs/AUR-305.md, tests/unit/AUR-305.go, tests/contracts/AUR-305.go, tests/integration/AUR-305.go, tests/e2e/AUR-305.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/fake-scm-offline-v1.json, .board/locks/oci/fake-scm-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, scm]
---

## Outcome

inputs, outputs e defaults do action.yml são caracterizados e migrados ao contrato v3 com diagnóstico de incompatibilidade.

## Non-goals

- Suporte indefinido ao contrato legado e publicação da action em marketplace; entrega migração com diagnóstico de incompatibilidade.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-001, AUR-108 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- inputs, outputs e defaults do action.yml são caracterizados e migrados ao contrato v3 com diagnóstico de incompatibilidade.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Migrar action.yml legado

- Given: o `action.yml` legado tem inputs, outputs e defaults caracterizados em `tests/migration/legacy/action-yml` e o contrato v3 está publicado, no profile `fake-scm-offline-v1`.
- When: a action migrada é invocada com o conjunto legado de inputs e, depois, com um input que o contrato v3 removeu.
- Then: os inputs suportados devolvem os mesmos outputs do baseline caracterizado e o input incompatível falha com diagnóstico nomeando o campo e sua substituição no v3.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-SCM-002, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001, CR-TRUST-003]

## Public contract

- Somente “Migrar action.yml legado” em `action.yml`, `tests/migration/legacy/action-yml`, `tests/acceptance/AUR-305.sh`, `docs/specs/AUR-305.md`.

## TDD proof

- Test: `tests/acceptance/AUR-305.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-305/AC-001: Migrar action.yml legado ausente`; build/loader/infra não valem.
- Green: reescrever `action.yml` para o contrato v3 mantendo os mapeamentos caracterizados em `tests/migration/legacy/action-yml` e emitindo erro explícito por input legado sem equivalente.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-305.go::TestAUR305`.
- Contract: `tests/contracts/AUR-305.go::ContractAUR305`.
- Integration: `tests/integration/AUR-305.go::IntegrationAUR305`.
- E2E: `tests/e2e/AUR-305.sh::E2EAUR305`.

## Acceptance

container_profile: `fake-scm-offline-v1`

accept: `./.board/bin/oci-run --profile fake-scm-offline-v1 --card AUR-305`

Expected artifact: `.board/evidence/AUR-305/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover input legado sem erro de migração.
- Expected: o acceptance falha com `AUR-305/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / scm

- Change: mudar o head após leitura ou duplicar efeito.
- Expected: o acceptance falha com `AUR-305/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `scm` -> “Migrar action.yml legado” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-305.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
