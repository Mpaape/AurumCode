---
id: AUR-316
version: 1
title: Separar demo legado do consumer repo
status: backlog
validation: tested
profile_owner: AUR-411
office: O14-legacy
depends_on: [AUR-001, AUR-011, AUR-411]
requirements: [PR-DEMO-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [demo, tests/characterization/legacy/demo, tests/acceptance/AUR-316.sh, docs/specs/AUR-316.md, tests/unit/AUR-316.go, tests/contracts/AUR-316.go, tests/integration/AUR-316.go, tests/e2e/AUR-316.sh]
read_paths: [go.mod, go.sum, internal, tests/fixtures, .board/oci/profiles/registry.v1.json, .board/oci/profiles/polyglot-toolchain-v1.json, .board/locks/oci/polyglot-toolchain-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: medium
data_class: internal
trust_boundaries: [repository]
---

## Outcome

bad-code.go e good-code.go permanecem fixtures históricas caracterizadas enquanto demo-repo é criado separadamente.

## Non-goals

- Não criar nem povoar o demo-repo separado; este card só congela o `demo/` legado como fixture e nega seu uso como repositório consumidor.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-001, AUR-011 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- bad-code.go e good-code.go permanecem fixtures históricas caracterizadas enquanto demo-repo é criado separadamente.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Separar demo legado do consumer repo

- Given: os manifests de AUR-001 e AUR-011 estão selados e `demo/bad-code.go` e `demo/good-code.go` estão congelados como fixtures históricas em `tests/characterization/legacy/demo`.
- When: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-316` caracteriza `demo/bad-code.go` e `demo/good-code.go` e tenta usá-los como evidência de clone de consumer.
- Then: `.board/evidence/AUR-316/acceptance/AC-001.json` marca `demo/` como fixture histórica com digest congelado, e o uso de qualquer arquivo sob `demo/` como prova de consumer repo sai 1.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-DEMO-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Separar demo legado do consumer repo” em `demo`, `tests/characterization/legacy/demo`, `tests/acceptance/AUR-316.sh`, `docs/specs/AUR-316.md`.

## TDD proof

- Test: `tests/acceptance/AUR-316.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-316/AC-001: Separar demo legado do consumer repo ausente`; build/loader/infra não valem.
- Green: gravar a caracterização e o digest congelado de `demo/bad-code.go` e `demo/good-code.go` e recusar o papel de consumer repo para qualquer path sob `demo/`.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-316.go::TestAUR316`.
- Contract: `tests/contracts/AUR-316.go::ContractAUR316`.
- Integration: `tests/integration/AUR-316.go::IntegrationAUR316`.
- E2E: `tests/e2e/AUR-316.sh::E2EAUR316`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-316`

Expected artifact: `.board/evidence/AUR-316/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: usar demo legado como prova de consumer clone.
- Expected: o acceptance falha com `AUR-316/AC-001/MUT-001`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository` -> “Separar demo legado do consumer repo” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-316.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
