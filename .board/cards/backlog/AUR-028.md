---
id: AUR-028
version: 1
title: Normalizar erros e exit codes
status: backlog
validation: tested
profile_owner: AUR-403
office: O01-core
depends_on: [AUR-027, AUR-403]
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [internal/domain/failure, internal/application/exitcode, tests/acceptance/AUR-028.sh, docs/specs/AUR-028.md, tests/specs/AUR-028, tests/contracts/AUR-028.go, tests/integration/AUR-028.go, tests/unit/AUR-028.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

classes input, gate, infraestrutura e segurança mapeiam para códigos 0 a 4 sem segredo.

## Non-goals

- Este card não altera o comportamento dos comandos existentes além do código de saída e da classe de erro emitida; política de retry e telemetria de falhas ficam de fora.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-027 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- classes input, gate, infraestrutura e segurança mapeiam para códigos 0 a 4 sem segredo.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Normalizar erros e exit codes

- Given: `tests/specs/AUR-028/cases.yaml` enumera ao menos um erro representativo das classes input, gate, infraestrutura e segurança, cada um com mensagem crua contendo token, caminho absoluto e host, e o exit code esperado dentro de 0 a 4.
- When: `internal/application/exitcode` traduz cada erro tipado de `internal/domain/failure` para código de saída e mensagem final, e o teste captura o par `exit_code` mais o texto realmente emitido em stdout e stderr.
- Then: a tradução é total e sem colisão — nenhuma classe cai em código default e duas classes distintas nunca compartilham código — e o texto emitido não contém token, caminho absoluto nem host; classe sem código, código fora de 0 a 4 ou vazamento na mensagem falha por `AUR-028/AC-001/exitcode-unmapped`.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Normalizar erros e exit codes” e os artefatos em `internal/domain/failure`, `internal/application/exitcode`, `tests/acceptance/AUR-028.sh`, `docs/specs/AUR-028.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-028.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-028/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar em `internal/domain/failure` as classes input, gate, infraestrutura e segurança e em `internal/application/exitcode` o mapeamento total delas para os códigos 0 a 4, com mensagem já redigida e sem detalhe sensível.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“mapear violação de segurança para sucesso.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-028.go::TestAUR028`.
- Contract: `tests/contracts/AUR-028.go::ContractAUR028`.
- Integration: `tests/integration/AUR-028.go::IntegrationAUR028`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-028`

Expected artifact: `.board/evidence/AUR-028/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Normalizar erros e exit codes” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: mapear violação de segurança para sucesso.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-028/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Normalizar erros e exit codes” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-028.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
