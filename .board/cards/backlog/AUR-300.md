---
id: AUR-300
version: 1
title: Provar zero-config em oitenta por cento
status: backlog
office: O09-demo
depends_on: [AUR-094, AUR-184, AUR-403]
requirements: [PR-CFG-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [tests/corpus/zero-config, tests/benchmark/zero-config, tests/acceptance/AUR-300.sh, docs/specs/AUR-300.md, tests/unit/AUR-300.go, tests/contracts/AUR-300.go, tests/integration/AUR-300.go, tests/e2e/AUR-300.sh]
read_paths: [go.mod, go.sum, internal, .board/oci/profiles/registry.v1.json, .board/oci/profiles/go-unit-offline-v1.json, .board/locks/oci/go-unit-offline-v1.lock.json]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository, labelled-corpus]
---

## Outcome

pelo menos 80% do corpus de repos padrão conclui baseline review sem arquivo de config, com denominador versionado.

## Non-goals

- Corrigir os repositórios que falham e acrescentar detecção automática de linguagem nova; o card mede a taxa sobre o denominador declarado.
- Autoridade ou efeito externo não nomeado no outcome.

## Preconditions

- os manifests aprovados de AUR-094, AUR-184 estão disponíveis na mesma base.
- Acceptance selado, árvore limpa e CandidateIdentityV1 completa.

## Postconditions

- pelo menos 80% do corpus de repos padrão conclui baseline review sem arquivo de config, com denominador versionado.
- Fora de `paths`, bytes, estado e capabilities permanecem iguais.

## Acceptance scenarios

### AC-001: Provar zero-config em oitenta por cento

- Given: `tests/corpus/zero-config` fixa um denominador versionado de repositórios padrão e nenhum deles carrega arquivo de configuração do Aurum.
- When: o benchmark de `tests/benchmark/zero-config` roda o baseline review em cada repositório do denominador sem receber configuração.
- Then: a razão entre repositórios concluídos e o denominador versionado é de no mínimo 0,80 e a evidence lista cada repositório com seu veredito, inclusive os que falharam.
- And: replay da identidade exata é estruturalmente idêntico.

- Covers requirements: [PR-CFG-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Somente “Provar zero-config em oitenta por cento” em `tests/corpus/zero-config`, `tests/benchmark/zero-config`, `tests/acceptance/AUR-300.sh`, `docs/specs/AUR-300.md`.

## TDD proof

- Test: `tests/acceptance/AUR-300.sh::AC-001`.
- Red: alcança a ausência do comportamento e falha com `AUR-300/AC-001: Provar zero-config em oitenta por cento ausente`; build/loader/infra não valem.
- Green: versionar o manifest de corpus em `tests/corpus/zero-config` e implementar em `tests/benchmark/zero-config` o runner que executa o baseline review por repo e calcula a razão sobre o denominador fixo.
- Refactor: AC-001 preserva API, exit, ordem e digest.
- Unit: `tests/unit/AUR-300.go::TestAUR300`.
- Contract: `tests/contracts/AUR-300.go::ContractAUR300`.
- Integration: `tests/integration/AUR-300.go::IntegrationAUR300`.
- E2E: `tests/e2e/AUR-300.sh::E2EAUR300`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-300`

Expected artifact: `.board/evidence/AUR-300/acceptance/AC-001.json` prova outcome e CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: remover repos que falham do denominador.
- Expected: o acceptance falha com `AUR-300/AC-001/MUT-001`, restore passa; compilação/infra não conta.

### MUT-002: AC-001 / labelled-corpus

- Change: alterar o denominador sem atualizar manifest.
- Expected: o acceptance falha com `AUR-300/AC-001/MUT-002`, restore passa; compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `labelled-corpus` -> “Provar zero-config em oitenta por cento” -> output bounded sanitizado.
- Secrets: `AURUM_SECRET_CANARY` ausente de stdout, stderr, logs, findings, cache, memória e evidence.
- Least privilege: somente `paths`; demais writes, rede, publicação, socket, mounts, devices e capabilities negados.

## Documentation

- `docs/specs/AUR-300.md` contém contrato, limites, exemplo executável, falhas e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: todo contrato fora deste card é preservado.
- Migration: not-applicable salvo quando o outcome nomeia legado; nesses casos a caracterização é precondição e o artifact registra before/after.
- Rollback: reverter só `paths`, restaurar artifact caracterizado quando aplicável e repetir AC-001 sem efeito externo novo.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
