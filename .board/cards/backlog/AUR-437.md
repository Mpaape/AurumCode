---
id: AUR-437
version: 1
title: Comentar o review no pull request
status: backlog
validation: tested
profile_owner: AUR-006
office: O05-review
depends_on: [AUR-006, AUR-430]
requirements: [PR-ARCH-001, PR-SEC-001]
controls: [CR-GATE-001, CR-TRUST-001]
paths: [internal/git/githubclient, tests/fixtures/scm/github, tests/acceptance/AUR-437.sh, tests/unit/AUR-437.go, tests/integration/AUR-437.go, tests/e2e/AUR-437.sh, docs/specs/AUR-437.md]
read_paths: [go.mod, go.sum]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards, .board/evidence]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: high
data_class: internal
trust_boundaries: [repository]
---

## Outcome

Ao apontar um pull request, o comando le as mudancas dele e publica os problemas encontrados como comentario, e recusa publicar quando nao tem permissao de escrita.

## Restauracao

Este cliente ja existiu no repositorio e foi removido em 2025-11-03 pelo
mesmo lote de commits que apagou o motor de review. Recuperar em vez de
reescrever.

- Origem: commit `c12d7ab` (anterior a exclusao).
- Arquivo: internal/git/githubclient/client.go (774 linhas). COMPILA e passa
  nos testes hoje via httptest, sem rede: GetPullRequestDiff (Accept v3.diff,
  cache ETag), ListChangedFiles (paginacao), PostIssueComment,
  PostReviewComment (ja e inline por path+linha) e SetStatus.
- Defeitos a corrigir na restauracao, todos medidos: o retry de POST reenvia
  o corpo ja consumido (req.Clone reusa o mesmo reader); 403 de rate-limit
  nao e retentado, so 429; time.Sleep ignora o contexto; extractFilePath casa
  o primeiro literal "b/" e erra o caminho em diretorio terminado em "b".
- Regra: restaurar o que ja compila, corrigir os quatro defeitos nomeados, e
  nao reescrever do zero o cliente que ja existe.

## Non-goals

- Qualquer comportamento que o usuario nao consiga observar executando o comando declarado neste card.
- Reescrever pacote existente que ja cumpre a funcao; este card integra o que existe em vez de recriar.

## Preconditions

- As dependencias declaradas estao em `done` e seus artefatos existem no repositorio.
- O aceite roda offline: qualquer chamada a modelo usa fixture deterministica, porque o sandbox nega rede.

## Postconditions

Ao apontar um pull request, o comando le as mudancas dele e publica os problemas encontrados como comentario, e recusa publicar quando nao tem permissao de escrita.

- Fora de `paths`, nenhum byte muda e nenhuma capacidade extra e concedida.

## Acceptance scenarios

### AC-001: Comentar o review no pull request

- Given: a fixture declarada em `paths` e, quando houver modelo, o provider deterministico offline.
- When: o usuario executa `aurumcode review --pr 42 --repo dono/projeto --publicar` dentro do profile pinado.
- Then: a saida entrega exatamente o que o Outcome promete e o exit code e o declarado.
- And: repetir a execucao sobre a mesma entrada produz a mesma saida.

- Covers requirements: [PR-ARCH-001, PR-SEC-001]
- Covers controls: [CR-GATE-001, CR-TRUST-001]

## Public contract

- O comando `aurumcode review --pr 42 --repo dono/projeto --publicar` e o formato da saida que ele produz. Nenhum outro contrato do repositorio muda.

## TDD proof

- Test: `tests/acceptance/AUR-437.sh::AC-001`.
- Red: sem a implementacao o comando nao entrega o resultado prometido e o aceite falha com `AUR-437/AC-001/behavior-missing`. Falha de compilacao, imagem, loader ou rede nao conta como RED.
- Green: implementar o comportamento reutilizando os pacotes ja existentes listados em `read_paths`.
- Refactor: preservar saida, exit codes e digests.
- Unit: `tests/unit/AUR-437.go::TestAUR437`.
- Contract: not-applicable: nenhum port novo e publicado.
- Integration: `tests/integration/AUR-437.go::IntegrationAUR437`.
- E2E: `tests/e2e/AUR-437.sh::E2EAUR437`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-437`

Expected artifact: `.board/evidence/AUR-437/acceptance/AC-001.json` registra o comando executado, o exit code bruto e o digest da saida.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: publicar sem verificar permissao faz o aceite falhar no vetor de token somente leitura.
- Expected: o mesmo `accept` sai diferente de zero registrando `AUR-437/AC-001/MUT-001`, e a restauracao reproduz o GREEN exato.

## Security and privacy

- Data flow: entrada do repositorio -> comando declarado -> saida sanitizada.
- Secrets: o canario `AURUM_SECRET_CANARY` nao aparece em stdout, stderr, prompt, cache, artefato ou evidencia.
- Least privilege: somente `paths` e gravavel; rede, socket do engine, mounts de host, devices e capabilities permanecem negados.

## Documentation

- `docs/specs/AUR-437.md` documenta o comando, suas opcoes, os exit codes, um exemplo executavel sem segredo e o diagnostico de falha.

## Compatibility, migration, rollback

- Compatibility: comandos e formatos ja publicados por outros cards permanecem inalterados.
- Migration: not-applicable.
- Rollback: reverter somente `paths`; nenhum estado externo e publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree, runs the declared command, and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-437/`: comando executado, exit code bruto, digest da saida, resultado da mutacao e verdict. Nunca persistir segredo, env, prompt bruto ou resposta bruta do provider.
