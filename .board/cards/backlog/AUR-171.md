---
id: AUR-171
version: 1
title: Autorizar MCP HTTP com OAuth
status: backlog
office: O11-mcp
depends_on: [AUR-170, AUR-021, AUR-410]
requirements: [PR-ARCH-001, PR-MCP-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-003]
paths: [internal/mcp/http, tests/acceptance/AUR-171.sh, docs/specs/AUR-171.md, tests/fixtures/AUR-171, tests/unit/AUR-171.go, tests/contracts/AUR-171.go, tests/integration/AUR-171.go, tests/e2e/AUR-171.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, mcp-client, secret-channel]
---

## Outcome

`AUR171ResultV1` é o único resultado reversível de “Autorizar MCP HTTP com OAuth” e limita a implementação a `internal/mcp/http`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “aceitar token emitido para outro resource”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-170, AUR-021]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-171` contém exatamente um caso nominal e um contracaso mutado, sob 32 mensagens, 512 KiB total, 5 s e 1 CPU; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR171ResultV1` canônico e o contracaso produz `AUR171_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Autorizar MCP HTTP com OAuth

- Given: CandidateIdentityV1, digests de [AUR-170, AUR-021] e a fixture bounded `tests/fixtures/AUR-171` com caso nominal e “aceitar token emitido para outro resource”.
- When: o consumidor invoca “Autorizar MCP HTTP com OAuth” uma vez no port público card-scoped, com limite de 32 mensagens, 512 KiB total, 5 s e 1 CPU.
- Then: OAuth, PKCE, audience, scopes e token curto recusam replay e wrong audience; o consumidor recebe `AUR171ResultV1` com status e digest canônicos. O contracaso “aceitar token emitido para outro resource” retorna `AUR171_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-MCP-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-003]

## Public contract

- `AUR171ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Autorizar MCP HTTP com OAuth”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-171.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-171/AC-001: AUR171_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR171ResultV1` dentro de 32 mensagens, 512 KiB total, 5 s e 1 CPU, enquanto “aceitar token emitido para outro resource” retorna `AUR171_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-171.go::TestAUR171NominalAndMutated`.
- Contract: `tests/contracts/AUR-171.go::TestAUR171ResultV1`.
- Integration: `tests/integration/AUR-171.go::TestAUR171Boundary`.
- E2E: `tests/e2e/AUR-171.sh::AC-001`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-171`

Expected artifact: o coordenador valida `.board/evidence/AUR-171/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: aceitar token emitido para outro resource.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-171/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / mcp-client

- Change: enviar tool result com instrução para elevar capabilities.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-171/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: injetar AURUM_SECRET_CANARY no erro da fronteira.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-171/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR171ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-171.md` fixa input, output, `AUR171_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR171ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer A: cobre cada hunk e as dez dimensões; prioriza contrato, SOLID, compatibilidade, rollback e sensibilidade dos testes.
- Reviewer B: cobre cada hunk e as dez dimensões; prioriza input hostil, segredos, permissões, supply chain, limites e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 em processo, sessão, conversation, cache e memória isolados; o manifest registra I0-I3 e backend family sem inflar independência.
- Skeptical approver: sela hipóteses antes dos reviews, executa cada MUT em OCI limpo, restaura e repete; sobrevivência, erro de teste ou inconclusivo veta.

## Evidence

Somente o coordenador escreve `.board/evidence/AUR-171/`; builder e testes emitem observações não confiáveis. O bundle contém identity/spec digests, RED/GREEN/refactor, gates, mapas completos A/B, challenge pré-selado, mutações, restore/replay e verdicts sanitizados. `done` exige o bundle de evidência recomputável; segredo, env, prompt/resposta brutos e chain-of-thought são proibidos.
