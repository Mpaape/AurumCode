---
id: AUR-052
version: 1
title: Atualizar índice incrementalmente
status: backlog
validation: tested
profile_owner: AUR-407
office: O02-index
depends_on: [AUR-044, AUR-045, AUR-050, AUR-051, AUR-407]
requirements: [PR-ARCH-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-006]
paths: [internal/index/incremental, tests/acceptance/AUR-052.sh, docs/specs/AUR-052.md, tests/specs/AUR-052, tests/integration/AUR-052.go, tests/e2e/AUR-052.sh, tests/unit/AUR-052.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, database, git-metadata]
---

## Outcome

add, edit, delete, rename, move, split e merge equivalem ao full rebuild; a execução efêmera não deixa o repo consumidor dirty.

## Non-goals

- Não decide quando descartar o índice e reconstruir do zero: este card entrega a aplicação incremental e a política de rebuild completo fica com quem orquestra o índice.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-044, AUR-045, AUR-050, AUR-051 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- add, edit, delete, rename, move, split e merge equivalem ao full rebuild; a execução efêmera não deixa o repo consumidor dirty.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Atualizar índice incrementalmente

- Given: `tests/specs/AUR-052/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Atualizar índice incrementalmente”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Atualizar índice incrementalmente” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que add, edit, delete, rename, move, split e merge equivalem ao full rebuild; a execução efêmera não deixa o repo consumidor dirty.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-006]

## Public contract

- Apenas “Atualizar índice incrementalmente” e `internal/index/incremental`, `tests/acceptance/AUR-052.sh`, `docs/specs/AUR-052.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-052.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-052/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/index/incremental` aplicando add, edit, delete, rename, move, split e merge sobre o snapshot anterior, tombstonando o que desapareceu e gravando tudo em store efêmero fora do checkout.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“não tombstonar entidade deletada.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-052.go::TestAUR052`.
- Contract: not-applicable: nenhum port ou formato externo é criado.
- Integration: `tests/integration/AUR-052.go::IntegrationAUR052`.
- E2E: `tests/e2e/AUR-052.sh::E2EAUR052`.

## Acceptance

container_profile: `sqlite-offline-v1`

accept: `./.board/bin/oci-run --profile sqlite-offline-v1 --card AUR-052`

Expected artifact: `.board/evidence/AUR-052/acceptance/AC-001.json` valida schema, prova “Atualizar índice incrementalmente” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: não tombstonar entidade deletada.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-052/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / database

- Change: corromper o cabeçalho ou exceder o budget do SQLite durante a operação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-052/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / git-metadata

- Change: resolver o alvo para a raiz do worktree em vez do Git dir autorizado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-052/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `database`, `git-metadata` -> “Atualizar índice incrementalmente” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-052.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
