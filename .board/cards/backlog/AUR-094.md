---
id: AUR-094
version: 1
title: Executar review local clean sem memória
status: backlog
validation: tested
profile_owner: AUR-410
office: O05-review
depends_on: [AUR-078, AUR-089, AUR-091, AUR-092, AUR-093, AUR-097, AUR-210, AUR-212, AUR-213, AUR-214, AUR-215, AUR-216, AUR-217, AUR-218, AUR-219, AUR-220, AUR-410]
requirements: [PR-ARCH-001, PR-REV-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [internal/application/review, tests/acceptance/AUR-094.sh, docs/specs/AUR-094.md, tests/specs/AUR-094, tests/contracts/AUR-094.go, tests/integration/AUR-094.go, tests/e2e/AUR-094.sh, tests/unit/AUR-094.go]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

um diff Go limpo de Git local percorre dois passes cegos completos e o gate cético com fake OpenAI-compatible, produz bundle aprovado, não publica, usa memory.mode=off e não cria nem acessa SQLite.

## Non-goals

- Não habilitar memória persistente nem reuso de contexto entre os dois passes; qualquer store SQLite de longo prazo exige card próprio.
- Não agregar resultado implantável independente; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-078, AUR-091, AUR-097, AUR-210, AUR-212, AUR-213, AUR-214, AUR-215, AUR-216, AUR-217, AUR-218, AUR-219, AUR-220 estão disponíveis e vinculados à mesma base.
- Aceite selado, árvore limpa e CandidateIdentityV1 calculada sem valores sensíveis.

## Postconditions

- um diff Go limpo de Git local percorre dois passes cegos completos e o gate cético com fake OpenAI-compatible, produz bundle aprovado, não publica, usa memory.mode=off e não cria nem acessa SQLite.
- Fora de `paths`, bytes e capacidades permanecem inalterados.

## Acceptance scenarios

### AC-001: Executar review local clean sem memória

- Given: `tests/specs/AUR-094/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Executar review local clean sem memória”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Executar review local clean sem memória” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact registra os dois passes cegos e o gate cético concluídos contra o fake OpenAI-compatible, `memory.mode=off`, zero arquivo SQLite aberto ou criado e nenhuma chamada de publicação.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-REV-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- Apenas “Executar review local clean sem memória” e `internal/application/review`, `tests/acceptance/AUR-094.sh`, `docs/specs/AUR-094.md`; formatos e consumidores não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-094.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-094/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: implementar em `internal/application/review` a orquestração que roda os dois passes cegos e o gate cético contra o fake OpenAI-compatible, força `memory.mode=off`, não abre arquivo SQLite algum e encerra no bundle aprovado sem publicar.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“tratar provider-fail como review vazio aprovado.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-094.go::TestAUR094`.
- Contract: `tests/contracts/AUR-094.go::ContractAUR094`.
- Integration: `tests/integration/AUR-094.go::IntegrationAUR094`.
- E2E: `tests/e2e/AUR-094.sh::E2EAUR094`.

## Acceptance

container_profile: `oci-conformance-v1`

accept: `./.board/bin/oci-run --profile oci-conformance-v1 --card AUR-094`

Expected artifact: `.board/evidence/AUR-094/acceptance/AC-001.json` valida schema, prova “Executar review local clean sem memória” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: tratar provider-fail como review vazio aprovado.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-094/AC-001/MUT-001`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-002: AC-001 / model-provider

- Change: fazer o fake provider retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-094/AC-001/MUT-002`, a restauração volta a passar e falha de compilação/infra não conta.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar a thread do outro reviewer ou revelar seu report antes da selagem.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-094/AC-001/MUT-003`, a restauração volta a passar e falha de compilação/infra não conta.

## Security and privacy

- Data flow: `repository`, `model-provider`, `review-isolation` -> “Executar review local clean sem memória” -> artefato sanitizado card-scoped.
- Secrets: `AURUM_SECRET_CANARY` fica ausente de todo sink.
- Least privilege: só `paths` é gravável; rede, engine socket, host/device mounts, capabilities e publicação são negados salvo declaração explícita.

## Documentation

- `docs/specs/AUR-094.md` descreve comportamento, contrato, limites, exemplo executável sem segredo e diagnóstico.

## Compatibility, migration, rollback

- Compatibility: tudo fora do Public contract é preservado e diferenças falham AC-001.
- Migration: not-applicable; estado/legado exige card próprio.
- Rollback: reverter só `paths`, remover derivados card-scoped e repetir AC-001; sem publicação externa.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
