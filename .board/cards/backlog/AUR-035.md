---
id: AUR-035
version: 1
title: Calcular CandidateIdentityV1 canônica
status: backlog
office: O01-core
depends_on: [AUR-034, AUR-404]
requirements: [PR-ARCH-001, PR-EVD-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [internal/application/idempotency, tests/acceptance/AUR-035.sh, docs/specs/AUR-035.md, tests/specs/AUR-035, tests/contracts/AUR-035.go, tests/integration/AUR-035.go, tests/e2e/AUR-035.sh, tests/unit/AUR-035.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

um único value object calcula o digest de repository/base/head/change, spec, config, policy, prompt/rubric, skills, provider-model-backend, tools/toolchain, dependency lock, images, tests e role-context; qualquer byte alterado muda a identidade.

## Non-goals

- Não decide reuso, cache hit ou dedupe de execução a partir do digest: o value object apenas calcula a identidade e a política que a consome vive em card dependente.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-034 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- um único value object calcula o digest de repository/base/head/change, spec, config, policy, prompt/rubric, skills, provider-model-backend, tools/toolchain, dependency lock, images, tests e role-context; qualquer byte alterado muda a identidade.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Calcular CandidateIdentityV1 canônica

- Given: `tests/specs/AUR-035/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Calcular CandidateIdentityV1 canônica”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Calcular CandidateIdentityV1 canônica” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que um único value object calcula o digest de repository/base/head/change, spec, config, policy, prompt/rubric, skills, provider-model-backend, tools/toolchain, dependency lock, images, tests e role-context; qualquer byte alterado muda a identidade.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Calcular CandidateIdentityV1 canônica” e os artefatos em `internal/application/idempotency`, `tests/acceptance/AUR-035.sh`, `docs/specs/AUR-035.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-035.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-035/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar em `internal/application/idempotency` o value object que serializa canonicamente repository/base/head/change, spec, config, policy, prompt/rubric, skills, provider-model-backend, tools, lock, images, tests e role-context em um digest estável.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“omitir head SHA da chave.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-035.go::TestAUR035`.
- Contract: `tests/contracts/AUR-035.go::ContractAUR035`.
- Integration: `tests/integration/AUR-035.go::IntegrationAUR035`.
- E2E: `tests/e2e/AUR-035.sh::E2EAUR035`.

## Acceptance

container_profile: `go-git-offline-v1`

accept: `./.board/bin/oci-run --profile go-git-offline-v1 --card AUR-035`

Expected artifact: `.board/evidence/AUR-035/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Calcular CandidateIdentityV1 canônica” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: omitir head SHA da chave.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-035/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Calcular CandidateIdentityV1 canônica” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-035.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
