---
id: AUR-375
version: 1
title: Caracterizar pipeline docs batch raiz
status: backlog
office: O14-legacy
depends_on: [AUR-001, AUR-233, AUR-412]
requirements: [PR-SEC-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [run-docs-pipeline.bat, tests/characterization/legacy/script-files/AUR-375, tests/acceptance/AUR-375.sh, docs/specs/AUR-375.md]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, container-engine, secret-channel]
---

## Outcome

o manifesto registra `run-docs-pipeline.bat`, source SHA-256, formato `batch`, interpreter/tool tokens, inputs, effects, disposition e `commands_executed=0`.

## Non-goals

- Interpretar `run-docs-pipeline.bat` sob Windows ou provar equivalência com a variante shell; o card tokeniza o dialeto batch estaticamente e registra o formato `batch`.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.

## Preconditions

- As dependências `[AUR-001, AUR-233]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/characterization/legacy/script-files/AUR-375/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- o manifesto registra `run-docs-pipeline.bat`, source SHA-256, formato `batch`, interpreter/tool tokens, inputs, effects, disposition e `commands_executed=0`.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Caracterizar pipeline docs batch raiz

- Given: `tests/characterization/legacy/script-files/AUR-375/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 1 MiB, 512 linhas lógicas, 128 tokens de comando e 10 s.
- When: o leitor estático resolve `run-docs-pipeline.bat` como arquivo regular contido, tokeniza batch sem subprocesso e compara o manifesto selado.
- Then: o nominal retorna `pass` e o manifesto registra `run-docs-pipeline.bat`, source SHA-256, formato `batch`, interpreter/tool tokens, inputs, effects, disposition e `commands_executed=0`; ausência, tamper/symlink, campo inválido e over-limit retornam respectivamente `legacy_source_uncharacterized`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded` antes de efeito externo.
- Covers requirements: [PR-SEC-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `legacy_source_uncharacterized|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-375.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-375/AC-001/legacy_source_uncharacterized`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir a caracterização estática de `run-docs-pipeline.bat` com source hash, tokens/effects bounded, disposition explícita e zero execução.
- Refactor: após GREEN, normalizar tokens sem alterar source SHA, effect set, disposition, zero-execution ou manifest digest; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-375.sh::ContractAUR375`.
- Integration: `tests/acceptance/AUR-375.sh::IntegrationAUR375`.
- E2E: `tests/acceptance/AUR-375.sh::E2EAUR375`.

## Acceptance

container_profile: `release-build-v1`

accept: `./.board/bin/oci-run --profile release-build-v1 --card AUR-375`

Expected artifact: o coordinator deriva `.board/evidence/AUR-375/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: substituir `run-docs-pipeline.bat` por symlink externo depois do source lock.
- Expected: o aceite sai 1 com `AUR-375/AC-001/source-escape` antes de ler fora do repositório; restore recupera o SHA-256.

### MUT-002: AC-001 / container-engine

- Change: alterar a fixture para permitir a execução do arquivo durante a caracterização.
- Expected: o canário de processo torna `commands_executed=1` e o aceite sai 1 com `AUR-375/AC-001/source-executed`; restore volta a zero.

### MUT-003: AC-001 / secret-channel

- Change: persistir o valor do canário em vez de somente sua classe de referência.
- Expected: o sink scan sai 1 com `AUR-375/AC-001/secret-value` sem imprimir o valor; restore persiste somente contagens.

## Security and privacy

- Data flow: comandos Windows, variáveis, ferramentas e writes do pipeline batch -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-375.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: a disposition `keep|migrate|replace|retire` é obrigatória antes de qualquer alteração do arquivo legado.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer A: cada hunk e as dez dimensões do protocolo, priorizando correção, SOLID, compatibilidade, determinismo e sensibilidade dos testes.
- Reviewer B: cada hunk e as dez dimensões do protocolo, priorizando entradas hostis, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 selada, mas usam processos, sessões, conversas, caches, memória e relatórios separados; registrar backend real e nível I0/I1/I2/I3.
- Skeptical approver: pré-sela hipóteses, executa todas as mutações em OCI limpo, restaura e repete o aceite; falha, divergência ou inconclusivo veta.

## Evidence

Somente o coordinator grava artefatos sanitizados em `.board/evidence/AUR-375/`: spec/identity seladas, red-green-refactor, clean tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay restaurado, hashes e verdicts. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.
