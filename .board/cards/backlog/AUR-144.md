---
id: AUR-144
version: 1
title: Gerar e verificar unit tests Go
status: backlog
office: O08-testqa
depends_on: [AUR-131, AUR-132, AUR-133, AUR-134, AUR-135, AUR-136, AUR-137, AUR-138, AUR-139, AUR-140, AUR-141, AUR-142, AUR-143, AUR-411]
requirements: [PR-ARCH-001, PR-TST-001, PR-TST-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-005, CR-COV-005]
paths: [internal/application/testgen/go, tests/acceptance/AUR-144.sh, docs/specs/AUR-144.md, tests/fixtures/AUR-144, tests/unit/AUR-144.go, tests/contracts/AUR-144.go, tests/integration/AUR-144.go, tests/e2e/AUR-144.sh]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, container-engine, filesystem]
---

## Outcome

`AUR144ResultV1` é um manifesto determinístico que decide “Gerar e verificar unit tests Go” somente a partir de digests e verdicts dos produtores dependentes; este card não reexecuta nem reúne comportamentos implementáveis de forma independente.

## Non-goals

- Não relaxar o contrato para aceitar “retornar patch antes do sandbox”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-131, AUR-132, AUR-133, AUR-134, AUR-135, AUR-136, AUR-137, AUR-138, AUR-139, AUR-140, AUR-141, AUR-142, AUR-143]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-144` contém exatamente um caso nominal e um contracaso mutado, sob 32 arquivos, 4 MiB total, 60 s e 2 CPUs; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR144ResultV1` canônico e o contracaso produz `AUR144_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Gerar e verificar unit tests Go

- Given: CandidateIdentityV1, digests de [AUR-131, AUR-132, AUR-133, AUR-134, AUR-135, AUR-136, AUR-137, AUR-138, AUR-139, AUR-140, AUR-141, AUR-142, AUR-143] e a fixture bounded `tests/fixtures/AUR-144` com caso nominal e “retornar patch antes do sandbox”.
- When: o gate de `internal/application/testgen/go` percorre em ordem lexicográfica os 13 manifests selados de geração de unit tests Go e confere path e digest de cada um sob `polyglot-toolchain-v1`, sem reexecutar `go test`, dentro de 32 arquivos, 4 MiB, 60 s e 2 CPUs.
- Then: o coordenador recebe `AUR144ResultV1` com uma linha por input, aggregate digest e verdict tipado; o contracaso “retornar patch antes do sandbox” retorna `AUR144_INVALID_INPUT` sem manifesto aprovado.
- Covers requirements: [PR-ARCH-001, PR-TST-001, PR-TST-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-005, CR-COV-005]

## Public contract

- `AUR144ResultV1` contém apenas input IDs, digests, verdicts, aggregate digest e erro tipado; não contém payload, comportamento reexecutado, segredo ou autoridade humana.

## TDD proof

- Test: `tests/acceptance/AUR-144.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-144/AC-001: AUR144_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR144ResultV1` dentro de 32 arquivos, 4 MiB total, 60 s e 2 CPUs, enquanto “retornar patch antes do sandbox” retorna `AUR144_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-144.go::TestAUR144NominalAndMutated`.
- Contract: `tests/contracts/AUR-144.go::TestAUR144ResultV1`.
- Integration: `tests/integration/AUR-144.go::TestAUR144Boundary`.
- E2E: `tests/e2e/AUR-144.sh::AC-001`.

## Acceptance

container_profile: `polyglot-toolchain-v1`

accept: `./.board/bin/oci-run --profile polyglot-toolchain-v1 --card AUR-144`

Expected artifact: o coordenador valida `.board/evidence/AUR-144/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: retornar patch antes do sandbox.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-144/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / container-engine

- Change: solicitar root, privileged, host mount, device ou engine socket.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-144/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / filesystem

- Change: trocar o destino por symlink que escape do staging.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-144/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR144ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-144.md` fixa input, output, `AUR144_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR144ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
