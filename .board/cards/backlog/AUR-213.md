---
id: AUR-213
version: 1
title: Selar snapshot e contexto por papel
status: backlog
office: O04-agents
depends_on: [AUR-035, AUR-036, AUR-083, AUR-210, AUR-009, AUR-405]
requirements: [PR-ARCH-001, PR-REV-003]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]
paths: [internal/agents/contextbroker, internal/review/snapshot, tests/acceptance/AUR-213.sh, docs/specs/AUR-213.md, tests/fixtures/AUR-213, tests/unit/AUR-213.go, tests/contracts/AUR-213.go, tests/integration/AUR-213.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository, model-provider, review-isolation]
---

## Outcome

`AUR213ResultV1` é o único resultado reversível de “Selar snapshot e contexto por papel” e limita a implementação a `internal/agents/contextbroker`, `internal/review/snapshot`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “incluir ReviewR1 no contexto entregue a R2”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-035, AUR-036, AUR-083, AUR-210, AUR-009]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-213` contém exatamente um caso nominal e um contracaso mutado, sob 16 contextos, 2 MiB total, 10 s e 1 CPU; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR213ResultV1` canônico e o contracaso produz `AUR213_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Selar snapshot e contexto por papel

- Given: CandidateIdentityV1, digests de [AUR-035, AUR-036, AUR-083, AUR-210, AUR-009] e a fixture bounded `tests/fixtures/AUR-213` com caso nominal e “incluir ReviewR1 no contexto entregue a R2”.
- When: o consumidor invoca “Selar snapshot e contexto por papel” uma vez no port público card-scoped, com limite de 16 contextos, 2 MiB total, 10 s e 1 CPU.
- Then: cada role recebe snapshot imutável allowlisted e nenhum reviewer vê trace do autor ou report peer; o consumidor recebe `AUR213ResultV1` com status e digest canônicos. O contracaso “incluir ReviewR1 no contexto entregue a R2” retorna `AUR213_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-REV-003]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-COV-001, CR-COV-003, CR-COV-004, CR-COV-005]

## Public contract

- `AUR213ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Selar snapshot e contexto por papel”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-213.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-213/AC-001: AUR213_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR213ResultV1` dentro de 16 contextos, 2 MiB total, 10 s e 1 CPU, enquanto “incluir ReviewR1 no contexto entregue a R2” retorna `AUR213_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-213.go::TestAUR213NominalAndMutated`.
- Contract: `tests/contracts/AUR-213.go::TestAUR213ResultV1`.
- Integration: `tests/integration/AUR-213.go::TestAUR213Boundary`.
- E2E: not-applicable: o contrato atômico de AUR-213 é totalmente observado no port e o fluxo consumidor pertence a card dependente.

## Acceptance

container_profile: `fake-provider-v1`

accept: `./.board/bin/oci-run --profile fake-provider-v1 --card AUR-213`

Expected artifact: o coordenador valida `.board/evidence/AUR-213/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: incluir ReviewR1 no contexto entregue a R2.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-213/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / model-provider

- Change: retornar schema inválido seguido de timeout.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-213/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / review-isolation

- Change: reutilizar thread/cache do peer ou revelar report antes da selagem.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-213/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR213ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-213.md` fixa input, output, `AUR213_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR213ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer A: cobre cada hunk e as dez dimensões; prioriza contrato, SOLID, compatibilidade, rollback e sensibilidade dos testes.
- Reviewer B: cobre cada hunk e as dez dimensões; prioriza input hostil, segredos, permissões, supply chain, limites e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 em processo, sessão, conversation, cache e memória isolados; o manifest registra I0-I3 e backend family sem inflar independência.
- Skeptical approver: sela hipóteses antes dos reviews, executa cada MUT em OCI limpo, restaura e repete; sobrevivência, erro de teste ou inconclusivo veta.

## Evidence

Somente o coordenador escreve `.board/evidence/AUR-213/`; builder e testes emitem observações não confiáveis. O bundle contém identity/spec digests, RED/GREEN/refactor, gates, mapas completos A/B, challenge pré-selado, mutações, restore/replay e verdicts sanitizados. `done` requer evento humano autenticado; segredo, env, prompt/resposta brutos e chain-of-thought são proibidos.
