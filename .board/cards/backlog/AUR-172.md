---
id: AUR-172
version: 1
title: Bloquear prompt e tool injection via MCP
status: backlog
validation: tested
profile_owner: AUR-406
office: O11-mcp
depends_on: [AUR-169, AUR-170, AUR-009, AUR-406]
requirements: [PR-ARCH-001, PR-MCP-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-003]
paths: [internal/mcp/security, tests/acceptance/AUR-172.sh, docs/specs/AUR-172.md, tests/fixtures/AUR-172, tests/unit/AUR-172.go, tests/contracts/AUR-172.go, tests/integration/AUR-172.go, tests/e2e/AUR-172.sh]
read_paths: []
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: confidential
trust_boundaries: [repository, mcp-client, secret-channel]
---

## Outcome

`AUR172ResultV1` é o único resultado reversível de “Bloquear prompt e tool injection via MCP” e limita a implementação a `internal/mcp/security`; qualquer comportamento irmão exige outro card.

## Non-goals

- Não relaxar o contrato para aceitar “converter descrição de tool em política confiável”, nem incorporar operações, adapters ou autoridade pertencentes a cards dependentes.

## Preconditions

- O coordenador fornece CandidateIdentityV1 selada e o índice de digests para [AUR-169, AUR-170, AUR-009]; conteúdo bruto de evidence não entra na fixture.
- `tests/fixtures/AUR-172` contém exatamente um caso nominal e um contracaso mutado, sob 32 mensagens, 512 KiB total, 5 s e 1 CPU; rede e relógio externo ficam negados.

## Postconditions

- A fixture nominal produz um único `AUR172ResultV1` canônico e o contracaso produz `AUR172_INVALID_INPUT` sem output aprovado.
- Dois replays preservam status, ordem e digest; paths não declarados e manifests de dependência permanecem inalterados.

## Acceptance scenarios

### AC-001: Bloquear prompt e tool injection via MCP

- Given: CandidateIdentityV1, digests de [AUR-169, AUR-170, AUR-009] e a fixture bounded `tests/fixtures/AUR-172` com caso nominal e “converter descrição de tool em política confiável”.
- When: o consumidor invoca “Bloquear prompt e tool injection via MCP” uma vez no port público card-scoped, com limite de 32 mensagens, 512 KiB total, 5 s e 1 CPU.
- Then: repo, skill e memória maliciosos não elevam autoridade nem exfiltram dados; o consumidor recebe `AUR172ResultV1` com status e digest canônicos. O contracaso “converter descrição de tool em política confiável” retorna `AUR172_INVALID_INPUT` sem saída parcial nem escrita fora do ownership.
- Covers requirements: [PR-ARCH-001, PR-MCP-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-002, CR-TRUST-003]

## Public contract

- `AUR172ResultV1` expõe status, output digest, erro tipado e contadores bounded para “Bloquear prompt e tool injection via MCP”; interfaces e formatos não citados permanecem byte-compatíveis.

## TDD proof

- Test: `tests/acceptance/AUR-172.sh::AC-001`.
- Red: no base selado, a fixture nominal alcança o port e o processo retorna exit 1 com `AUR-172/AC-001: AUR172_CONTRACT_ABSENT`; erro de compilação, carregamento, permissão ou runtime OCI é `test_error`, nunca RED.
- Green: o caso nominal retorna `AUR172ResultV1` dentro de 32 mensagens, 512 KiB total, 5 s e 1 CPU, enquanto “converter descrição de tool em política confiável” retorna `AUR172_INVALID_INPUT`; nenhum comportamento fora do Public contract é acrescentado.
- Refactor: duas execuções nominais mantêm status, ordem e digest, a mutação continua detectada e os limites de tamanho, tempo e recursos não aumentam.
- Unit: `tests/unit/AUR-172.go::TestAUR172NominalAndMutated`.
- Contract: `tests/contracts/AUR-172.go::TestAUR172ResultV1`.
- Integration: `tests/integration/AUR-172.go::TestAUR172Boundary`.
- E2E: `tests/e2e/AUR-172.sh::AC-001`.

## Acceptance

container_profile: `parser-worker-v1`

accept: `./.board/bin/oci-run --profile parser-worker-v1 --card AUR-172`

Expected artifact: o coordenador valida `.board/evidence/AUR-172/acceptance/AC-001.json` contra o schema de acceptance result, vincula CandidateIdentityV1 e registra somente status, códigos, contadores e digests sanitizados.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: converter descrição de tool em política confiável.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-172/AC-001/MUT-001` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-002: AC-001 / mcp-client

- Change: enviar tool result com instrução para elevar capabilities.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-172/AC-001/MUT-002` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

### MUT-003: AC-001 / secret-channel

- Change: injetar AURUM_SECRET_CANARY no erro da fronteira.
- Expected: o mesmo `accept` retorna exit 1 e `AUR-172/AC-001/MUT-003` sem result digest; após restore, dois replays produzem o digest nominal. Erro de compilação, carregamento da fixture, permissão ou runtime OCI é `test_error`, nunca evidência da mutação.

## Security and privacy

- Data flow: fixture bounded -> port card-scoped -> `AUR172ResultV1` sanitizado; payload bruto e valores de ambiente não alcançam evidence.
- Secrets: o canário sintético `AURUM_SECRET_CANARY` deve permanecer ausente de stdout, stderr, logs, manifests e digests reutilizáveis.
- Least privilege: checkout read-only salvo `paths`; rede, engine socket, mounts de host/device, capabilities, credenciais e publicação ficam negados.

## Documentation

- `docs/specs/AUR-172.md` fixa input, output, `AUR172_INVALID_INPUT`, bounds, fixture, rollback e um exemplo sem credencial.

## Compatibility, migration, rollback

- Compatibility: `AUR172ResultV1` v1 é o único contrato novo; consumidores e formatos fora do card permanecem inalterados.
- Migration: não há conversão implícita de estado; qualquer dado legado exige dry-run e card próprio.
- Rollback: remover somente artefatos em `paths`; manifests dependentes, checkout consumidor e efeitos externos permanecem intactos.

## Review

- Reviewer: one independent reviewer reads the exact immutable candidate in a clean detached worktree and checks every changed hunk, the public contract, paths/read_paths, acceptance, declared selectors, mutations, profile/runtime compatibility, security boundaries, and exit-code handling.
- Independence: the reviewer is separate from the builder and does not edit the candidate or write evidence in the coordinator checkout. No extra review role is required.
- Verdict: approve only on raw exit 0 with real assertions. Runtime, loader, image, dependency, or other infrastructure failures are blocked or inconclusive, never approval. The validator reruns the declared acceptance on the same SHA when validation is not none.

## Evidence

Only the coordinator writes sanitized evidence under .board/evidence/<card-id>/; tests and builders emit observations only. Evidence binds the exact candidate SHA, review verdict, acceptance/validation exits, mutation result when declared, clean tree, and artifact hashes. Never store secrets, raw prompts/responses, credentials, environment values, or chain-of-thought.
