---
id: AUR-032
version: 1
title: Aplicar precedência de config com base confiável
status: backlog
office: O01-core
depends_on: [AUR-030, AUR-031, AUR-403]
requirements: [PR-ARCH-001, PR-CFG-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [internal/config/precedence.go, internal/config/precedence_test.go, tests/acceptance/AUR-032.sh, docs/specs/AUR-032.md, tests/specs/AUR-032, tests/contracts/AUR-032.go, tests/integration/AUR-032.go, tests/e2e/AUR-032.sh, tests/unit/AUR-032.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: lock-at-execution
spec_digest: lock-at-execution
risk: critical
data_class: internal
trust_boundaries: [repository]
---

## Outcome

defaults, organização, base branch e flags não sensíveis são mesclados sem elevação pelo head.

## Non-goals

- Não valida schema das camadas nem resolve referências de segredo: `precedence.go` recebe estruturas já decodificadas e devolve somente a config efetiva mesclada.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- os manifests aprovados de AUR-030, AUR-031 estão disponíveis e vinculados à mesma base.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- defaults, organização, base branch e flags não sensíveis são mesclados sem elevação pelo head.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Aplicar precedência de config com base confiável

- Given: `tests/specs/AUR-032/cases.yaml` contém vetores `nominal`, `invalid` e `boundary` para “Aplicar precedência de config com base confiável”, cada um com digest de entrada, saída ou erro esperado e limites do domínio; o harness rejeita mais de 64 vetores, 4 MiB totais ou deadline acima de 30 s.
- When: AC-001 executa “Aplicar precedência de config com base confiável” para cada vetor no perfil OCI pinado e captura `exit_code`, código de resultado ou erro, contagem de efeitos e digest do artifact.
- Then: o vetor `nominal` sai `0` e o artifact prova que defaults, organização, base branch e flags não sensíveis são mesclados sem elevação pelo head.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-CFG-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Aplicar precedência de config com base confiável” e os artefatos em `internal/config/precedence.go`, `internal/config/precedence_test.go`, `tests/acceptance/AUR-032.sh`, `docs/specs/AUR-032.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-032.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-032/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Implementar `internal/config/precedence.go` com a ordem defaults, organização, base branch e flags, descartando do head toda chave marcada como sensível ou capaz de elevar permissão antes do merge.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“permitir head branch habilitar publicação.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: `tests/unit/AUR-032.go::TestAUR032`.
- Contract: `tests/contracts/AUR-032.go::ContractAUR032`.
- Integration: `tests/integration/AUR-032.go::IntegrationAUR032`.
- E2E: `tests/e2e/AUR-032.sh::E2EAUR032`.

## Acceptance

container_profile: `go-unit-offline-v1`

accept: `./.board/bin/oci-run --profile go-unit-offline-v1 --card AUR-032`

Expected artifact: `.board/evidence/AUR-032/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Aplicar precedência de config com base confiável” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: permitir head branch habilitar publicação.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-032/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository` -> contrato “Aplicar precedência de config com base confiável” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-032.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-032/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige ainda evento humano autenticado para a identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

