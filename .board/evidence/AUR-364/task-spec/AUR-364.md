---
id: AUR-364
version: 1
title: Fixar ferramentas de documentação
status: done
office: O00-security
depends_on: [AUR-359]
requirements: [PR-SUP-001, PR-ARCH-001]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]
paths: [.board/bootstrap/locks/docs.yml, tests/bootstrap/locks/AUR-364, tests/acceptance/AUR-364.sh, docs/specs/AUR-364.md]
read_paths: [Gemfile, .aurumcode/Gemfile, .docker/docs.Dockerfile, .github/workflows/documentation.yml]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: 8b2742326c15eb8a670e053a26d825071bfc28bf
spec_digest: sha256:c79e8ec3462505edd1e62c0186ea667e971b948063403b8b4cb3b9e3373d8005
risk: critical
data_class: internal
trust_boundaries: [repository, documentation-tool, supply-chain]
---

## Outcome

cada ferramenta possui versão e artifact digest, runtime resolvido de forma consistente, comando allowlisted e output schema; nenhuma instalação ocorre durante o aceite.

## Non-goals

- Gerar o site de documentação ou instalar Jekyll e Pagefind; o card fixa versão, digest, runtime e comando allowlisted de cada ferramenta em `docs.yml`.
- Agregar os resultados irmãos; o card pai verifica o conjunto em etapa separada.
- **Julgar compatibilidade entre a versão resolvida de uma ferramenta e a versão resolvida do runtime.** Nenhum ramo deste card compara as duas: o que é verificado é consistência de resolução (toda família usada tem exatamente uma versão exata resolvida, todo runtime resolvido é usado, e toda entrada `tool[]` cita a mesma família e a mesma versão). A lacuna está declarada em `limits[6]` do lock e na seção 6 do spec, com a medição que mostra por que delegar ao resolvedor também não a fecharia; o caso `disclosed_runtime_not_compatibility_checked` a executa e **passa** de propósito.

## Preconditions

- As dependências `[AUR-359]` estão aprovadas na mesma CandidateIdentityV1, quando não vazio.
- A fixture `tests/bootstrap/locks/AUR-364/cases.tsv` contém nominal, ausência, symlink/tamper e primeiro valor acima do limite; nenhuma entrada contém credencial real.

## Postconditions

- cada ferramenta possui versão e artifact digest, runtime resolvido de forma consistente, comando allowlisted e output schema; nenhuma instalação ocorre durante o aceite.
- O input observado e todos os paths fora de `paths` permanecem byte-idênticos; não há processo, rede ou secret sink não declarado.

## Acceptance scenarios

### AC-001: Fixar ferramentas de documentação

- Given: `tests/bootstrap/locks/AUR-364/cases.tsv` sela source/input digests, resultados e códigos esperados, limitada a 256 entradas, 4 MiB e 15 s.
- When: `VerifyBootstrapLockV1` valida em `.board/bootstrap/locks/docs.yml` a versão e o artifact digest de cada ferramenta, a consistência de resolução do runtime da família (exatamente uma versão exata por família, toda família usada resolvida, todo runtime resolvido usado, e `tool[].runtime_version` igual à da família), o comando allowlisted e o output schema, sem instalar pacote algum.
- Then: o nominal retorna `pass` e cada ferramenta possui versão e artifact digest, runtime resolvido de forma consistente, comando allowlisted e output schema; nenhuma instalação ocorre durante o aceite; ausência, tamper/symlink, campo inválido e over-limit retornam respectivamente `docs_tool_lock_incomplete`, `source_integrity_error`, `invalid_lock_or_manifest` e `input_limit_exceeded` antes de efeito externo.
- Covers requirements: [PR-SUP-001, PR-ARCH-001]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001, CR-TRUST-001]

## Public contract

- Introduz somente o manifesto/lock card-scoped e seus códigos `docs_tool_lock_incomplete|source_integrity_error|invalid_lock_or_manifest|input_limit_exceeded`; consumers recebem digests, counts e dispositions, nunca conteúdo sensível.

## TDD proof

- Test: `tests/acceptance/AUR-364.sh::AC-001`.
- Red: com o arquivo de teste executável e o parser alcançável, a ausência do manifesto/lock esperado sai 1 com `AUR-364/AC-001/docs_tool_lock_incomplete`; engine indisponível, permissão ou erro de loader invalidam essa prova.
- Green: produzir o lock .board/bootstrap/locks/docs.yml e validar cada ferramenta possui versão e artifact digest, runtime resolvido de forma consistente, comando allowlisted e output schema; nenhuma instalação ocorre durante o aceite antes de qualquer consumo do domínio.
- Refactor: após GREEN, reordenar entradas canonicamente e preservar identity set, error codes e manifest digest no replay; aplicar MUT-001, observar o erro comportamental, restaurar e reproduzir o digest original.
- Unit: not-applicable: a unidade é um artefato estático validado integralmente pelo aceite bounded.
- Contract: `tests/acceptance/AUR-364.sh::ContractAUR364`.
- Integration: `tests/acceptance/AUR-364.sh::IntegrationAUR364`.
- E2E: `tests/acceptance/AUR-364.sh::E2EAUR364`.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-364`

Expected artifact: o coordinator deriva `.board/evidence/AUR-364/acceptance/AC-001.json` do stdout bounded, valida o schema e vincula CandidateIdentityV1; builder e teste não escrevem evidence.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: adicionar comando de documentação no workflow sem atualizar o lock.
- Expected: o aceite sai 1 com `AUR-364/AC-001/docs-tool-unlocked`; restore recupera cobertura.

### MUT-002: AC-001 / documentation-tool

- Change: quebrar a consistência de resolução do runtime — resolver o mesmo nome de ferramenta em versões divergentes em dois manifestos da mesma família, ou remover a resolução da família Ruby usada pelos manifestos. Não é um julgamento de compatibilidade: ver Non-goals.
- Expected: o aceite sai 1 com `AUR-364/AC-001/runtime-resolution-inconsistent`; restore recupera a resolução consistente e reproduz o digest original. Os dois ramos são exercitados separadamente por `mut002_cross_manifest_resolution_conflict` e `mut002b_ruby_runtime_unresolved`, cada um exatamente pelo ramo que seu nome descreve.

### MUT-003: AC-001 / supply-chain

- Change: permitir instalação de Pagefind sem versão/digest.
- Expected: o aceite sai 1 com `AUR-364/AC-001/docs-tool-mutable`; restore exige artifact imutável.

## Security and privacy

- Data flow: Jekyll, Bundler, Pagefind e extratores externos de documentação -> parser/probe bounded -> digests, counts, status e disposition sanitizados.
- Secrets: `AURUM_SECRET_CANARY` e valores credential-shaped não aparecem em stdout, stderr, manifest, logs ou evidence; somente classes/counts são permitidas.
- Least privilege: worker non-root, rootfs e checkout read-only, rede none, capabilities ALL dropped, no-new-privileges, PIDs/memória/CPU/deadline bounded; nenhuma publicação.

## Documentation

- `docs/specs/AUR-364.md` documenta schema, bounds, códigos, exemplo sintético, diagnóstico e comando OCI offline.

## Compatibility, migration, rollback

- Compatibility: contratos e bytes fora do Public contract permanecem iguais; divergência falha AC-001.
- Migration: consumidores migram somente depois que o agregador AUR-233 aceita todos os lock manifests.
- Rollback: remover somente o manifesto/lock e derivados deste card, restaurar qualquer source mutation e repetir AC-001; não há estado externo.

## Review

- Reviewer A: cada hunk e as dez dimensões do protocolo, priorizando correção, SOLID, compatibilidade, determinismo e sensibilidade dos testes.
- Reviewer B: cada hunk e as dez dimensões do protocolo, priorizando entradas hostis, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: ambos recebem a mesma CandidateIdentityV1 selada, mas usam processos, sessões, conversas, caches, memória e relatórios separados; registrar backend real e nível I0/I1/I2/I3.
- Skeptical approver: pré-sela hipóteses, executa todas as mutações em OCI limpo, restaura e repete o aceite; falha, divergência ou inconclusivo veta.

## Evidence

Somente o coordinator grava artefatos sanitizados em `.board/evidence/AUR-364/`: spec/identity seladas, red-green-refactor, clean tree, gates, coverage maps A/B, challenge pré-selado, mutações, replay restaurado, hashes e verdicts. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

