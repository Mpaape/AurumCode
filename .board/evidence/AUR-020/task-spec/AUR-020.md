---
id: AUR-020
version: 2
title: Escolher memória e mapa incremental mínimos
status: done
office: O00-research
depends_on: []
requirements: [PR-ARCH-001, PR-EVD-001, PR-MEM-002]
controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]
paths: [.board/research/memory-design.md, .board/decisions/ADR-0001-optional-local-memory.md, standards/memory, tests/acceptance/AUR-020.sh, docs/specs/AUR-020.md, tests/specs/AUR-020, tests/integration/AUR-020_test.go]
forbidden_paths: [.git, .env, credentials, secrets, .board/cards]
base_sha: ac79f1719544176d6c341aa3c892e1fab120ac0e
spec_digest: sha256:97786b4bc7b59baea8b53b7227dd85b6909a8576b2bc03c7ac396902fab79043
risk: high
data_class: internal
trust_boundaries: [repository, network-source]
---

## Outcome

ADR compara Aider, Karpathy, Hermes, Tree-sitter e SCIP e fecha SQLite FTS plus grafo.

## Non-goals

- Este card não constrói o índice SQLite FTS nem o grafo incremental decididos na ADR; implementar o mapa de repositório e sua atualização cabe aos cards de indexação.
- Não agregar outro resultado implantável; expansões usam cards dependentes.

## Preconditions

- o snapshot base e a policy bootstrap estão disponíveis.
- O teste de aceite está selado, a árvore está limpa e a CandidateIdentityV1 foi calculada sem valores sensíveis.

## Postconditions

- ADR compara Aider, Karpathy, Hermes, Tree-sitter e SCIP e fecha SQLite FTS plus grafo.
- Arquivos fora de `paths` permanecem byte a byte inalterados e nenhuma capacidade extra é concedida.

## Acceptance scenarios

### AC-001: Escolher memória e mapa incremental mínimos

- Given: `.board/research/memory-design.md` traz, para Aider, Karpathy, Hermes, Tree-sitter e SCIP, fonte com versão e data, custo de indexação e escopo de consulta, e `standards/memory` declara os limites de tamanho de índice e de janela de contexto a respeitar.
- When: o lint de `tests/acceptance/AUR-020.sh` resolve cada alternativa citada na ADR para exatamente uma linha da matriz de critérios e confere que `.board/decisions/ADR-0001-optional-local-memory.md` só invoca critérios presentes nessa matriz, sem acesso de rede.
- Then: a ADR fecha em SQLite FTS mais grafo com justificativa por critério e cada alternativa rejeitada nomeada; alternativa sem fonte versionada, critério citado fora da matriz ou limite ausente em `standards/memory` falha por `AUR-020/AC-001/undecided-criterion`.
- And: `invalid` e o primeiro valor além de `boundary` retornam erro tipado antes de efeito proibido; replay da mesma CandidateIdentityV1 preserva exit code, ordem e digest.

- Covers requirements: [PR-ARCH-001, PR-EVD-001, PR-MEM-002]
- Covers controls: [CR-EVD-001, CR-EVD-002, CR-EVD-003, CR-REV-001, CR-REV-002, CR-REV-003, CR-REV-004, CR-REV-005, CR-GATE-001]

## Public contract

- Somente o contrato “Escolher memória e mapa incremental mínimos” e os artefatos em `.board/research/memory-design.md`, `.board/decisions/ADR-0001-optional-local-memory.md`, `standards/memory`, `tests/acceptance/AUR-020.sh`, `docs/specs/AUR-020.md`; consumidores e formatos não listados permanecem inalterados.

## TDD proof

- Test: `tests/acceptance/AUR-020.sh::AC-001`.
- Red: com teste e vetores selados, a ausência do comportamento no boundary público faz AC-001 alcançar a chamada e falhar com `AUR-020/AC-001/behavior-missing`; compilação, loader, dependência, permissão, engine ou infraestrutura invalidam o RED.
- Green: Escrever `.board/decisions/ADR-0001-optional-local-memory.md` comparando Aider, Karpathy, Hermes, Tree-sitter e SCIP por critério explícito e fechando em SQLite FTS mais grafo, com os limites codificados em `standards/memory`.
- Refactor: após RED→GREEN, aplicar `MUT-001` (“introduzir vector DB obrigatório no baseline.”); o mesmo acceptance deve falhar por divergência comportamental, e a restauração deve reproduzir exatamente o GREEN.
- Unit: not-applicable: o artefato é policy/dado; o lint estrutural é o teste focado.
- Contract: not-applicable: nenhum port ou wire contract é criado.
- Integration: `tests/integration/AUR-020_test.go::TestAUR020Boundary`.
- E2E: not-applicable: este nó não entrega sozinho um use case público.

## Acceptance

container_profile: `bootstrap-readonly-v1`

accept: `./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-020`

Expected artifact: `.board/evidence/AUR-020/acceptance/AC-001.json` valida contra o schema, contém o resultado observável de “Escolher memória e mapa incremental mínimos” e vincula CandidateIdentityV1.

## Skeptical mutations

### MUT-001: AC-001 / repository

- Change: introduzir vector DB obrigatório no baseline.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-020/AC-001/MUT-001`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

### MUT-002: AC-001 / network-source

- Change: substituir a referência pesquisada por uma URL não allowlisted ou conteúdo sem versão.
- Expected: o mesmo `accept` sai diferente de zero, registra `AUR-020/AC-001/MUT-002`, depois da restauração volta a passar e não aceita falha de compilação/infraestrutura como detecção.

## Security and privacy

- Data flow: entradas limitadas das fronteiras `repository`, `network-source` -> contrato “Escolher memória e mapa incremental mínimos” -> artefato sanitizado e card-scoped.
- Secrets: o canário `AURUM_SECRET_CANARY` não aparece em stdout, stderr, finding, log, cache, memória ou evidência.
- Least privilege: somente `paths` é gravável pelo builder; rede, engine socket, host mounts, devices, capabilities e publicação são negados salvo autorização explícita deste TaskSpec.

## Documentation

- Atualizar `docs/specs/AUR-020.md` com o comportamento, contrato, limites, exemplo sem segredo e procedimento de diagnóstico; o exemplo é executado pelo mesmo aceite.

## Compatibility, migration, rollback

- Compatibility: contratos fora do Public contract permanecem inalterados; qualquer diferença não declarada falha AC-001.
- Migration: not-applicable neste card; migração de estado/legado exige card próprio.
- Rollback: reverter somente `paths`, remover artefatos derivados card-scoped e repetir AC-001 no base; nenhum estado externo é publicado por este card.

## Review

- Reviewer A: revisão completa das dez dimensões e de cada hunk, priorizando correção, SOLID, compatibilidade e sensibilidade do teste.
- Reviewer B: revisão completa das dez dimensões e de cada hunk, priorizando entrada hostil, segredos, permissões, supply chain, resiliência e fail-closed.
- Independence: mesmo CandidateIdentityV1 selado; processos, sessões, provider conversations, caches e memória separados; registrar I0/I1/I2/I3 sem tratar aliases do mesmo backend como diversidade.
- Skeptical approver: selar hipóteses antes dos reports, executar todas as MUT em OCI limpo, restaurar e repetir; falha ou inconclusivo veta.

## Evidence

Persistir somente artefatos sanitizados em `.board/evidence/AUR-020/`: TaskSpec selado, CandidateIdentityV1 completa, red/green/refactor, clean-tree, gates, coverage maps integrais A/B, challenge plan pré-selado, mutações, replay restaurado, hashes e verdicts. Estado `done` exige o bundle de evidência recomputável ligado à identidade exata. Nunca persistir segredo, env, prompt/resposta brutos ou chain-of-thought.

