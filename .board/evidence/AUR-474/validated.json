{
 "schema": "aurum.delivery-record",
 "version": 1,
 "card": "AUR-474",
 "commit": "02bea07ea7816af28acda59e50c366bbcb4179d0",
 "review": "approved",
 "validation": "passed",
 "validator_run": {
  "command": "./.board/bin/oci-run --profile bootstrap-readonly-v1 --card AUR-474",
  "exit_code": 0,
  "raw_output": "raw exit 0 on the integrated SHA"
 },
 "review_notes": {
  "false_positive_sweep": "The reviewer drove matchBashFunction directly with eight shapes beyond the card's list -- my-array=(), x=$(cmd), a one-line if, a quoted string containing foo() { }, a case arm, f() ( subshell ), an alias, and a comment -- and none became a symbol.",
  "predates_this_card": "greet() with no body does match, but the reviewer confirmed against HEAD~1 that the trailing brace was already optional, so it is not a regression this card introduced.",
  "powershell_claim_verified": "git diff shows no file under extractors/powershell changed, and powerShellFunctionPattern carries no end-of-line anchor, which is why it already matched one-liners. The builder pinned that with a regression test instead of touching working code.",
  "neighbour_table_intact": "AUR-464's four-case table still passes with its own untouched script. Recognising more symbols changes when the first symbol occurs, which is what could have shifted that table, and it did not.",
  "declared_gap": "A body that opens on one line and closes on another is still unrecognised -- deliberately out of scope, since this card is not a bash parser."
 }
}
