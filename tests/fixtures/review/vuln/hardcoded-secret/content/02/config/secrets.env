# Synthetic values only, planted so AUR-442's hardcoded-secret matcher has
# a real unquoted KEY=VALUE line to find. Never a real credential.
SERVICE_NAME=aurum-fixture
DB_PASSWORD=AURUM-FAKE-PASSWORD-9000-1111
