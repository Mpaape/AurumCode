// Bounded, non-sensitive fixture corpus for the AUR-363 C++ adapter lock.
// It exists only to be digested, never parsed.
#pragma once

/// Returns a static greeting.
inline const char *greet(const char *name) { return "hello"; }
