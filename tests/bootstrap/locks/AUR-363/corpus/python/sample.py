"""Bounded, non-sensitive fixture corpus for the AUR-363 Python adapter lock.

It exists only to be digested, never parsed.
"""


def greet(name: str) -> str:
    """Return a static greeting."""
    return f"hello, {name}"
