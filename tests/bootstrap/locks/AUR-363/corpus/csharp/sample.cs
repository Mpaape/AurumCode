namespace Sample
{
    /// <summary>
    /// Bounded, non-sensitive fixture corpus for the AUR-363 C# adapter
    /// lock. It exists only to be digested, never parsed.
    /// </summary>
    public static class Greeter
    {
        public static string Greet(string name) => $"hello, {name}";
    }
}
